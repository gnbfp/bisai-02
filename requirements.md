# 需求真源 · 小组作业项目经理机器人

> **新来的先读 `AGENTS.md`（入口与当前状态），名词看不懂查 `docs/GLOSSARY.md`。**
> **本文件是需求的唯一真源。** 任何 AI 或协作者动手前必须先读完全文。
> **主要依据 = 方案层定稿文档**（用户 2026-09-11 确认）。原 `docs/定稿文档-v2.md` **已于 2026-09-15 移除**，见 §10。
> 需求冲突时，以 `docs/requirements/` 下的两张截图原件为准。
> 截图之外的冲突**一律听定稿文档**（《方案一.docx》与旧对话都让位），见 D-32 / §10。
> **本文件没有写到的地方，禁止臆想。** 见 §8 未定义清单，遇到就停下问人。
> 历史清理：v1（三 Agent 架构版）与旧架构稿已作废并删除，本仓库不再存在。

---

## 0. 一句话

小组在飞书群里丢一份作业书 → 机器人抽出评分点 → 拆成任务卡 → 群里投票定方向 → 私聊收志愿并自动分配 → 执行期催办 → 输出执行报告。

机器人是**"群里的第 N+1 个成员"**：智能只押在拆解（M3），聊天只是交互层；它不提供思路、不替人写作业，只主持流程、匿名代言、发产物。人只做两个动作——**私聊填志愿、群里投票**。

---

## 1. 硬性边界（不可突破，B = Boundary）

| 编号 | 边界 |
|---|---|
| B1 | 系统 = **1 个 Agent + M0–M8 九个模块**，单进程 Python 服务。**不是三 Agent**；不存在 Brain / Feishu / Data 的进程或目录划分 |
| B2 | 全项目**只有三处 LLM 调用**：M1 输入解析、M2 方向候选、M3 任务拆解。**M4–M7 是纯规则与模板，零智能** |
| B3 | M8 **不在运行时**，是离线评测工具，旁挂在主链路之外 |
| B4 | 数据存 **JSON 文件**（落盘位置统一为仓库根目录 `data/`，见 `docs/ARCHITECTURE.md` §4），**不用数据库** |
| B5 | 飞书机器人是**唯一 UI**；收消息走 **WebSocket 长连接**，不用公网回调。（2026-09-11 用户确认：**飞书侧已实测通过**，草稿机器人可运行） |
| B6 | 存在**命令行主链路**（作业书→任务卡→核对清单），与飞书群入口并行 |
| B7 | **不写飞书文档。** v1 的"周报写入飞书文档"已作废，**不需要 `docx` 文档权限** |
| B8 | **LLM 不判定自己的产出。** 判定权在代码（M3 循环内）或独立脚本（M8） |
| B9 | **拍板永远在人。** 机器兜底到建议为止 |
| B10 | 指令用**前缀精确匹配**，不做自然语言理解 |

---

## 2. 五层结构

| 层 | 模块 | 性质 |
|---|---|---|
| 交互层 | M0 机器人网关 | 身体：消息循环 + 指令路由 |
| 智能层 | M1 输入解析 / M2 方向共识 / M3 任务拆解 | 大脑：仅有的三处 LLM 调用 |
| 协作层 | M4 志愿分配 / M5 匿名代言 | 手脚：纯规则状态机 |
| 流程层 | M6 催办 | 手脚：定时扫描 |
| 交付层 | M7 执行报告 | 手脚：模板渲染 |
| （旁挂）证据层 | M8 评测集 | 离线，不进运行时 |

---

## 3. 用户场景（逐条来自截图，未加推断）

| # | 场景 | 截图来源 |
|---|---|---|
| S1 | 机器人拉入测试群，发送任意消息 → 机器人回复指令列表（5 条） | T01 / M0 |
| S2 | 群里发一份作业书 → 抽出评分点，产出 ≥3 张任务卡，每张含评分点溯源字段 | T02 / M1 / M3 |
| S3 | 输入含明确评分占比的作业书 → 每个评分点至少映射一张任务卡，覆盖率可复算 | T03 |
| S4 | 作业书含模糊要求（如"内容充实"）→ 不硬拆，标记为"需组长确认" | T04 |
| S5 | 群里发「方向」口令 → 出 2–3 个候选方向，回复数字投票，过半落定 | T05 / M2 |
| S6 | 3 人私聊填志愿且无冲突 → 按志愿直接分配，人人有任务 | T06 / M4 |
| S7 | 2 人私聊抢同一模块 → 先到先得，后到者落入第二志愿 | T07 / M4 |
| S8 | 有人不回复志愿私聊 → 兜底分配，仍保证每人至少 1 个任务 | T08 / M4 |
| S9 | 私聊发「我想提议：...」→ 群里匿名发布，不泄露姓名；系统内部有记录 | T09 / M5 |
| S10 | 模块 A 依赖模块 B → 任务卡 `depends_on` 正确，甘特图体现先后 | T10 |
| S11 | 任务临期未标记完成 → 群内 @负责人提醒真实送达（有送达记录） | T11 / M6 |
| S12 | 全流程跑完一份作业书 → 输出核对清单，覆盖率数字与脚本复算一致 | T12 / M7 |
| S13 | 发一篇非作业书文本（如新闻）→ 拒拆或要求确认，不硬拆 | T13 |

> 注：S1 / T01 里的"5 条"是截图原文。后来加的两条指令（`完成 T3` / `登记`）的由来见 §7.1、§7.7 与 D-22 / D-34。

---

## 4. 模块与优先级（原文照录）

| 功能 | 优先级 | 完成定义 |
|---|---|---|
| M0 机器人网关 | P0 | 测试群收发消息跑通，5 条指令全部路由正确 |
| M1 输入解析 | P0 | 3 份真实作业书评分点全部抽出，字段校验通过 |
| M2 方向候选（简版） | P0 | 触发口令出 2-3 个候选，回复数字投票可收敛 |
| M3 任务拆解 | P0 | 5 份作业书跑通自检循环，覆盖率达标（初始参数 80%，待校准） |
| M4 志愿私聊指令 | P0 | 分配规则全过 T06-T08 |
| M5 匿名代言 | P0 尾部 | 私聊提议 → 群里匿名发布，不泄露姓名；进度落后时第一个牺牲 |
| M7 执行报告（简版） | P0 | 命令行跑出评分点核对清单 |
| M8 评测集（前 5 份基线） | P0 | 人工拆解基线标好，覆盖率脚本可复算 |
| M6 催办 @提醒 | P1 | 群里 @负责人真实送达，两档临期提醒 |
| 投票升级交互卡片 | P1 | 从"回复数字"升级为点选卡片 |
| M7 执行报告（完整版） | P1 | 分配总表 + 甘特图成品发群 |
| 组长查询匿名提交者 / 改派 | P1 | 私聊查询口令 + 改派指令 |
| M6 全量评测（15 份） | P1 | human vs agent 对比表产出 |

> 注 1：M0 完成定义里的"5 条指令"是截图原文。后来另立两条：第 6 条 `完成 T3`（定稿 §3.7 / D-22）、第 7 条 `登记`（D-34）。见 §7.1 与 §7.7。
> 注 2：表末"M6 全量评测（15 份）"的编号有误，实为 **M8** 的扩量评测（定稿 §3.9 / D-27）。原文照录不改字，勘误在此登记。
> 注 3：**M2 方向候选 + 群内投票已落地**（2026-09-14）。实现见 `src/intelligence/direction.py`（唯一 LLM 点）+ `src/gateway/vote.py`（纯规则），决策见 **D-61 / D-62**。

---

## 5. 验收标准

**T01–T13 全表通过即为验收完成。** 用例原文见 `docs/requirements/README.md`。

- **验收方式 = 人工验收清单，不是自动化测试脚本**（定稿 §8「验收纪律」）：每条用例跑一次、记录一次结果，演示材料里附 **T01–T13 执行记录表**。这套做法采纳自"研采"方案的方法论（D-26）
- 用例与模块的对应（定稿各节"验收"行）：T01→M0、T02–T04→M1、T05→M2、T06–T08→M4、T09→M5、T11→M6、T12→M7
- **勘误**：定稿 §3.4（M3）那节写"验收：T06–T08、T13"，与 §3.5（M4）的用例撞车，判定为笔误；M3 由 T02 / T03 覆盖（任务卡 + 覆盖率）

---

## 6. 数据模型

### 6.0 作业元信息 assignment meta —— M1 产出

定稿 §3.2：M1 的输出除评分点清单外，还必须含**课程名、作业标题、交付形式、截止时间**。

```json
{ "course": "编译原理", "title": "C 语言课程设计",
  "submission": "源码 + 报告", "deadline": "2026-09-19T23:59",
  "source_file": "作业书.pdf" }
```
- 落盘 `data/assignment.json`：写入 M1；读取 M6（催办）、M7（分配总表 / 甘特图截止线）
- **截止时间只有作业级这一个**——一份作业一根截止线。任务卡**不设** `due_date`（D-23）；定稿 §3.4 的任务卡字段表里也确实没有这一项
- `deadline` **可为空字符串**（D-49）：原文没写截止时间就不要编 —— schema 不再要求非空，报告显示「未标注」；`course` / `title` / `submission` / `source_file` 仍必填。编出来的占位值（实测出现过 `1970-01-01T00:00`）由 `check_deadline()` 软校验点名

### 6.1 评分点 rubric point —— M1 产出

```json
{ "id": "R1", "quote": "作业书原文片段", "weight": 40,
  "observable": "可观察的验收描述", "status": "normal" }
```
- `quote` 必须是作业书**原文**，用于防 LLM 编造评分点（D-03）
- `status` ∈ `normal`（可拆）/ `ambiguous`（模糊，如"内容充实""学习态度、遵守纪律"）（D-19）。**判据见 D-50**：看有没有具体交付物，不看标题虚不虚；拿不准填 `normal`
- `weight` 是**可选**的——定稿 §3.2 原文写的是"分值**或**权重"，作业书未必标占比（如 T02 那份）
  - 缺失的点不参与均衡计算；权重软校验**全部缺失时整体跳过、部分缺失时改软警告**（§7.5，D-48）
- **来源定义（D-48）**：评分点 = 作业书里「评分标准 / 成绩评定 / 考核方式 / 评分细则 / 评分表 / 评分依据」这类小节中的评分项，或全文中其他**带明确分值 / 百分比 / 权重**的评分项。**不是评分点**（即使原文写"必须 / 应 / 需"）：交付与提交要求、格式要求、字数页数、页眉页脚、装订方式、学术诚信与抄袭条款、参考文献规范、技术选型建议、参考资料链接、课程目的 / 目标、分组要求、选题建议。**一条评分点 = 一个评分项**（表格一行 / 编号一段 / 并列一个分项）；一句话里并列的多个分项各自出一条，同一项不许重复、也不许把一句话切成多条
- **全文没有评分标准时**：`rubric` 返回**空数组**（**不许拿正文要求凑数**），M3 拒拆、网关回「没找到评分标准」，见 D-48

### 6.2 任务卡 task card —— M3 产出（7 字段，定稿 §3.4）

```json
{ "task_id": "T1", "module_name": "实现登录模块",
  "rubric_refs": ["R1","R3"], "effort_hours": 6, "depends_on": ["T2"],
  "deliverable": "一个源文件", "acceptance": "从 R1 原文改写，可被核对清单复用" }
```

| 字段 | 类型 | 说明 |
|---|---|---|
| `task_id` | string | `T1`、`T2`…… 全局唯一 |
| `module_name` | string | 模块名，**动词开头**（如"实现登录模块"） |
| `rubric_refs` | array | **必填**。溯源字段：对应评分点编号 **+ 原文引用**。演示时评委看的就是它——证明 agent 真读懂了作业书 |
| `effort_hours` | number | 工作量估计（人时）。只用于均衡计算与甘特图，**不对外承诺** |
| `depends_on` | array | 前置任务的 `task_id` 列表；空数组 = 可立即开始 |
| `deliverable` | string | 这块交什么：一个源文件 / 一段报告章节 / 一页 PPT |
| `acceptance` | string | 怎么算做完。从对应评分点原文改写，**可被核对清单复用** |

- `rubric_refs` **落盘只存 `id` 数组**（`["R1","R3"]`），**展示时才 join 出原文引用**。理由：§7.3 的 `check()` 是 id 集合运算（`eligible <= covered`），数组里塞不得原文（D-03 / D-24）
- "模块"是任务卡面向组员的通俗叫法，**数据模型里就是 `task_id`**（D-14）

### 6.3 志愿 preference（D-14）

```json
{ "user_id": "u_张三", "ranked_task_ids": ["T2","T5"], "submitted_at": "ts" }
```

- **后投覆盖先投**：同一个 `user_id` 再发一次就整条替换（`submitted_at` 刷新），不做版本历史 —— 与 M2 改票同款（D-33）
- 花名册外的人不进分配：旧名单残留的志愿、陌生人的私聊一律忽略（D-53）

### 6.4 分配 assignment（D-14 / D-20）

```json
{ "task_id": "T2", "assignee": "u_张三", "source": "volunteer_1",
  "completed_at": null }
```
- `source` ∈ `volunteer_1`（第一志愿命中）/ `volunteer_2`（落入第二志愿）/ `auto`（未填表兜底）/ `leader`（组长指定与改派，P1）
- 改派时 `source` 翻成 `leader`，**不留 history**（P1 不做审计链）
- `completed_at`：`null` = 未完成。组员私聊「`完成 T3`」（§7.1 第 6 条）把它写成时间戳。M6 靠它判"临期**未**完成"，M7 核对清单靠它画勾；**MVP 不做提交物上传**（D-31）。**M4 的分配语义不看它** —— 它只被 M6 / M7 读

- **怎么分（S6–S8，见 D-53）**：① **先到先得** —— 按 `submitted_at` 升序逐个人处理，取他志愿里第一个还没被占的卡（命中第 1 个记 `volunteer_1`、第 2 个记 `volunteer_2`；`source` 枚举里没有 volunteer_3，**第 2 个及以后一律记 `volunteer_2`**；`auto` **严格只留给未填表的人** —— 这样总表末行"几人被兜底"才等于"几人没填表"）；② **兜底** —— 剩下的卡按 `effort_hours` 升序（并列按 `task_id`）分给"当前手上卡数最少的人"（并列按花名册顺序），没填志愿的人因此拿到 `auto`
- 一张卡只能有一个 `assignee`；`depends_on` **不参与分配**（只影响甘特图）。**卡数 < 人数**时做不到"人人有份"，分配总表如实标「无任务」，不硬塞

### 6.5 提议 proposal —— M5 产出（待定义-8 已关闭）

- 落盘 `data/proposals.json`：写入 M5；读取 M5 与**组长查询**
- **必须留痕真实身份**：群里匿名发布，但 `data/proposals.json` 里记下提交者 `user_id`（定稿 §3.6「防滥用：系统内部记录提交者，组长可查询」）
- 发布文案是"有组员提议：……"，内容**原样转达**，不修改、不总结、不加工
- 组长查询**为 P1**；查询口令名尚未拍板（见 §8.1 待定义-36）

### 6.6 花名册 roster —— M0「登记」产出（D-34）

```json
{ "leader": "u_张三",
  "members": [ { "open_id": "u_张三", "name": "张三" },
               { "open_id": "u_李四", "name": "李四" } ],
  "registered_at": "ts", "confirmed_by": "u_张三" }
```
- 落盘 `data/members.json`：写入 M0（登记流程）；读取 M2（投票分母）、M4（私聊广播对象）、M5（组长身份）、M6（@对象）、M7（分配总表上的名字）
- `leader` 有且只有一个；`members` 至少 2 人
- 同时存 `open_id`（用来私聊 / @）和 `name`（用来给人看：分配总表、甘特图）
- **重发「登记」即整体覆盖**，不做版本历史

---

## 7. 关键机制

### 7.1 指令路由表（前缀精确匹配 + 状态优先，不做自然语言理解）

| 前缀 | 目标 | 说明 |
|---|---|---|
| `作业书` + 文件 | M1 | 解析评分点，清单**发群全员可见**（群 / 私聊都行） |
| `方向` | M2 | 基于评分点生成 2–3 个候选大方向并发起投票 |
| `拆解` | M3 | 评分点 → 任务卡（**不是**"评分为"） |
| `你想做哪一块` | M4 | 志愿填报（**私聊**） |
| `我想提议：…` | M5 | 匿名代言（**私聊**） |
| `完成 T3` | M6 | 标记任务完成（**私聊**，MVP 不做提交物上传） |
| `登记` + @组员 | M0 | 建 / 更新花名册（**群聊**，两步确认，见 §7.7） |
| `报告` | M7 | 执行报告（分配总表 + 核对清单 + 甘特图）**发群**（**组长**口令；第 8 条顶层指令 —— D-64） |

> **关于条数**：T01 截图原文写"指令列表（5 条）"，指的是 M0 指令集那 5 条（`作业书` / `方向` / `拆解` / `你想做哪一块` / `我想提议：`）。此后又长出三条：`完成 T3`（定稿 §3.7，T11 靠它才可测 —— D-22）、`登记`（建花名册 —— D-34）和 `报告`（组长要执行报告 —— D-64）。**路由表实收 8 条**；T01 的口径如何改述，见 §8.1 待定义-37。**`封盘` 不是顶层指令**，只在 `awaiting=preference` / `awaiting=vote` 两个状态机内被消费，不进前缀表（D-52 / D-56 与本节口径一致）。

**「`作业书` + 文件」—— 实测结论（2026-09-12）**

飞书**一条消息只能是一种类型**：要么纯文字、要么纯文件。用户实测确认：**客户端不允许一条消息里同时放文字和附件**。所以"说一句 `作业书` 同时挂上附件"**必然拆成两条消息**发出：

    [群] 张三：[附件] 课程设计报告.pdf     ← 没有文字
    [群] 张三：@机器人 作业书               ← 没有附件

**但两条机器人都收得到**（实测 `python -m tools.probe_feishu --echo`）：6 个测试事件全部收到，其中 3 条文件消息的 `file_key` **全部下载成功**（落盘 `data/probe/`，各 1.0 MB）⇒ **文件下载权限是通的，主链路第一环不会断**。

**⇒ 对 M0 的硬要求**：必须**缓存"最近一条收到的文件消息"**，等紧随其后的 `作业书` 文字指令到了再配对（群、私聊同理）。路由表不改，但这条配对逻辑原先文档里没有，实现时别漏。

> **为什么群里不带 @ 也收得到（2026-09-12 已查明）**：后台实际开通的是 **`im:message.group_msg.include_bot:read`**（获取群组中用户和机器人发送的消息）+ **`im:message:readonly`**（获取单聊、群组消息），而不是只收 @ 的那条。⇒ 机器人**读得到群里的全部消息**，`@` 只是给人看的习惯。**两条后果**：① §7.1 已记录的"投票窗口内闲聊里的纯数字会被计成票"**是真实风险**，不是理论风险（备选方案见 §8.1）；② 权限名里的"**和机器人发送的消息**"意味着**机器人自己的消息也可能作为事件回来** —— M0 路由必须**先丢弃 `sender_type == "app"` 的事件**，否则会自己回自己。


未命中任何前缀 → 回复"我只会这几件事" + 指令列表（T01）。这是刻意的产品性格，不做开放式闲聊兜底。

**路由优先级：先看状态，再看前缀（B10 的补充 —— D-33）**

两条补充：**先把 @段 摘掉**，以及**裸数字要靠状态解释**。

0. **先剥 @段，再做前缀匹配**：`@小组作业机器人 拆解` 要能认成 `拆解`。但 **@ 到的人要单独留着**——每个人的 `open_id` 就在 @ 结构里，「登记」表单全靠它拿 id（见 §7.7）
1. 再查 `data/state.json` 有没有 `awaiting`（当前在等什么）：
   - `awaiting = vote`（群里正在投票）→ 纯数字 = **票号**
   - `awaiting = preference`（刚问过"你想做哪一块"）→ 纯数字 = **志愿序号**，**且只有私聊（`chat_type == "p2p"`）算数**；群里照走 7 条前缀（窗口长达 5 小时，不限定会话的话群里谁打一个数字都会被记成序号 —— D-54）
   - `awaiting = register`（群里正在走登记表单）→ 这条消息按**表单内容**解析
2. 不在等数字 / 等表单 → 走上面的前缀精确匹配（7 条）
3. 仍然命中不了 → 回复指令列表（T01）

**投票窗口（M2）**

- **开启**：M2 把候选方向发到群里的那一刻
- **分母 = 已投票的人**（D-35，用户 2026-09-11 拍板）：某个方向拿到**已投票者过半**（票数 > 已投票数 ÷ 2）即落定
- **关闭**：满足任一即关 ——
  1. 有方向拿到**已投票者过半** → 该方向落定
  2. **超时**（默认 10 分钟）→ 无人过半时**冻结窗口**（不是清空），见下
  3. **组长直接拍板**
- **窗口内的规矩**：一人一票；**后投覆盖先投**（改票不用特殊指令）；窗口一关，裸数字立刻不再被当成票
- **超时且无人过半**（例如 3 人投出 3 个不同方向）→ 机器人报票数明细 + **冻结窗口**（`state.vote.closed = true`：数字不再计票，候选与票数原样保留）→ 组长在**同一批候选、同一张票数表**上 `封盘` / `封盘 N` 拍板（`reason = 超时后组长指定`），或任何人再发一次「方向」开新窗口（B9：拍板永远在人）
- **⚠️ 我加的一道保险（工程默认 D-36，可推翻）**：按"已投票者过半"，**第 1 个人投完就已经"过半"了**（1 > 0.5），投票会当场结束。所以默认加一道门槛：**参与投票的人数要达到花名册人数的过半（向上取整），才允许按"已投票者过半"落定**；没到门槛就等满 10 分钟。花名册来自 §6.6。**不想要这道门槛，删掉这一句即可**（那就变成"谁先投谁定"，演示上更干脆、但一人就能带走方向）
- **已知残留风险**：群里**不强制 @机器人**，所以窗口内别人闲聊打出的纯数字会被计成票。兜底 = 窗口短 + 一人一票 + 后投覆盖；若实测误判严重，再启用 §8.1 的备选 1（@机器人才算指令）

- **实现口径（D-61，`src/gateway/vote.py`）**：
  - **收票只认开窗那个群 + 花名册成员**（两个条件同时成立）：私聊的裸数字照走 7 条前缀（投票是群里的动作）；别的群 / 非花名册成员的裸数字**静默不计**（不刷屏、不回话）；花名册成员发越界数字 / 非数字 → 回一句提示、**不落盘**；一人一票、后投覆盖
  - **组长拍板复用 M4 的 `封盘`**（不再新增第 8 条口令）：`封盘` 取票最多（并列取**编号小**），`封盘 N` 直接指定（**没票也能用**）；`封盘` 且一票都没有 → 让组长点名；非组长发 `封盘` → 回「只有组长能封盘」；这些都**只在投票状态机内消费**，不进 7 条前缀
  - **超时且无人过半** → 报票数明细 + **冻结窗口**（`closed: true`）：数字静默不计，**只有组长**还能 `封盘` / `封盘 N` 在同一批候选上拍板；再发「方向」则开新窗口。**超时收口由任何到达的消息触发**（不只是数字），且收口时那条指令照常执行（外审必修 A）
  - **窗口块**：`state.vote = {chat_id, opened_at, opened_by, candidates[{id,title,note,rubric_refs}], votes{open_id: 编号}, closed}`（`closed: true` = 超时冻住）；**落定产物** `data/direction.json`（整份覆盖、裸 JSON，同 `proposals.json`）：`{decided_at, decided_by, winner, candidates, votes, tally, reason}`，`reason ∈ 过半落定 / 超时后组长指定 / 组长拍板`，`decided_by ∈ vote / leader`
  - **候选生成是 B2 的第 2 处 LLM 点**（`src/intelligence/direction.py`）：2–3 条、`id` 从 1 连续、`rubric_refs` 只能引用清单里真实存在的评分点；**没有评分点就不调 LLM**（D-48 口径）；私聊发「方向」只回「去群里发」、**不烧 token**；条数与长度口径见 D-62；`rubric_refs` 允许空数组、不算错；候选方向**不进覆盖率统计**（D-63）
  - **与 M4 / 登记互清**（§2.7 / P0-D 口径延伸）：开投票窗口时清 `register` / `preference` 残留；M4 开窗反向清 `vote`

**志愿窗口（M4）**

- **开启**：群里发 `你想做哪一块` → 任务卡清单发群 + 给花名册**每个人**私聊一份；私聊发同一句只回他自己那份清单，**不动窗口**（否则随便一个组员私聊一句就能把窗口关掉）
- **关闭（三条任一即结算，只结算一次）**：
  1. 花名册**全员都提交过** → 立刻封盘（**5 小时是上限，不是必须等满** —— 三人 5 分钟填完不能干等 5 小时）
  2. **超时 5 小时**（`PREFERENCE_TTL`）→ 结算，没交的人走兜底
  3. **组长确认封盘**：组长在群里再发一次 `你想做哪一块` —— **还没人交过志愿** → 只重发清单（幂等），**不封盘**；**已有人交过** → 回确认语「现在封盘会按已有 N 份志愿分配，还有 M 人没交。回复「封盘」确认，回复别的继续等。」，窗口保持不变；组长回 **`封盘`** 才结算（**重发本身不再是封盘按钮** —— P0-B / D-56）。演示 S8 走这条：自动结算要等 5 小时，现场等不起
- **窗口内的规矩**：**只认私聊** —— 群里打裸数字**一律不算**志愿，照走 7 条前缀（D-54）；一人一份、**后投覆盖先投**；数字 = 清单序号（= `task_id` 的序号），可一次发多个按优先级排（`2 1`）；越界 / 非数字 / 空 → 回提示、**不落盘**；非花名册成员发数字 → 回"花名册里没有你"，不给假确认
- **过期即失效**：读到 `opened_at` 距今 ≥ 5 小时的窗口就不再收志愿、`awaiting` 清掉，**并当场结算出总表** —— "失效"与"封盘"是同一个动作，不是静默丢弃（D-52）。没有这一条，昨天的窗口会吃掉今天的数字
- **结算后**：`awaiting` 清空 → 分配落 `data/assignments.json` → **分配总表发群**（按人分组 + 末行"第一志愿 x 人 / 第二志愿 y 人 / 兜底 z 人" + **未交志愿名单**，P1-F）；裸数字立刻不再被当志愿序号
- **前置**：`data/cards.json` 有任务卡、`data/members.json` 有花名册；缺哪个就回哪句提示。**M2 的投票窗口仍是 10 分钟，不要跟着改**
- **清单首行**点明这套卡来自哪份作业书（`当前任务卡来自《标题》（N 张）`，P1-E）—— 新群看到旧卡时至少知道来源；title 由 app 层从 `data/assignment.json` 读出来传参，`render_task_list()` 仍是纯函数
- **⚠️ 单群假设（D-57）**：`data/` 全局共享、**不按群隔离**（沿用 D-30「唯一落盘位置 = 仓库根 `data/`」）。演示 / 验收都是单群；**换群或重做前必须清掉 `data/state.json`、`data/preferences.json`、`data/assignments.json`、`data/seen.json`、`data/direction.json`、`data/proposals.json`**，否则上一轮的志愿 / 花名册 / 投票（含冻住的窗口）/ 去重记录会被当成这一轮的。另，`data/direction.json` 的 `votes` 落的是**真实 `open_id`**，只作留痕：**不许进 M7 报告、不许进任何群消息**（外审 🟡6）
- **事件去重（D-58）**：同一 `message_id` 只处理一次，落 `data/seen.json`（只留最近 200 条）—— 飞书重复投递 / 重连补投不再把一条指令跑两遍
- **单实例保护（D-59）**：机器上只允许跑一个网关（bind `127.0.0.1:47653`）；第二个实例启动即失败退出（`return 3`）。`data/app.lock` 只写来给人看，不参与判定
- **post 归一（D-60）**：富文本 / 转发消息拍平成 `text` 再进路由（P1-I），否则整条被静默丢弃
- **回复轨迹（P1-J）**：每条收/发都打一行带时间戳的日志（`[M0] <ts> recv …` / `[M0] <ts> -> id_type:chat_id ok|失败(…) | text[:40]`）

### 7.2 覆盖率的三个数字（**分母不同、用途不同，禁止混用** —— D-19）

| 数字 | 分母 | 要求 | 谁判 |
|---|---|---|---|
| 循环退出 | 可拆评分点（`status=normal`） | **100%** | 代码 |
| D5 门 | **人工标注**的可拆评分点 | **≥80%**（初始参数） | M8 独立脚本 |
| M8 报告值 | 同 D5 分母 | 实际值，**不设阈值** | 独立脚本 |

不把循环阈值降到 80% 的理由：生成器手上拿着完整清单，"每点至少一卡"是可满足指令，循环存在的意义就是第 2 轮补漏；而降阈值等于允许生成器偷懒。D5 的 80% 量的是"对人工基线的达成度"，两码事。

### 7.3 M3 自检循环（**代码判定，不是 LLM 判定** —— D-16 / D-19）

```python
def check(cards, rubric):
    if not cards:
        return False                       # 空集 → 直接判不达标（原待定义-31）
    eligible = {r.id for r in rubric.points if r.status == "normal"}
    covered  = {ref for c in cards for ref in c.rubric_refs}
    hi = max_hours(cards)
    lo = max(min_hours(cards), 0.5)        # 工时地板 0.5，避免除零
    return eligible <= covered and hi / lo <= 3
```
- **待定义-31 已关闭**（工程默认，本轮拍板生效，D-25）：`cards` 为空 → 短路返回不达标；`min_hours` 取下限 0.5
- 流程：生成 → 最多 2 轮 `check()` → 失败详情喂回重拆 → 输出附覆盖率
- 用**子集**判定（`<=`），卡片多引用模糊点不算错
- 模糊点仍按 T04 推给组长确认；确认后 `status` 改回 `normal`，重新进分母
- **病态边界**：全部评分点都是 ambiguous → **拒拆**，走"需人工"路径（与 T13 同类）
- 2 轮后仍不达标 → 输出当前最优 + 未达标项标注（如"R4 未覆盖"）发群，由人决定（D-18）

### 7.4 均衡度两套口径（**分两个函数，禁止互相复用** —— D-17）

| 用途 | 公式 |
|---|---|
| 循环内过/不过判据 | `max/min ≤ 3` |
| M8 评测报告展示值 | 归一化极差 `1 − (max−min)/Σ` |

### 7.5 作业书抽取（D-15 / D-21）

- M1 的 LLM **只吃 `{ text: string }`**
- PDF → PyMuPDF；Word → python-docx；粘贴纯文本 → 直通
- **表格用 `find_tables()` 单独抽**（需 PyMuPDF 1.23.0+），每行序列化为"列名：值｜列名：值"，与正文用 `=== 表格区域 ===` 分隔后拼进输入
  - **表格行分隔（待定义-33 已关闭，D-25）**：列内用 `｜`，**每行前加 `- ` 前缀、行间用换行**，避免跨行粘连
- **权重加总软校验**：weight 加总明显偏离 100（<90 或 >110）→ 附警告"评分标准解析疑似丢行，请对照原文核对"。**软警告不拒收**
  - **分支（待定义-32 已关闭，D-25；D-48 补"部分缺失"档）**：**全都有** `weight` 且能归一化到百分制 → 才判加总是否偏离 100；**全都没有** → **整体跳过，不误报**（§6.1 允许无分值的评分点）；**部分有、部分没有** → 软警告"n/N 条没有分值，疑似把正文要求当成了评分点"（真评分点通常整节都标分）
- **拒收两种**：扫描版 PDF（空文本层 → 回复"请发文字版"）、图片（OCR 已砍，P2）
- 双栏排版不过度投入，靠上面两道兜住主要风险
- **字符归一化（D-43，2026-09-12 实测定）**：PDF 字体缺 ToUnicode 映射时，PyMuPDF 会把常用汉字抽成**康熙部首**（如 一 → U+2F00）或**简体部首**（页 → U+2EDA、风 → U+2EDB、门 → U+2ED4、马 → U+2EE2、角 → U+2EC6、见 → U+2EC5）。实测一份真实课程任务书：**2360 字里 176 个（7.5%）是部首字符，正常「一」出现 0 次**。必须在抽取后归一化，否则 LLM 拿到的是"⻚面""作⻛"，而它写引用时很可能写成"页面""作风" —— **`quote` 原文子串硬校验会误判成幻觉**，且 M8 人工基线（人写正常汉字）无法与 agent 输出对齐。修法：逐字符对 U+2E80–U+2FDF / U+F900–U+FAFF 做 NFKC（修掉 165/176），再补一张 6 字小表兜住 NFKC 不覆盖的简体部首（剩 11/176）。实测 176 → 0，**总长不变**。小表兜不住的由 `check_radical_residue()` 点名 —— 软警告、不拒收。

### 7.6 「方向」共识（M2）—— 待定义-6b 已关闭

- **「方向」= 作业的选题大方向**。不是章节，不是任务卡，是"这份作业我们打算做什么题目"
- 流程（定稿 §3.3）：
  1. 组员先在群里**自由讨论**，**机器人不插话**
  2. 无人提案、或讨论不收敛时，**任何人**发「方向」触发
  3. 机器人**基于评分点清单**生成 2–3 个候选大方向发到群里，明确标注"仅供参考，由全组拍板"
  4. 组员**回复数字投票**，**过半即落定**
  5. **组长已有方向时直接拍板跳过**本环节
- 边界：候选方向是从作业书评分点长出来的"脚手架"，属于"读懂作业书"的智能延伸，**不是替人思考**；机器人平时不提供任何思路
- 投票需要在多条消息之间保持状态（候选列表 + 各人票数）→ 落在 `data/state.json`，见 `docs/ARCHITECTURE.md` §4

### 7.7 「登记」建花名册（M0，两步确认 —— D-34）

群聊里，**组长** @机器人 发「登记」→ 机器人回一张**空白表单** → 组长照着填、一条消息发回来 → 机器人回显**填好的表**请他确认 → 组长回「同意」→ 落盘 `data/members.json`（§6.6）。

1. **触发**：群里发 `登记`（@不@机器人都行，见 §7.1 的剥 @段规则）
2. **机器人回空白表单**（模板，让发起人照着填）：
   ```
   请照这个样子填，@到每个人：
   登记
   组长：@某人
   组员：@某人 @某人 @某人
   ```
3. **发起人填写并发出** —— **必须用 @ 人，不要打名字**：@ 结构里自带 `open_id`，机器人因此**根本不需要"读取群成员名单"那项权限**（这正是这条设计的价值）
4. **机器人回显规范化后的表**：谁是谁、共几人（含不含组长），并附一句"回复「同意」保存，回复别的就作废"
5. **只有发起人的回复才算数**（旁人发言静默忽略：不推进、不作废、也不回话）：发起人回「同意」 → 写入 `data/members.json`；回别的、或超时（默认 5 分钟）没回 → 作废，**不动原有名单**。逃生词 `取消登记` / `取消` 随时可退出；collect 阶段只有「**带 @ 且含独占一行的 `组长：` / `组员：`**」的消息才归状态机管（且只有发起人能推进），其余消息——**包括带 @ 的**——一律照走 7 条前缀。口径见 D-45。「旁人提交表单」刻意静默不回（与图片的短拒收是有意不一致：图片是“格式错、必须纠正”，旁人填表是“轮不到你、不必打扰”）。
6. **重发「登记」即整体覆盖**旧名单（不做历史版本）

- 硬规则：`组长` 有且只有一个；`组员` 至少 2 人
- 组长身份在表单里**明写**（一般就是发起登记的这个人自己 @ 自己），**不再靠猜**——定稿里"组长拍板跳过方向 / 组长可查询 / 组长改派"都要用到它

---

## 8. 未定义清单 —— **遇到就停下问人，禁止臆想**

> 本清单已于 2026-09-11 清算（依据 = `docs/定稿文档-v2.md`）。**"禁止臆想"的规矩不变**，但作用范围收窄为：§8.1 那张"仍开放"表（**已无挡 P0 编码的条目**）、§8.3 里标"仍开放"的三项。

### 8.1 会直接影响编码的

**已关闭（结论已写入正文，出处写明）**

| 编号 | 原问题 | 结论 | 出处 |
|---|---|---|---|
| 待定义-6b | 「方向」指什么 | = 作业**选题大方向**；完整流程见 §7.6 | 定稿 §3.3 |
| 待定义-8 | M5 完整链路 / 内部记录存哪、谁能查 | 存 `data/proposals.json`、**留痕真实身份**、群内匿名发布；组长可查询**为 P1**（查询口令名待定，见下方开放项） | 定稿 §3.6 |
| 待定义-9 | M6 编号出现两次 | **笔误**：P1 那条"M6 全量评测（15 份）"实为 **M8** 的扩量评测。M6 只有"催办"一个含义 | 定稿 §3.9 编号 |
| 待定义-11 | M8 用途：比赛评分项 or 内部自测 | **两者都是**——既是全项目可信度来源（内部调优依据），也是"可验收成果"评分项的硬通货，对比表进演示材料 | 定稿 §3.9 |
| 待定义-12 | T01–T13 形态 | **人工验收清单**，不是自动化脚本。每条跑一次记一次结果，附执行记录表 | 定稿 §8 |
| 待定义-31 | `check()` 空集 / 除零 | 空集 → 短路判不达标；`min_hours` 地板 0.5 | §7.3 / D-25 |
| 待定义-32 | 权重软校验前置条件 | 仅当 weight 全部存在且可归一化到百分制才执行，否则整体跳过 | §7.5 / D-25 |
| 待定义-33 | 表格行分隔符 | 每行前加 `- ` 前缀、行间换行 | §7.5 / D-25 |
| 待定义-34 | 无截止时间字段、无"标记完成"指令 | 截止时间 = M1 抽出的**作业级** `deadline`（§6.0），任务卡不设 `due_date`；"完成" = 私聊「**完成 T3**」（§7.1 第 6 条） | 定稿 §3.2 / §3.7 |
| 待定义-35 | 两档临期阈值 | **48 小时 / 24 小时** | 定稿 §3.7 |
| 待定义-36 | 组长查询的口令名（只知道是 P1） | **`报告`**（D-64）：组长在群里发「报告」，机器人把执行报告（总表 + 核对清单 + 甘特图）发群。**这就是 M7 的触发点**；"查询匿名提交者"仍是 P1、不实现 | §7.1 / D-64 |
| 待定义-38 | 数字回复怎么进来 | **状态优先**：先查 `state.json` 的 `awaiting`，再走前缀匹配；投票窗口 = 过半 / 超时 10 分钟 / 组长拍板关闭，一人一票、后投覆盖。详见 §7.1 | §7.1 / D-33 |
| 待定义-39 | 组员名单 / `open_id` 从哪来 | **群内两步确认的「登记」表单**（§7.7）：组长 @人报名单，id 从 @ 结构里直接拿，**不需要"读群成员名单"权限** | §7.7 / D-34 |
| 待定义-40 | 机器人怎么知道谁是组长 | 登记表单里**明写** `组长：@某人`，落在 `data/members.json` 的 `leader` | §7.7 / D-34 |
| 待定义-41 | 群投票"过半"的分母 | **已投票者过半**（谁投了算谁的分母）+ 参与人数门槛，详见 §7.1 | §7.1 / D-35 / D-36 |
| 待定义-42 | 飞书把「文字 + 附件」发成**一条**还是**两条**？ | **必然两条**（客户端不允许同条）；且两条机器人都收得到、文件可下载。对 M0 的后果：需**缓存最近一条文件消息**并与随后的 `作业书` 配对 | §7.1 / D-42 |

**仍开放（定稿文档里也没有，编码前需拍板）**

| 编号 | 待定义内容 | 阻塞 | 说明 |
|---|---|---|---|
| 待定义-37 | T01 原文"指令列表（5 条）"与路由表 **7 条**的**口径**：改述 T01，还是把后来加的两条并进 M0 的 5 条说法里 | 验收口径（T01 / M0） | 建议采"T01 指 M0 那 5 条"的读法（D-22），但需拍板。**不挡编码** |

**待定义-38 的备选方案（未采用，留档 —— 2026-09-11）**

- **群里一律 @机器人 才算指令**：能彻底消灭"闲聊里的数字被计票"。未采用的理由是要改 T05 原文"回复数字投票"，而 T05 属截图层真源。**若实测误判严重，随时启用**（这正是 §7.1 残留风险的备案）。
- **升级成交互卡片点选**：零误判，还能知道"这票是谁投的"。定稿已把它列为 **P1**，等 D5 门过了再说。
- **把投票搬到私聊**：会削弱"群里现场投票收敛"的演示效果（演示剧本第 3 步就是这个），不采用。

> **M4 的私聊志愿序号风险很低**：私聊里只有机器人和你，机器人上一句刚问过"你想做哪一块"，数字没有歧义。真正要动脑筋的只有 M2 的群内投票。

### 8.2 排期类

| 编号 | 原问题 | 结论 |
|---|---|---|
| 待定义-25 | **M4 / M5 / M6 没有 owner** | **已关闭**。定稿 §6 分工表补齐：**机器人 owner** = M0 + M1 + M4 / M5 指令路由 + **M6 定时任务**；**拆解 owner** = M2 + M3 + 评测脚本；**报告与评测 owner** = M7 + M8 + 演示材料。协作层与流程层归机器人 owner（D-28） |

> 分工是"今晚认领、可换"，但**必须有人认领**——9/16–9/17 是最挤的两天，不能空着。

### 8.3 背景类（不影响开发）

| 编号 | 问题 | 状态 |
|---|---|---|
| 待定义-20 | 「不整合」——被列为对话拍板决定之一，未解释含义 | **仍开放**：定稿文档全文亦无此词，出处只在那次对话里 |
| 待定义-21 | 「智能押拆解」——疑为笔误 | **已关闭**：是刻意措辞。定稿开篇"智能只押在拆解（M3）"，"押"= 把智能全押在一处 |
| 待定义-22 | 「研采」——队友代号还是方法论来源？ | **已关闭**：三个方案之一的**方案代号**。它贡献验收方法论——本文的 T01–T13 用例表与 D5 发布门即来自它 |
| 待定义-23 | 「赛道图」——用户最初提供的比赛要求 | **已关闭**（2026-09-12）：**不存在赛道图**。比赛交付物 = **运行视频 + 附件**；「附件」= 软件运行所需的一切，见 D-37 |
| 待定义-24 | 《方案一.docx》（意图层原件） | **仍开放**：位于 `C:\Users\617\Desktop\方案一.docx`，未纳入本仓库 |
| 待定义-26 | 「智汇简报」的 Dify 骨架 | **已关闭**：**工程兜底**。队友已有**可运行 Dify 原型**；主线彻底跑不通时用它保底交差（定稿 §7 备用路径 + 文末三方案定位） |

---

## 9. 决策台账

| 编号 | 决策 |
|---|---|
| D-01 | 需求真源 = 用户提供的**截图**；冲突时以此为准 |
| D-02 | 指令集见 §7.1（M0 原有 5 条，加上「完成 T3」与「登记」共 **7 条**）；**前缀精确匹配 + 状态优先，不做自然语言理解** |
| D-03 | 评分点结构见 §6.1；任务卡溯源字段 `rubric_refs` 见 §6.2。**定稿 §3.4 有明确出处**，不再是"后加设计"（原判断基于《方案一.docx》无数据模型，仍成立但已不是唯一依据）。落盘只存 `id` 数组、展示时 join 原文引用 |
| D-04 | 覆盖率分母用**人工标注**（防自出题自改卷）；80% 为初始参数 |
| D-05 | **全做 P0**。不可砍底线 = M0 / M1 / M3 / M7简版 / M8前5份；牺牲顺序 = M5 代言 → M2 方向候选 → M4 只保先到先得；工作量 8–10 人天 < 24 人天（三人并行） |
| D-06 | 机制继承自《方案一.docx》：志愿优先 / 不填随机 / 人人有活 → M4；学习通式临期提醒 → M6；智能分析报告 → M7；人为权重大于机器权重 → 组长指定改派（**降为 P1**） |
| D-07 | 飞书机器人即唯一 UI；WebSocket 长连接 |
| D-08 | **1 个 Agent + M0–M8 九个模块**，不是三 Agent。见 B1/B2/B3 |
| D-09 | 五层结构 + M8 旁挂，见 §2 |
| D-10 | **D5 发布门 = 9/15（周二）晚**。三条：① M0 测试群收发跑通；② 主链路命令行版跑通（M1+M3+M7简版）；③ 前 5 份覆盖率达标。**全过 → 开始 P1；任一不过 → 冻结全部 P1** |
| D-11 | **时间线**（9/11 周五 → 9/19 周六）：9/11 今晚定稿+认领分工 → 9/12–13 M0 网关骨架+M1 原型 → 9/14 M3 迭代+人工基线 → 9/15 **D5** → 9/16 指令层（M4+M5+M2）→ 9/17 交付层+全量评测 → 9/18 彩排 → 9/19 提交缓冲。**若官方截止非 9/19，整线以真实截止日为锚平移，D5 固定"截止前 4 天"** |
| D-12 | 数据存 JSON，不用数据库。**落盘位置统一为仓库根目录 `data/`**（`.gitignore`），`src/` 下不建存储目录（D-30） |
| D-13 | 存在命令行主链路，与飞书入口并行；M7 简版即命令行交付 |
| D-14 | 术语三分：评分点（M1）/ 任务卡（M3）/ 「模块」= `task_id` 的口语。**志愿对象 = `task_id`**；一张卡可横跨多章节，一个评分点可被多卡共覆盖 |
| D-15 | 作业书统一归一化为纯文本；拒收扫描版 PDF 与图片。详见 §7.5 |
| D-16 | M3 自检循环由代码判定；三层达标判定归属见 §7.2 / §7.3 |
| D-17 | 均衡度两套口径，禁止复用。详见 §7.4 |
| D-18 | 2 轮重拆仍不达标 → 输出最优 + 标注，由人拍板 |
| D-19 | **覆盖分母改为"可拆评分点"**（规格修正），详见 §7.2 / §7.3 |
| D-20 | `assignment.source` 扩为四值，详见 §6.4 |
| D-21 | 表格抽取策略 + 权重软校验，详见 §7.5 |
| D-22 | **指令集补第 6 条**：私聊「`完成 T3`」标记完成（定稿 §3.7）。T11 依赖它才可测。T01 截图"5 条"的口径见待定义-37 |
| D-23 | **截止时间是作业级、不是任务级**：M1 抽出 `deadline` 存 `data/assignment.json`（§6.0）；任务卡不设 `due_date`；M6 催办与 M7 甘特图共用这一根截止线 |
| D-24 | **任务卡 7 字段**（§6.2），含 `effort_hours` / `deliverable` / `acceptance`；`rubric_refs` 落盘存 id、展示拼原文 |
| D-25 | 待定义-31 / 32 / 33 / 35 采纳既定值（§7.3 / §7.5），编码不再阻塞 |
| D-26 | T01–T13 = **人工验收清单 + 执行记录表**（非自动化脚本）；方法论来自"研采" |
| D-27 | **M6 编号笔误勘误**：P1 的"M6 全量评测（15 份）"= **M8** 扩量。M8 用途 = 内部可信度来源 **兼** 比赛"可验收成果"硬通货 |
| D-28 | **模块 owner**：机器人 owner = M0 + M1 + M4/M5 指令路由 + M6；拆解 owner = M2 + M3 + 评测脚本；报告与评测 owner = M7 + M8 + 演示材料（关闭待定义-25） |
| D-29 | **方案层定稿文档归档**为 `docs/定稿文档-v2.md`（逐字转录），作为 §10 第 3 层依据 |
| D-30 | 落盘目录二义性（`src/data/` vs 根 `data/`）裁定：**唯一落盘位置 = 仓库根目录 `data/`**，见 `docs/ARCHITECTURE.md` §4 |
| D-31 | 「`完成 T3`」的落地字段 = `assignment.completed_at`（时间戳，`null` 表示未完成）；**MVP 不做提交物上传** |
| D-32 | **冲突裁决（用户 2026-09-11 拍板）**：截图 > 定稿文档 > 《方案一.docx》/ 对话拍板。第 2/3/4 层互撞时**一律听定稿**，不必再问人。见 §10 |
| D-33 | **路由优先级 = 先看状态、再看前缀**（补 B10）：`state.json` 的 `awaiting` 决定裸数字的解释（投票票号 / 志愿序号）；M2 投票窗口 = 过半 / 超时 10 分钟 / 组长拍板三者任一即关，一人一票、后投覆盖（用户 2026-09-11 拍板，采用候选 1「状态优先」）。详见 §7.1 |
| D-34 | **第 7 条指令「登记」+ 两步确认建花名册**（用户 2026-09-11 拍板）：组长在群里发「登记」→ 机器人回空白表单 → 组长 @人填回 → 机器人回显 → 组长回「同意」落盘 `data/members.json`。`open_id` 从 @ 结构里取，**免除"读群成员名单"权限**。详见 §6.6 / §7.7。**重登记限组长**（F1，2026-09-14）：**已有花名册时只有现任组长能重开登记**（非组长 → 回一句拒绝、**不改状态也不动名单**）；花名册为空（还没登记过）时任何人可登记 |
| D-35 | **投票分母 = 已投票的人**（用户 2026-09-11 拍板）：某方向票数 > 已投票数 ÷ 2 即落定；配 D-33 的窗口。详见 §7.1 |
| D-36 | **工程默认（可推翻）**：投票加一道门槛——参与人数达**花名册人数的过半（向上取整）**才允许落定；超时且无人过半 → **交组长拍板**。防"第 1 票就宣布过半"（D-35 的退化情形） |

| D-37 | **比赛交付物 = 运行视频 + 附件；不存在赛道图**（用户 2026-09-12 拍板）。「附件」= 软件运行所需要的一切（源码、依赖、配置与运行说明）。三点含义：① 不存在未归档的评分表，"猜评分标准"的风险消除；② **运行视频是硬交付物** ⇒ 演示必须真跑、可录，9/18 彩排与演示路径升为关键路径；③ 真实作业书进群所需的**群文件下载权限是必需项**，不是可选项 |

| D-38 | **`check()` 返回失败原因列表，不是布尔**（工程实现口径，2026-09-12）：§7.3 的伪代码写 `return False` / `return eligible <= covered and ...`，但同节的流程要求"失败详情喂回重拆"，布尔带不了详情。落实现为 `list[str]`：空列表 = 通过，非空 = 喂回 LLM 的失败原因；`if not check(...)` 的用法与伪代码一致。空集短路与 0.5 地板照旧（待定义-31 不变）。见 `src/intelligence/decompose.py` |

| D-39 | **权重软校验"可归一化到百分制"的落地口径**（工程默认，可推翻，2026-09-12）：§7.5 只说"全部有 weight 且能归一化到百分制才执行"，但"加总 85（丢行）"与"20 分制"在数值上无法仅凭加总区分。实现取门限 `PERCENT_SCALE_MIN = 50`：加总 < 50 一律视为另一种量纲 → 整体跳过、不误报；≥ 50 才按 [90,110] 判是否疑似丢行。门限待真实作业书校准。见 `src/intelligence/extract.py` |

| D-40 | **模糊点建卡口径**（用户 2026-09-12 拍板）：`status="ambiguous"` 的评分点**允许建卡**（它对应的交付物可能真实存在），但卡的 `acceptance` 必须显式标注"该评分点为模糊要求，需组长/教师确认后方可核对"，**不得声称已满足**。模糊点**不进覆盖率的分子分母**（`coverage_loop()` 只收 `normal`），但它的卡**照常计入均衡计算**（`balance_loop()` 不筛 `rubric_refs`）。见 `src/intelligence/decompose.py` 的 `M3_SYSTEM` |

| D-41 | **真实作业书不入库**（2026-09-12）：定稿 §3.9 要收 15 份真实作业书构成评测集，但那是**别人课程的作业材料**（含版权与他人信息），放进交付物仓库不合适；且 M8 的可复现性靠的是**人工标注 + 独立脚本**，不是原文分发。因此：真实作业书只存本地、不 commit；入库的只有合成样本 `eval/samples/` 与标注 / 复算结果 |
| D-42 | **作业书文件怎么进机器人**（2026-09-12，已实测）：① 飞书客户端**不允许**一条消息里同时有文字和附件 ⇒ 必然两条；② 实测 6/6 事件全收到、文件全部下载成功 ⇒ **主链路第一环不会断**，私聊与群两条路都可用；③ 对 M0 的硬要求：**缓存"最近一条收到的文件消息"**，与随后的 `作业书` 文字指令配对；④ 演示与验收用**私聊投递**（干扰最少），群投递同样可用。**已查明**：后台实际开通 `im:message.group_msg.include_bot:read` + `im:message:readonly`，故群里不带 @ 也收得到，详见 §7.1。出处：§7.1 + `tools/probe_feishu.py` |

| D-43 | **抽取后必须做 CJK 字符归一化**（2026-09-12 实测，见 §7.5）：真实课程任务书 PDF 抽出的 2360 字里混进 176 个部首字符（U+2E80–U+2FDF），正常「一」出现 0 次。危害：M1 的 `quote` 原文子串硬校验会因"部首 vs 汉字"不一致而误判幻觉，且 M8 人工基线（人写正常汉字）无法与 agent 输出对齐。修法：逐字符 NFKC 覆盖 U+2E80–U+2FDF 与 U+F900–U+FAFF，再补显式小表（角/门/页/风/马/见），实测 176→0 且总长不变。**已实现**（2026-09-12）：`src/intelligence/extract.py` 的 `normalize_cjk()`，在 `extract_text()` 唯一出口统一调用；实测 176→0、总长不变。**同日补齐兜底**：扫过 7 份真实作业书（5 PDF + 2 DOCX，2026-09-12），小表漏掉 ⻅（U+2EC5，BIM 任务书命中 2 次）已补入；另加 `check_radical_residue()` —— 归一化后仍有残部即点名报数（与 `check_weight_sum()` 同款软警告、不拒收），M0 核对清单里以 `[软警告]` 冒出来，新部首不再静默污染 `quote` 校验。7 份实测残留 0 |
| D-44 | **飞书权限集冻结在已实测的 9 条，不再裁剪**（用户 2026-09-12 拍板）：后台实际开通 `im:message:readonly`、`im:message.group_msg.include_bot:read`、`im:message.p2p_msg:readonly`、`im:message:send_as_bot`、`im:message:update`、`im:resource`（应用身份）、`im:message:send_multi_depts`、`im:message:send_multi_users`、`im:resource`（用户身份）。其中 `im:message:update`、两个 `send_multi_*` 与 `im:resource`（用户身份）本项目均未使用，但**决定保留不撤**：实测跑通的配置就是这一套，裁剪需重走发布审批并重测，收益不划算；材料中也不写"遵循最小权限原则"，避免与实际权限自相矛盾。见 `docs/ARCHITECTURE.md` §12.1 |
| D-45 | **M0 落盘口径与两个新增字段**（2026-09-12，外审必修 1–5 定）：① **`state.pending_file` 只缓存 `file`**，图片**不入缓存**，但回一句短拒收（`IMAGE_REJECTED`；2026-09-12 用户拍板，不再静默）—— 实测一张图会把刚发来的作业书 PDF 挤掉，随后「作业书」下到的是图、报 `.bin` 不认识，真实原因完全看不出；字段集固定为 `file_key` / `file_name` / `resource_type` / `chat_id` / `message_id` / `received_at`（`resource_type` 目前恒为 `"file"`，保留是为了下载时选对 `type` 参数、并让 `.bin` 回退名可诊断）。② **登记窗口（`state.register`）字段集**：`stage` / `initiator_open_id` / `leader` / `members` / `expires_at`；窗口规则：**只有发起人能推进**（旁人发言静默忽略：不推进、不作废、不回话），collect 阶段只有「**带 @ 且含独占一行的 `组长：` / `组员：`**」的消息才归状态机管（**带 @ ≠ 表单消息**：飞书用户习惯发指令前先 @ 机器人，只按「带 @」接管会把指令吃掉，真机已复现，必修 6），其余照走 7 条前缀（§7.7），collect 与 confirm 都受 5 分钟 TTL 约束，逃生词 `取消登记` / `取消` 随时退出 —— 没有这几条，一次误触「登记」不填表就把整个群的指令吃掉，且永不超时。③ **`Outcome` 在方案 §3 的三字段之外新增两个**：`save_roster`（登记确认后交 app 层落 `members.json`，避免把判定复制到 app 层）与 `pipeline`（`"assignment"` / `"decompose"`，由 `route()` 一次算出、app 层只读）；原 `pipeline_kind()` 已删 —— 否则「回什么话」与「起不起重活」会各判一遍并互相矛盾（回了「表单没看懂」却照样烧一次 LLM）。④ 清缓存只按 `message_id` 匹配地清：跑 M1 的十几秒里新到的文件不能被上一轮收尾顺手删掉。⑤ 均属 §7.1 / §7.7 的落地细节，与需求无冲突；升级期订正：见 `requirements-upgrade.md` §6 L12（PM 2026-09-15 裁） |
| D-46 | **`state.pending_file` 的有效期 = 30 分钟**（工程默认，可推翻；2026-09-12 定）：`received_at` 现在真的被读了 —— `_pending_file()` 是“有没有可用缓存”的唯一入口，超过 TTL 就当没有，于是「作业书」自然回既有的 `FILE_MISSING`、`pipeline` 也不会起。理由：`pending_file` 是**落盘**的（关掉重启仍在），而唯一的清除时机是「作业书」跑完 —— “发过文件、没接着说「作业书」”的残留会一直留着，第二天录制时会静默复用一份旧作业书。30 分钟对一场演示足够，只拦跨场次串味。**缺 `received_at` 或时间戳脏 → 当没有 TTL**（不因脏数据丢文件） |
| D-47 | **`state.pending_file` 做会话隔离**（2026-09-12，外审 2.5 定）：`_pending_file()` 是“有没有可用缓存”的唯一入口，除 D-46 的时间轴外再判空间轴 —— 只有 `chat_id` 与当前 `inbound.chat_id` 一致的文件才算数。现状是全局单份，而 D-42 ④ 定的演示方式（三人各一个私聊投递）必现互相顶掉：组长发 PDF-A、队友发 PDF-B 后，组长发「作业书」会配到 B、解析错人的作业书，并顺手覆盖 `data/rubric.json` / `cards.json` / `assignment.json`。`chat_id` 缺省则放行（兼容旧 state，与 D-46 同款防御）。与 D-46 配成一对：D-46 管时间轴、D-47 管空间轴。 |
| D-48 | **评分点来源定义 + 空 rubric 处置**（用户 2026-09-12 拍板）：① 评分点 = 作业书里「评分标准 / 成绩评定 / 考核方式 / 评分细则 / 评分表 / 评分依据」这类小节中的评分项，或全文中其他**带明确分值 / 百分比 / 权重**的评分项；**不是评分点**（即使原文写"必须 / 应 / 需"）：交付与提交要求、格式要求、字数页数、页眉页脚、装订方式、学术诚信与抄袭条款、参考文献规范、技术选型建议、参考资料链接、课程目的 / 目标、分组要求、选题建议。**一条评分点 = 一个评分项**；一句话里并列的多个分项各自出一条，同一项不许重复、不许把一句话切成多条。② 全文确实没有评分标准 → `rubric` 返回**空数组**（`_validate_points()` 放行空数组、只有非数组才报错），M3 的 `_refusal()` 与 `decompose()` **不花 token 直接拒拆**，gateway `_run_assignment()` 回 `NO_RUBRIC_FOUND`（**不跑 M3、不拿正文要求凑数**）。③ `check_weight_sum()` 补"**部分带分值**"档：部分缺失 → 软警告"n/N 条没有分值，疑似把正文要求当成了评分点"；**全部没有**仍整体跳过（§6.1 允许无分值）。④ **M8 人工基线口径**：人也是从「评分标准小节」标（不是从正文要求），否则 D5 门③ 覆盖率两个分母不可比。根因：§6.1 与提示词都没定义"评分点是什么"，实测 BIM 任务书真标准 3 条（30/10/60）、M1 却抽出 25 条（22 条无分值）。见 `src/intelligence/parse.py` 的 `M1_SYSTEM`；§6.1 / §7.5 |
| D-49 | **`deadline` 可为空 + 占位值软校验 + 拒拆不落盘**（用户 2026-09-12 拍板，外审 round2 定）：① `AssignmentMeta.validate()` 不再要求 `deadline` 非空（`course` / `title` / `submission` / `source_file` 仍必填）—— 否则提示词"绝不编造"与"没有就留空"两条硬规则互相打死，模型为跳出重试会死填占位值：实测 5 份作业书里 3 份被编出 `1970-01-01T00:00` / `2024-06-01T00:00` / `2025-01-01T00:00`，1 份把封面日期当截止。**"prompt 不许留空"被明确否决** —— 那等于官方授权编日期，比现在更糟。② 新增 `check_deadline()` 软校验：空 → 警告"没读到明确截止时间"；年份 < 2000 → 判占位值（门限用 2000 而非"早于今天"，避免把真实但已过期的截止时间误报）。③ `render_checklist()` 空 `deadline` 显示「未标注」。④ `_run_assignment()` 改为**先判空再落盘**：拒拆（空 rubric）时一个字都不写，避免演示"这份没有评分标准"时把上一份好产物清空。⑤ M1 提示词补两句保命条款：带明确分值就算数（不要求表格 / 编号 / 小节标题）；评分小节里没标分值的条目仍照收、`weight` 填 null、不拒拆。见 `src/models.py` / `src/intelligence/extract.py` / `src/gateway/app.py` / `src/report/checklist.py`；§6.0 / §7.5 |

| D-50 | **`status` 判据从"能不能客观核对"改为"有没有具体交付物"**（用户 2026-09-13 拍板）：① 指向具体交付物、或已列举内容/子项（A/B/C/D、里程碑、清单）→ `normal`，**即使标题读起来很虚**（"XX情况""XX质量"）；② 没有任何交付物、只能主观印象打分（"学习态度、遵守纪律""内容充实""团队协作精神"）→ `ambiguous`；③ 拿不准 → `normal`（默认拆，确认权留给群里看到原文的组长）。动机：旧判据只有"无法客观核对"一句、不可执行，实测 5 份真实作业书中营销 4 条里 3 条被判模糊（**95% 的分数**）、BIM 被吃掉的正是 **60 分**那条，导致清单写着"覆盖率 1/1 = 100%"而实际几乎没拆 —— 与 T04「模糊不硬拆」的原意相反（T04 是"别硬拆虚的"，不是"把能拆的也推给人"）。**不改变** D-19/D-40 的分母规则：模糊点仍不进覆盖率的分子分母、仍推给组长确认。见 `src/intelligence/parse.py` 的 `M1_SYSTEM`；§6.1。**实测验收（2026-09-13 真机复跑，用户同日确认）**：营销 4/4、BIM 3/3、数据库 2/3、软件系统 4/5、河流 7/7 —— **五个都是预期**；设计稿 §1 验收表里"DB 3/3、SW 5/5、河流 5/7"三个数字**作废**（与判据②自相矛盾：数据库剩的"平时考核"、软件系统剩的"学习态度、遵守纪律"本就该 `ambiguous`），河流 R6/R7 变 `normal` 是"指向具体交付物"的正常结果。**明确不加**"『是否…』式条目 → `ambiguous`"这类规则 —— 河流 7 条里 6 条是这种句式，加了会把分母打回 1–2、等于把这次治好的病治回去 |
| D-51 | **M8 评测口径**（2026-09-13 定，见 `eval/` 与 M8 设计稿）：① 覆盖率**分母 = 人工基线集合**（`baseline.decomposable=true` 的点），**不采纳 agent 的 `status`** —— 实测软件系统设计那份 agent 把 5 条里 3 条判 ambiguous、循环分母塌成 2，若 M8 沿用就成 `2/5 = 40%` 直接不过门，可它给 5 条全建了卡；分子 = 被任一卡片 `rubric_refs` 引用、且在人工分母里的点。② 均衡度用**归一化极差** `1-(max-min)/Σ`（**不筛** `rubric_refs`，模糊点建的卡照常计入，D-40），`Σ=0` → `N/A`；**不复用** `balance_loop()`（那是循环判据 max/min，不是展示值）。③ 对齐 = 人工 `order=n` ↔ agent `R{n}`，另做**条数 / 分值**两项校验，不一致即 ⚠️ 且该份不算通过。④ 门③ 判据 = **逐份 ≥80% 且无 ⚠️**（判定用逐份，不用平均）。⑤ 复算口径在 `eval/report.py` **独立实现**，**禁止 import `src/intelligence/coverage.py`**（D-19，有单测守着）；`eval/runs/` 含作业书原文，已加 `.gitignore`（D-41）。见 `eval/evaluate.py` / `eval/report.py`。⑥ **基线状态（2026-09-13）**：`eval/baseline/` 原为 AI 预标（`annotator` 为占位值、`decomposable` 与 agent 的 `status` 高度一致），**已于当日由左独立重标收口 —— 见本行末 "2026-09-13 收口"段**。**2026-09-13 收口**：左独立重标 5 份 baseline 完成（`annotator=左`），复算 5/5 通过、门③ 生效。唯一口径差异：数据库 R1「平时考核」人工判可拆、agent 判 ambiguous，M8 按人工分母算（`report.py` 已留痕）。注意：本次 5 份覆盖率均为 100%，区分度偏低 —— 人与 agent 共用"拿不准就拆"的判据，两边天然趋同；答辩时用「数据库 R1 那处口径差异」说明对比确实发生过。 |
| D-52 | **M4 志愿窗口 = 5 小时（上限）**（用户 2026-09-13 拍板；实现见 `src/gateway/preference.py` 的 `PREFERENCE_TTL`）：**结算三条，任一即结算、只结算一次** —— ① 花名册全员都提交过 → 立刻封盘；② 超时 5 小时 → 结算，没交的人走兜底；③ **组长在群里再发一次**「你想做哪一块」→ 立即结算。**5 小时是上限不是必须等满**（三人 5 分钟填完不能干等 5 小时）；**过期即失效**：读到 `opened_at` 距今 **≥ 5 小时**（正好 5 小时即到期）的窗口就不再收志愿、`awaiting` 清掉，**并当场结算出总表** —— "失效"与"封盘"是同一个动作，不是静默丢弃（否则"昨天的窗口会吃掉今天的数字"）；结算后裸数字立刻不再被当志愿序号，`data/assignments.json` 一次性覆盖写。选"组长重发"而不是新增第 8 条口令，是为了**不破坏 T01 的 7 条指令口径**（待定义-37 还没拍板）。**M2 的投票窗口仍是 10 分钟，不动**（用户只让动 M4 这一处）。见 §7.1 的"志愿窗口（M4）" |
| D-53 | **M4 分配算法**（用户 2026-09-13 认可；实现见 `src/gateway/allocation.py`）：① **先到先得** —— 按 `submitted_at` 升序逐个人处理，取他志愿里第一个还没被占的卡（命中第 1 个记 `volunteer_1`、第 2 个记 `volunteer_2`；`source` 枚举里没有 volunteer_3，**第 2 个及以后一律记 `volunteer_2`**；`auto` **严格只留给未填表的人**（D-53 修订）—— 这样总表末行"几人被兜底"才等于"几人没填表"）；② **兜底** —— 剩下的卡按 `effort_hours` 升序（并列按 `task_id`）分给"当前手上卡数最少的人"（并列按花名册顺序），没填志愿的人因此拿到 `auto`；③ 一张卡只能有一个 `assignee`，`depends_on` **不参与分配**；④ **卡数 < 人数**时做不到"人人有份"，分配总表如实标「无任务」，不硬塞。**花名册外的人不进分配**（旧名单残留的志愿、陌生人私聊一律忽略）；陌生人私聊发数字**不给假确认**，直接回 `PREFERENCE_NOT_MEMBER`（工程默认，可推翻）|
| D-54 | **M4/M5 的会话边界与"群是哪个"**（工程默认，可推翻；2026-09-13 定）：① **志愿窗口只认私聊** —— `awaiting = preference` 时只有 `chat_type == "p2p"` 的裸数字算志愿，群里一切消息照走 7 条前缀。窗口长达 5 小时而 `awaiting` 是全局的，不限定会话的话群里谁打一个数字都会被记成志愿序号（与 §7.1 记的 M2 那条残留风险同类，M4 这边从一开始就堵住）；② **机器人怎么知道"群"是哪个**：任何群消息都把 `chat_id` 记进 `state.group_chat_id`（无需新权限、无需改数据模型），M4 的清单 / 总表、M5 的匿名转达都发这个 `chat_id`；③ 取不到（从没收到过群消息）→ 回 `NEED_GROUP`「先在群里发一次指令，我先认一下群」，并**跳过发群动作**：M4 结算结不了就保留窗口、只清 `awaiting`；M5 不转达、**也不落盘**（不假装发过）。④ **志愿窗口块自记 `chat_id`**（P1-H）：开窗时把群写进 `state.preference.chat_id`，结算（`settle()`）优先按它发总表 —— `state.group_chat_id` 是"最后见到的那条群消息"刷新的，窗口长达 5 小时，别的群来一条消息就会把总表发错群。取不到才退回 `group_chat_id`。注：`state.group_chat_id` 由 **app 层**（`Gateway._remember_group`）落盘，router 仍是纯函数 |
| D-55 | **M5 匿名代言的落地口径**（§6.5；工程默认，可推翻）：① 正文 = 冒号后面的原文，**只 strip 两端**，不修改、不总结、不加工；正文里原带的 @ 段会被一起剥掉（与其它指令一致，也免得把 `@_user_N` 占位符泄露到群里）；② 群里的文案是「有组员提议：…」——**绝不能出现发送者姓名 / open_id**（有单测断言）；③ `data/proposals.json` 里**记真实 `user_id`**（防滥用留痕；组长查询为 P1，口令名待定 —— 待定义-36）；④ **内容为空**（只发了「我想提议：」）→ 回提示，**不转达、不落盘**；⑤ **不做限流**（规格没写；一个人连发多条就转达多条，P1 再说）；⑥ **非花名册成员**（F5，2026-09-14）：**不转发、不落盘、回一句提示**（`PROPOSAL_NOT_MEMBER`）—— 与 D-61 ② 对非成员的**静默有意不同**：M2 静默的场景是“陌生人在群里打个裸数字”（无意义输入，静默合理），而代言是“一个真人认真写了一句提议”，静默会让他以为机器人坏了；`roster` 为空（还没登记）时谁都算数 |
| D-56 | **M4 封盘要二次确认**（P0-B，2026-09-13 真机故障；实现见 `src/gateway/preference.py` 的 `SEAL_WORD` / `command()` / `accept()`）：原实现把"组长在群里再发一次「你想做哪一块」"和"立即封盘"绑成同一个动作（D-52 ③），没有二次确认、不可撤回 —— 真机现象是"没人交志愿，群里却出了总表"。改为：① 窗口里**还没人交过志愿** → 只重发清单（幂等），**不封盘**；② **已有人交过** → 回「现在封盘会按已有 N 份志愿分配，还有 M 人没交。回复「封盘」确认，回复别的继续等。」，窗口保持不变；③ 组长回 **`封盘`** 才结算。`封盘` 在 `awaiting=preference` 状态机内消费、**不进 7 条前缀**；只认组长，旁人喊不算数。见 §7.1 志愿窗口 |
| D-57 | **单群假设**（工程默认，可推翻；2026-09-13 定）：`data/` 全局共享、**不按群隔离**（沿用 D-30「唯一落盘位置 = 仓库根 `data/`」）—— 演示与验收都只用一个测试群，做按群隔离性价比不划算。后果：**换群或重做演示前必须清掉 `data/state.json`、`data/preferences.json`、`data/assignments.json`、`data/seen.json`、`data/direction.json`、`data/proposals.json`**，否则上一轮的志愿 / 花名册 / 投票（含冻住的窗口）/ 去重记录会被当成这一轮的；另，`data/direction.json` 的 `votes` 落的是**真实 `open_id`**，只作留痕：**不许进 M7 报告、不许进任何群消息**（外审 🟡6）。这是**有意的取舍，不是 bug**；真要支持多群再单独拍板 |
| D-58 | **事件去重**（P0-A，2026-09-13 真机故障；实现见 `src/gateway/app.py` 的 `Gateway._is_duplicate()`）：飞书会重复投递 / 重连补投同一个事件，`handle()` 不去重就会把一条指令完整跑两遍（真机现象：新群"先清单、再总表"、旧群"两次登记空白表"）。按 `message_id` 去重，落 **`data/seen.json`**（只留最近 200 条，`_SEEN_LIMIT`）；重复事件直接 `return Outcome()`（不发消息、不写业务数据）。`message_id` 为空时不去重（老事件 / 夹具没标识）|
| D-59 | **单实例保护**（P0-E，2026-09-13 真机故障；实现见 `src/gateway/app.py` 的 `SingleInstance`）：真机上同时跑了两个网关进程、同一套凭据 → 两条 WebSocket → 每条群消息被两个进程各处理一次（重复回话、落盘互相覆盖；上一轮"两次登记空白表""先清单再总表"都是它）。启动时用 **bind `127.0.0.1:47653`**（`INSTANCE_PORT`）做进程级互斥，第二个实例直接打一行醒目错误并 **`return 3`**；正常退出（含 `--seconds` 到点）放端口 + 删 `data/app.lock`。`app.lock`（`{pid, started_at, port}`）**只写来给人看、不参与判定** —— 硬退留下的残留锁不会挡住下次启动。**不用 pid 判存活**：Windows 上 `os.kill(pid, 0)` 会真的把进程杀掉。`message_id` 去重（D-58）保留，但两个进程各读各的 `seen.json` 有竞态、挡不住 —— 单实例才是这条的解 |
| D-60 | **post 富文本 / 转发归一成 text**（P1-I，2026-09-13 真机故障；实现见 `src/gateway/events.py` 的 `_post_text()`）：`to_inbound` 原来只从 content 取顶层 `text`，post 类型取不到 → `message_type="post"`、`text=""` → 路由直接静默丢弃（真机日志那行 `type=post text=` 就是它）。改为把 `title` + `content`（段落 × 元素两层数组，只取有 `text` 的元素、段间换行）拍平，并把 `message_type` 归一成 `"text"`，让 router 照常走前缀 / 志愿解析 |
| D-61 | **M2 方向候选 + 群内投票落地口径**（2026-09-14；实现见 `src/intelligence/direction.py` + `src/gateway/vote.py`）：① **候选生成是 B2 的第 2 处 LLM 点** —— 2–3 条、`id` 从 1 连续、`rubric_refs` 只能引用清单里真实存在的评分点；**没有评分点就不调 LLM**（D-48 口径），私聊发「方向」只回「去群里发」、**不烧 token**；② **收票只认开窗那个群 + 花名册成员**（两个条件同时成立）：私聊裸数字照走 7 条前缀、别的群 / 非花名册成员**静默不计**、花名册成员越界 / 非数字 → 回提示不落盘；③ **关闭三条任一、只关一次**：过半（D-35）+ 门槛（D-36）/ 超时 10 分钟（`VOTE_TTL = timedelta(minutes=10)`，`>=` 到点即关；**不跟 M4 改成 5 小时**）/ **组长拍板复用 M4 的 `封盘`**（`封盘` 取票最多、并列取编号小；`封盘 N` 直接指定、没票也能用；非组长不受理）——**不新增第 8 条口令**；④ **超时且无人过半** → 报票数明细 + **窗口冻结**（`state.vote.closed = true`：`awaiting` 保持 `vote`，候选与票数原样保留，**数字不再计票**）→ 组长在**同一批候选、同一张票数表**上 `封盘` / `封盘 N` 拍板，`reason = 超时后组长指定`（M2 复核 P1：原方案"当场关窗"会让 `封盘` 无处可消费、被迫重开一轮，而重开会**重新生成候选**、编号与票数表对不上）；冻结窗口没有 TTL —— 要么组长封盘，要么任何人再发「方向」开新窗（换群 / 重做前按 D-57 清 `data/`）；**收口的触发点 = 任何到达的消息**：超时后第一条有内容的消息就发明细 + 冻住（不是"等下一条数字"），而那条消息若是指令（「拆解」「作业书」…）**收口之后照常执行**、不许被吞（外审必修 A）；⑤ **窗口块** `state.vote = {chat_id, opened_at, opened_by, candidates[{id,title,note,rubric_refs}], votes{open_id: 编号}, closed}`（`closed: true` = 超时冻住，只有组长还能动它）；**落定产物** `data/direction.json`（整份覆盖、裸 JSON，同 `proposals.json`）：`{decided_at, decided_by, winner, candidates, votes, tally, reason}`，`reason ∈ 过半落定 / 超时后组长指定 / 组长拍板`，`decided_by ∈ vote / leader`；⑥ **与 M4 / 登记互清**（P0-D 口径延伸）：开投票窗口清 `register` / `preference` 残留，M4 开窗反向清 `vote` |
| D-62 | **M2 候选的条数与长度口径**（工程默认，可推翻；2026-09-14）：① **条数 2–3 由代码判定**（`direction._validate_directions()`），不靠 LLM 自检：1 条 / 4 条以上一律判不合格、带着失败原因重试，**重试用尽抛 `LLMError`**（降级不猜）；② **长度分两档**：给模型的 `DIRECTION_SYSTEM` 写「title ≤ 24 汉字、note ≤ 40 字」（写作指引），代码硬校验放宽到 `TITLE_MAX = 40` / `NOTE_MAX = 60`（方案 §2.1 的硬线）—— **指引收紧、判定放宽**：模型写长一点不该整轮重试，但明显超长要拦 |
| D-63 | **候选方向不进覆盖率统计 + `rubric_refs` 允许空数组**（工程默认，可推翻；2026-09-14，外审 🟡8）：① **`rubric_refs` 允许空数组、不算错** —— 它只是"这条方向呼应了哪些评分点"的线索，模型一时对不上号时给空数组比硬凑一个假 id 更诚实；`_validate_directions()` **不硬校验"至少 1 个"**（硬校验会把合理候选判死、白烧一轮 token，B2 只把 LLM 用在"生成"上）；② **候选方向不进覆盖率统计** —— 覆盖率的分母只有 `status="normal"` 的评分点（D-19 / D-40），方向候选是**脚手架**，既不进分子也不进分母：换一个方向不改变覆盖率读数 |
| D-64 | **M7 执行报告的触发口径 = 组长在群里发「报告」**（用户 2026-09-14 拍板方案 A；实现见 `src/gateway/router.py` 的 `_report()` + `src/gateway/app.py` 的 `_run_report()` + `src/report/gantt.py`）：① **只认群里、只认组长** —— 私聊发回 `REPORT_NEED_GROUP`、非组长回 `REPORT_NEED_LEADER`（组长取 `roster.leader`）：拍板权在人，口令只给组长；② 前置缺失各回各话、**都不起重活**：没花名册 → `REPORT_NEED_ROSTER`、没分配 → `REPORT_NEED_ASSIGNMENTS`（回话与 `Outcome.pipeline="report"` **同源**，必修 4 的口径）；③ 产物三件套 = **分配总表（含完成列）+ 评分点核对清单（含负责人 / 完成列）+ 甘特图 PNG**，**发群**（**拆成两条消息**：总表一条、核对清单一条 —— 拼成一条会被客户端折叠，必修 F）并落盘 `data/report.md` / `data/gantt.png`（存档仍是两份拼起来的完整版）；④ 报告是**快照**：自检项按盘上现状重算（`generations=0`），不表示"这一版拆了几轮"；⑤ 顺带**关闭待定义-36**（组长口令名 = `报告`；"查询匿名提交者"仍是 P1、不实现）；路由表 **7 条 → 8 条** |
| D-65 | **甘特图的时间条 = 示意排期，不是承诺**（工程默认，可推翻；实现见 `src/report/gantt.py`）：需求里**没有**"每张卡的开始 / 结束时间"这个字段（任务卡只有 `effort_hours`，`deadline` 是**作业级**，D-23），所以按 `task_id` 顺序把 `[今天, deadline]` 平均切开、一张卡一格，图题写明"示意排期（非承诺）"；`deadline` 为空 / 脏 → **不画截止线**、图下注明"截止未标注"（D-49 允许空，不许因此报错）；已完成的卡加深色 + `√`（`✓` U+2713 中文字体没有，退用 U+221A）；颜色按 `assignee` 分组，`depends_on` 画"前置卡右端 → 后置卡左端"的箭头；中文字体必须**在代码里显式设**（`Microsoft YaHei` / `SimHei`），否则满图方框 |
| D-66 | **M6 催办的落地口径**（§2.2；前两档 = 待定义-35 已定，其余为工程默认、可推翻；实现见 `src/gateway/reminder.py` + `app.Gateway.scan_reminders()`）：① **锚点只有一根** —— `data/assignment.json` 的作业级 `deadline`；认不出来 → **整轮跳过**（宁可漏催、不可乱催）；② **三档**：≤48h 且 >24h「48 小时」/ ≤24h「24 小时」/ 已逾期「已逾期」——**逾期也催**；③ **跳过** `completed_at` 非空的卡；④ **去重按 `(task_id, tier)`**，落 `data/reminders.json`，字段集 `{task_id, tier, assignee, chat_id, sent_at, ok}`（`assignee` = 催的是谁，必修 E），**成败都记**（`ok:false` 也是 T11 要的"送达记录"）；**去重只看发成功的** —— `ok:false` 不进 `already`、下一轮重试（必修 A），否则一次网络抖动就让那人这一档永远收不到；换档可再催一次（48h → 24h → 逾期）；⑤ @人用飞书文本语法 `<at user_id="ou_x"></at>`（写成纯文本 `@某人` 不会真 @）；⑥ 可被环境变量覆盖：`REMIND_INTERVAL_SECONDS`（默认 3600）、`REMIND_TIER1_HOURS`（48）、`REMIND_TIER2_HOURS`（24）；**启动时先扫一次**（演示不必等一个钟头）；⑦ 群取 `state.group_chat_id`（单群假设 D-57），取不到就跳过并打一行日志；⑧ **逾期也只催一次**（`(task_id, "已逾期")` 同样去重）—— 不做"每天催一次"，否则每小时扫一次就是每小时刷屏；要连催得引入第三级去重键（`task_id, tier, 日期`）。工程默认，可推翻 |
| D-67 | **重新结算不丢完成标记**（工程默认，可推翻；2026-09-14，必修 B；实现见 `src/gateway/app.py` 的 `_save_assignments()`）：M4 结算整份覆盖 `data/assignments.json`，但 `completed_at` 是**执行期的证据**，不能因为组长重开一次志愿窗口（「你想做哪一块」→ 封盘）就归零 —— 覆盖前按 `task_id` 把旧的 `completed_at` 合并回来，**仅在负责人（`assignee`）没变时**：卡换了人，新负责人的"完成"不该继承前任的；`completed_at` 仍**不改 M4 的分配语义**（D-22 / §6.4） |
| D-68 | **M3 的输入不含方向候选（“方向没进 M3”）—— 保持现状，记取舍与代价**（最终外审 F3，2026-09-14）：定稿 `docs/定稿文档-v2.md` §3.4 明写 M3 的输入 = **评分点清单**，实现与定稿一致。把「方向」并进 M3 prompt 会让 `cards` 变，而 **M8 人工基线与门③复算全部作废**（D-51 的分母是按现行产物口径标的）。代价：拆出的卡不体现所选方向，方向仅作为脚手架（同 D-63 ②的定位），覆盖率读数与方向无关。若将来要“按方向拆”，需重标基线并重跑门③ —— 属 P1 之后的口径变更，不在本次范围，代码不动 |
| D-69 | **群聊触发加 @ 门槛**（用户 2026-09-15 拍板；升级需求 U1，见 `requirements-upgrade.md`）：推翻 §7.1 的"群里任何消息按前缀命中即响应" —— 群聊生效条件改为**必须 @ 机器人**；@ 后仍走**功能词精确匹配**（D-02 的"不做自然语言理解"**不变**）。配套：① **闭嘴纪律** = 未被 @ 不说话，白名单播报 4 类（入群欢迎仅一次 / 投票发起 / 临期提醒两档 / 报告发布）；② **状态机免 @ 窗口** = 投票进行中的纯数字与 `封盘`、志愿窗口内的 `封盘`（沿用"状态机内消费"的既有做法，D-34 同源）；③ **私聊不套该规则**，沿用前缀；④ 兜底两级：能近似对上的先确认"你是想做 X 吗"，对不上才列能力清单 / 直说做不到；⑤ 文件类指令 = @ + `作业书` 取"本群最近一条未解析文件"，取不到回提示。代价：T01 需口径改写（见 D-76） |
| D-70 | **一个群 = 一个工作空间（群级数据隔离）**（用户 2026-09-15 拍板；升级需求 U2）：**推翻 D-57「单群假设」**。群的飞书唯一标识即"身份证"；落盘改为**每群独立目录** `data/workspaces/<群标识>/`，MVP 扁平 `data/*.json` 迁入对应工作空间；工作空间有**可读名**并出现在报告抬头。**私聊归属** = 绑定该用户**最近一次群内互动**的工作空间，候选 ≥2 且无法判定时反问，未加入任何群时提示先拉群。清空 / 重置 / 改名需二次确认 + 记录执行人。**一个群只做一份作业**（用户同日拍板，不做多份并存）。代价：改了数据地基 → **T01–T13 必须整体回归复跑**；升级期订正：见 `requirements-upgrade.md` §6 L11（PM 2026-09-15 裁） |
| D-71 | **无评分点作业书走并行链路（覆盖率口径零污染）**（用户 2026-09-15 拍板；升级需求 U3）：有评分点 → 现有链路与覆盖率口径**一字不改**（D-19 / D-40 不动，T03/T12 复算不受影响）；无评分点 → 按工作量 / 难度拆卡，报告出**工作量分布**、**不出覆盖率**，溯源字段 = 原文段落引用 + 估算依据。估算一律标"参考值、可调整"，不得写成结论。**不得把"无评分点"等同于"要求模糊"** —— 只有逐条内容模糊才走 T04 的"需确认"，否则整份作业书会全变待确认项（等于没拆） |
| D-72 | **任务可变更：换人 / 退出回流 / 加入补位**（用户 2026-09-15 拍板；升级需求 U4）：MVP 的分配是终局快照（D-22 / §6.4），升级后变为**可演进**：变更一律**留痕**（谁 / 何时 / 从什么改成什么 / 为什么），换人与退出需**双向确认**（本人确认退出、接手人确认接手），并触发一次群内公示（属白名单播报）。权限沿用既有 `roster.leader`（组长，D-34），**不新造角色** —— 组长拍板改派与"待确认项"，组员可提请求。回流任务进待认领池，**兜底分配保留**（T08 不砍）；仅"兜底后仍有剩余"才公示等人工拍板。契约表 1 的 P1「组长改派」视为**提前交付**；拆卡 / 并卡 / 工作量重估留 P1；升级期订正：见 `requirements-upgrade.md` §6 L14（PM 2026-09-15 裁） |
| D-73 | **输出话术 = 自然语言成句 + 事实字段锁定**（用户 2026-09-15 拍板；升级需求 U5）：所有对外输出（回执 / 催办 / 报告导语 / 报错 / 兜底）必须是**完整句子、口语词序**；禁止字段拼接（形如"已登记｜任务:T3｜状态:完成"）与公文腔（形如"系统已受理您的登记申请"），也不卖萌；允许"机械"= 短、直、不修辞。**红线：人名、任务号、时间、数字必须原样输出**，不得被润色成同义表达（"周五 18:00" 不得变成"这周末之前"）。群消息默认 ≤4 行，长内容进报告。验收为主观口径：抽 10 条真实输出由 3 名非项目成员盲评，多数判"像人"即通过 |
| D-74 | **方向双通道：候选投票（T05）与人工拍板（U6）并存**（用户 2026-09-15 拍板；升级需求 U6）：新增顶层指令 `我们要做的方向是：<任意文本>`（前缀 + 自由文本，形态同 `我想提议：…`），路由表 **8 条 → 9 条**（D-64 口径之后）。人工拍板**直接落定、不进投票**，回执写清方向、**署名**并写进方向台账；与既有候选高度重合时归并提示（"这跟候选 B 差不多，我按 B 记了"），避免同义方向并存导致后续拆解分裂。**与匿名提议严格区分**：`我想提议：…` 仍匿名（T09），拍板方向署名 —— 否则匿名机制被绕过。覆盖已定方向需组长确认；已出任务卡**不自动重拆**（理由同 D-68：重拆使 M8 基线与门③复算作废），是否重拆由人决定 |
| D-75 | **升级版另开飞书机器人，与 MVP 机器人互不污染**（用户 2026-09-15 拍板）：队友继续在 MVP 机器人上调试（`origin`），升级版用**新凭据**（新 app id/secret + 新 LLM 密钥）另起一个机器人。**隔离四件套** = ① 独立凭据文件 ② 独立数据根（配合 D-70 的按群目录）③ 独立长连接进程 ④ **独立测试群**（同群会互相抢答：MVP 仍无 @ 门槛）。凭据一律**不入库、不贴进对话**（`.env` 已被 gitignore，§7 不变）。验收 = 给升级版发消息 MVP 零响应（反之亦然） |
| D-76 | **契约口径改写 3 处（D-69 引发）+ 关闭待定义-37**（用户 2026-09-15 拍板）：① **T01** 字面是"发送任意消息 → 回指令列表（5 条）"，与 @ 门禁冲突 → 改为"**入群时主动发一条欢迎语**（含 5 条高频指令 + 有事 @我），@ 后给全 8 条"，同时**关闭待定义-37**；② **T05** 投票期间开**免 @ 白名单**（纯数字 / `封盘`），投票结束即恢复门禁，预期结果不变；③ **T11** 催办属白名单播报，**不受闭嘴纪律约束**（闭嘴纪律只约束"不接无关消息"）。验收是人工清单 + 执行记录表（D3 / D-26），口径改写须在执行记录表里注明理由；升级期订正：见 `requirements-upgrade.md` §6 L14（PM 2026-09-15 裁） |

---

## 10. 依据层级与出处

> **2026-09-15 变更**：第 3 层（方案层定稿文档 `docs/定稿文档-v2.md`）**已移除** —— 它描述的是已冻结的 MVP 方案，升级期不再作为依据。原文保留在 git 历史：`git show 9938ab7:docs/定稿文档-v2.md`。下方第 1/2/4 层与裁决规则**保留为历史记录**。

1. **截图层** —— 功能优先级表 + 验收用例表（`docs/requirements/`）。**需求真源，冲突时以此为准。**
2. **意图层** —— 《方案一.docx》：痛点、机制、定位，五段话，**无架构、无数据模型**。原件在 `C:\Users\617\Desktop\方案一.docx`，**未纳入仓库**（待定义-24）。
3. **方案层** —— 定稿文档：架构、模块、排期。**已归档为 `docs/定稿文档-v2.md`（逐字转录，D-29）**，是本仓库的**主要依据**（用户 2026-09-11 确认）。
4. **对话拍板** —— 不整合、志愿制、智能拆解、飞书机器人、人为权重大于机器权重。

**✅ 冲突裁决规则（已拍板，用户 2026-09-11 定）**

1. **第 1 层（截图）高于一切**——含定稿文档在内，谁跟截图冲突都以截图为准（D-01 不变）。
2. **第 2 / 3 / 4 层之间冲突 → 听第 3 层（定稿文档）**。即《方案一.docx》与"对话拍板"都让位于定稿文档（D-32）。
3. 理由：第 2 层原件不在仓库、无法逐句核对；第 4 层本文件自己就标着"二手转述"；只有第 3 层完整落盘、可逐句引用。

> 实操含义：以后发现《方案一.docx》或那次对话的说法跟定稿文档不一致，**不用停下问人，直接按定稿改，并在 D- 台账里记一笔**。

**⚠️ 断链警告** —— 第 4 类依据发生在另一个会话，本仓库无原始记录，只能记为**二手转述**；第 2 类原件在桌面上、未入库（待定义-24）。

**⚠️ 出处倒推提示** —— D-04（覆盖率用人工标注分母）属"后加设计"，《方案一.docx》无出处；D-03 里"落盘只存 id、展示拼原文"是工程决定，定稿只定义了 `rubric_refs` 字段本身。引用时必须说明来源。
