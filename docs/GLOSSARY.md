# 名词速查 · 小组作业项目经理机器人

> 给新对话 / 新同事用。每条一句话，右边是详情所在的文件与章节。
> 遇到这里没有的名词，去 `requirements.md` 搜；搜不到就是**未定义**，按 §8 的规矩停下问人。

---

## 一、九个模块

| 名词 | 一句话 | 详情 |
|---|---|---|
| **M0 机器人网关** | 交互层地基：飞书长连接、消息收发、**7 条指令**路由、「登记」表单 | `requirements.md` §7.1 / §7.7 |
| **M1 输入解析** | 作业书 → 作业元信息 + 评分点清单。**唯一三处 LLM 之一** | §6.0 / §6.1 |
| **M2 方向共识** | 群里发「方向」→ 出 2–3 个候选大方向 → 数字投票过半落定。**LLM 之二** | §7.6 |
| **M3 任务拆解** | 评分点 → N 张任务卡 + 自检循环。**LLM 之三，全项目的灵魂** | §6.2 / §7.3 |
| **M4 志愿分配** | 私聊收志愿 → 先到先得 → 人人有活。纯规则 | §6.3 / §6.4 |
| **M5 匿名代言** | 私聊「我想提议：…」→ 群里匿名发布，系统内部留痕。纯规则 | §6.5 |
| **M6 催办** | 每天扫 deadline，临期 48h / 24h 两档 @负责人。纯规则 | §7.1 / §8.3 |
| **M7 执行报告** | 分配总表 + 甘特图 + 评分点核对清单。模板渲染 | 定稿 §3.8 |
| **M8 评测集** | 离线：15 份作业书的 human vs agent 对比表。**不进运行时** | 定稿 §3.9 |

**五层**：交互层 M0 / 智能层 M1–M3 / 协作层 M4–M5 / 流程层 M6 / 交付层 M7（+ 旁挂证据层 M8）。

---

## 二、核心名词

| 名词 | 一句话 |
|---|---|
| **作业书** | 老师下发的原始任务书（文字版 PDF / Word / 纯文本；**图片和扫描件拒收**） |
| **评分点（rubric point）** | M1 从作业书抽出的可打分项（`R1`、`R2`…）。必须带**原文引用 `quote`**，用来防 LLM 编造 |
| **任务卡（task card）** | M3 拆出来的执行单元（`T1`、`T2`…），**7 个字段**，含 `rubric_refs` 溯源 |
| **「模块」** | 任务卡面向组员的**口语叫法**；数据模型里就是 `task_id`。志愿填的就是它 |
| **作业元信息** | 课程名 / 作业标题 / 交付形式 / **截止时间**；截止时间是**作业级一根线**，任务卡不设 `due_date` |
| **志愿（preference）** | 组员对 `task_id` 的排序（第一志愿、第二志愿） |
| **分配（assignment）** | 谁干哪张卡。`source` 四值：`volunteer_1` / `volunteer_2` / `auto` / `leader` |
| **提议（proposal）** | M5 的匿名代言记录，含真实身份，存 `data/proposals.json` |
| **「方向」** | 作业的**选题大方向**（不是章节、不是任务卡）。M2 在群里投票定 |
| **花名册（roster）** | 组长 + 组员（`open_id` + 显示名），群里「登记」产出，存 `data/members.json` |

---

## 三、机制与指标

| 名词 | 一句话 |
|---|---|
| **覆盖率三口径** | ① 循环退出：分母 = 可拆评分点，**必须 100%**；② D5 门：分母 = 人工标注点，**≥80%**；③ M8 报告值：同 ②，**不设阈值**。三者禁止混用 |
| **均衡度两套口径** | 循环内判据 `max/min ≤ 3`；M8 报告展示 `1 − (max−min)/Σ`。两个独立函数，禁止复用 |
| **自检循环 `check()`** | 生成 → 最多 2 轮代码判定 → 失败喂回重拆。**LLM 不参与判定** |
| **D5 发布门** | **9/15 周二晚**，三条全过才开始 P1；任一不过 → 冻结全部 P1 |
| **前缀精确匹配** | 指令只看开头几个字，不做自然语言理解 |
| **状态优先** | 路由顺序：先剥 @段 → 再查 `state.json` 的 `awaiting` → 最后才认前缀。裸数字靠它解释成票号 / 志愿序号 |
| **`awaiting`** | `state.json` 里的"当前在等什么"：`vote` / `preference` / `register` |
| **投票窗口** | M2 发候选时开启；某方向拿到**已投票者过半** / 超时 10 分钟 / 组长拍板，任一满足即关 |

---

## 四、文档与编号

| 编号 / 文件 | 是什么 |
|---|---|
| **B1–B10** | 硬性边界，`requirements.md` §1。**不可突破** |
| **T01–T13** | 验收用例，来自截图原件。**人工验收清单**（每条跑一次记一次结果），不是自动化脚本 |
| **S1–S13** | 用户场景，来自截图原件 |
| **待定义-N** | 未定义清单编号，`requirements.md` §8。**仍开放的条目 = 停下问人** |
| **D-NN** | 决策台账编号，`requirements.md` §9。引用决策就说 D 编号 |
| **截图原件** | `docs/requirements/*.png` + 逐字转录。**第 1 层依据，冲突时以它为准** |
| **定稿文档** | （原 `docs/定稿文档-v2.md`，第 3 层依据）。**已于 2026-09-15 移除**，见 git 历史 |
| **《方案一.docx》** | 第 2 层依据（意图层），**尚未入库**，在桌面上 |

---

## 五、绕不过去的几个专有说法

| 说法 | 出处与含义 |
|---|---|
| **「研采」** | 三个候选方案之一。**贡献验收方法论**——T01–T13 用例表与 D5 发布门都来自它 |
| **「智汇简报」** | 三个候选方案之一。**工程兜底**：队友已有可运行的 Dify 原型，主线彻底跑不通时用它保底交差 |
| **「不整合」** | 来自那次对话拍板，**含义仍未解释**（待定义-20，不影响开发） |
| **「智能只押在拆解」** | 不是笔误。"押"= 把智能全押在 M3 这一处 |
