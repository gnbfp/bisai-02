# 架构设计 v1.2

> 依据：`requirements.md`（需求唯一真源）与其 §9 决策台账 D-01…D-36，以及方案层定稿文档（已于 2026-09-15 移除，见 git 历史）。
> 本文件只描述**怎么实现**，不改需求。需求冲突以 `requirements.md` 为准。
> 标【待定义-N】处继承 `requirements.md` §8。**v1.1 后影响编码的条目已全部关闭**，仅剩两条不阻塞项（见 §10.3）。
>
> **v1.1 修订记录（2026-09-11）**：① §10 未定义项清算完毕，两条**开放项**已挪回 `requirements.md` §8.1；② 指令集补第 6 条 `完成 T3`；③ 任务卡补至 7 字段（D-24）；④ 截止时间定为**作业级**一根线（D-23）；⑤ 落盘目录裁定为仓库根 `data/`（D-30）；⑥ `state.json` 由"设计建议"**转正为必需**。
>
> **v1.2 修订记录（2026-09-11）**：⑦ 路由补"先剥 @段、再先看状态、最后看前缀"（D-33）；⑧ 新增第 7 条指令「登记」与花名册 `data/members.json`（D-34）；⑨ 投票分母定为"**已投票者**过半"（D-35），并加参与人数门槛（D-36）。**至此 §10 已无挡 P0 的开放项。**

---

## 0. 架构一句话

**单进程 Python 服务。两个入口（飞书长连接 / 命令行）→ 一个前缀路由 → 五层九个模块 → JSON 落盘。**

---

## 1. 从硬性边界推出的三条架构基线

需求 `§1` 的 B1–B10 是本架构的地基，不重复。以下三条是从中推出的、会影响代码形态的结论：

1. **不引入 HTTP 服务。** B1（单进程）+ B6（命令行主链路共用同一套逻辑）⇒ 两个入口直接调用同一组模块函数。没有端口、没有 RPC、没有跨进程通信。**若将来要拆进程，必须先改 B1。**
2. **模块之间是函数调用，不是消息传递。** 同样是 B1 的直接后果。所谓"五层"是**代码分层**，不是部署单元。
3. **M8 只读文件，不 import 运行时。** B3 + B8 ⇒ 评测代码必须独立于被测代码，否则"不信 M3 自报的数"就无从谈起。

---

## 2. 目录结构

```
项目根/
├── AGENTS.md                    AI 入口与工作约定
├── requirements.md              需求唯一真源
├── docs/
│   ├── ARCHITECTURE.md          本文件
│   └── requirements/            需求原件（截图 + 逐字转录）
├── src/
│   ├── gateway/                 M0 交互层：WebSocket 事件循环、7 条指令路由
│   ├── intelligence/            M1 解析 / M2 方向 / M3 拆解（LLM 调用 + 自检循环）
│   ├── collab/                  M4 志愿 / M5 匿名代言（纯规则状态机）
│   ├── schedule/                M6 催办（定时扫描）
│   ├── report/                  M7 执行报告生成
│   └── main.py                  CLI 入口（主链路：作业书 → 任务卡 → 核对清单）
├── eval/                        M8 评测集（离线脚本，独立于运行时）
├── tests/
└── data/                        运行时 JSON 落盘目录（.gitignore，**唯一落盘位置** —— D-30）
```

**目录结构源自 D-08**（用户给定的组织建议）。

- `src/data/` **已删除**：落盘只有根目录 `data/` 一处（D-30），需求 B4 亦已同步。
- 原树里的 `TEAM_GUIDE.md` **在仓库中并不存在**，已从树里删除；将来真要做团队手册再新建，不要预先占位。

---

## 3. 依赖方向（单向，禁止回头）

```
                    ┌──────────────┐
  飞书群 ──WS──▶    │   gateway    │  M0   ◀── CLI (src/main.py)
                    └──────┬───────┘
                           │ 调用（唯一入口）
        ┌──────────────┬───┴────┬──────────────┐
        ▼              ▼        ▼              ▼
 ┌────────────┐  ┌──────────┐ ┌────────┐ ┌──────────┐
 │intelligence│  │  collab  │ │schedule│ │  report  │
 │  M1 M2 M3  │  │  M4 M5   │ │   M6   │ │    M7    │
 └─────┬──────┘  └────┬─────┘ └───┬────┘ └────┬─────┘
       └──────────────┴───────────┴───────────┘
                      ▼
                ┌───────────┐
                │   data    │  JSON 落盘（唯一有副作用的地方）
                └───────────┘
                      ▲
                      │ 只读，单向
                ┌───────────┐
                │   eval/   │  M8（离线，不 import src/）
                └───────────┘
```

**四条硬规则：**

| 规则 | 理由 |
|---|---|
| `gateway` 是唯一能"对外说话"的层 | B5：只有它认识飞书 |
| `intelligence` / `collab` / `schedule` / `report` **不得 import `gateway`** | 它们只返回数据，不自己发消息。要发消息就把结果交回 gateway |
| `data` 是唯一有副作用的层 | 便于测试：其余模块全是纯函数 |
| `eval/` **不得 import `src/`** | B3 + B8：评测必须独立复算，不能复用被测代码的判定函数（否则等于自己改自己的卷子） |

---

## 4. 数据落盘

| 文件 | 内容 | 写入者 | 读取者 |
|---|---|---|---|
| `data/assignment.json` | **作业元信息**：课程名 / 作业标题 / 交付形式 / **截止时间**（`§6.0` 结构） | M1 | M6、M7 |
| `data/rubric.json` | 评分点（`§6.1` 结构） | M1 | M3、M7、M8 |
| `data/cards.json` | 任务卡（`§6.2` 结构） | M3 | M4、M7、M8 |
| `data/preferences.json` | 志愿（`§6.3` 结构） | M4 | M4 |
| `data/assignments.json` | 分配 + **完成标记**（`§6.4` 结构，含 `completed_at`） | M4 写入分配 / M6 写入完成标记 | M6、M7 |
| `data/proposals.json` | 提议记录（含真实身份） | M5 | M5、组长查询（P1） |
| `data/members.json` | **花名册**：组长 + 组员（`open_id` 与显示名）（`§6.6` 结构） | M0「登记」 | M2（投票分母）、M4（广播对象）、M5（组长）、M6（@对象）、M7（显示名） |
| `data/state.json` | 会话状态机（当前阶段、待确认项、M2 投票中的候选与票数） | gateway | 全部 |

> `state.json` **转正为必需**（v1.1）：M2 投票、M4 收志愿、M3 组长确认模糊点，这三处都要跨消息保持状态。落盘形式受 B4 限定为 JSON。
> **落盘只有根目录 `data/` 一处**（D-30）；`src/` 下不建存储目录。

---

## 5. LLM 调用点（全项目仅 3 处 —— B2）

**统一契约（三处都遵守）：**

1. 强制 JSON 输出（DeepSeek 的 `response_format: json_object`）
2. 出参过 schema 校验
3. 校验失败重试，**最多 2 次**
4. 仍失败 → 降级（M1/M3 报错给用户，M2 退回 `unknown`），**不猜**

| 模块 | 输入 | 输出 | 约束 |
|---|---|---|---|
| M1 解析 | `{ text: string }`（`§7.5` 抽取后的纯文本） | **作业元信息（课程名 / 作业标题 / 交付形式 / 截止时间）+ 评分点数组** | 每个点必须带 `quote`（原文）；模糊点标 `status=ambiguous`；元信息落 `data/assignment.json` |
| M2 方向 | **评分点清单**（`data/rubric.json`） | 2–3 个候选**选题大方向** | 标注"仅供参考、由全组拍板"；回复数字投票，过半落定；组长可直接拍板跳过 |
| M3 拆解 | 评分点数组 + few-shot 示例 | 任务卡数组 | 输出后必须过 `check()`；**LLM 不参与判定** |

---

## 6. 关键算法与实现位置

### 6.1 覆盖率三口径 → 两个独立实现（D-19 + B8）

| 函数 | 位置 | 分母 | 要求 |
|---|---|---|---|
| `coverage_loop()` | `src/intelligence/coverage.py` | `status=normal` 的点 | 100% |
| `balance_loop()` | `src/intelligence/coverage.py` | — | `max/min ≤ 3` |
| `coverage_gate()` | `eval/coverage.py` | **人工标注**的可拆点 | ≥80% |
| `balance_report()` | `eval/coverage.py` | — | `1−(max−min)/Σ` |

**`src/` 与 `eval/` 各写一份，不共享代码。** 这是刻意的重复——D-16 要求"不信 M3 自报的数"，共用函数就等于自己改自己的卷子。

### 6.2 M3 自检循环（D-16 / D-19）

```python
# src/intelligence/decompose.py
for round in range(2):
    failures = check(cards, rubric)
    if not failures:
        break
    cards = llm_regenerate(rubric, feedback=failures)
```

**边界处理（原待定义-31，已关闭 —— D-25）**：
- `cards` 为空 → **短路返回不达标**，不调 `max()`
- 某张卡 `effort_hours = 0` → `min_hours` 取下限 `max(min_hours(cards), 0.5)`，避免除零

判定函数本体见 `requirements.md` §7.3。

### 6.3 作业书抽取管线（D-15 / D-21）

```
文件 ──┬─ PDF  ──▶ PyMuPDF 抽正文 + find_tables() 抽表格
       ├─ Word ──▶ python-docx
       └─ 纯文本 ─▶ 直通
                    │
                    ▼
        归一化为 { text: string }
        表格块用 === 表格区域 === 分隔，每行为 "- 列名：值｜列名：值"
                    │
                    ▼
        [软校验] weight 加总 ∈ [90,110] ？否 → 附警告（不拒收）
                    │
                    ▼
                交给 M1
```

**拒收分支**：扫描版 PDF（空文本层）→ 回复"请发文字版"；图片 → 拒收（OCR 已砍，P2）。

**软校验的前置条件（原待定义-32，已关闭 —— D-25）**：仅当**全部**评分点都有 `weight`、且能归一化到百分制时才执行；有缺失、或本身非百分制（如 20 分制）→ **整体跳过，不误报**。`weight` 缺失的点也不参与均衡计算。

**表格行序列化（原待定义-33，已关闭 —— D-25）**：列内分隔符 `｜`；**每行前加 `- ` 前缀、行间用换行**，避免跨行粘连。

---

## 7. 指令路由（M0，前缀精确匹配 + 状态优先 —— B10 / D-02 / D-33）

| 前缀 | 目标 | 通道 |
|---|---|---|
| `作业书` + 文件 | M1 → M3 | 群 |
| `方向` | M2 | 群 |
| `拆解` | M3 | 群 |
| `你想做哪一块` | M4 | **私聊** |
| `我想提议：…` | M5 | **私聊** |
| `完成 T3` | M6：写 `assignments.json` 的 `completed_at` | **私聊** |
| `登记` + @组员 | M0：两步确认建花名册 → `data/members.json` | **群** |

未命中任何前缀 → 回复"我只会这几件事" + 指令列表（T01）。

- 路由表**不再有空洞**：原待定义-34 缺的是第 6 条 `完成 T3`（D-22 / D-31）；第 7 条 `登记` 见 §8.4（D-34）。
- `作业书` + 文件**必须续跑 M3**：T02 要求"发作业书 → 产出 ≥3 张任务卡"。`拆解` 是**单独重跑 M3** 的口令（组长确认模糊点后重拆用）。群 / 私聊都能发作业书，恒等于 M1。
- **路由优先级：先剥 @段 → 再看状态 → 最后看前缀（D-33）** —— 实现顺序：
  0. 摘掉 @段 再匹配（`@机器人 拆解` 要认成 `拆解`），但 **@ 到的人与其 `open_id` 要单独留着**（「登记」靠它）；
  1. 查 `state.json` 的 `awaiting`：`vote` → 数字 = 票号；`preference` → 数字 = 志愿序号；`register` → 按登记表单解析；
  2. 都不在等待态 → 走上面 7 条前缀匹配；
  3. 仍不中 → 回复指令列表（T01）。
- **M2 投票窗口（D-33 / D-35 / D-36）**：M2 发候选时开启；关闭条件 = 某方向拿到**已投票者过半** / 超时 10 分钟 / 组长拍板，任一满足即关；窗口内一人一票、后投覆盖先投。**门槛**：参与人数须达花名册人数的过半（向上取整）才允许落定（防"第 1 票即过半"）；超时无人过半 → 交组长拍板。残留风险与备案见 §11。
- ⚠️ 口径待定（**不影响编码**）：T01 原文写"指令列表（5 条）"，本表 **7 条** —— 见 `requirements.md` §8.1 待定义-37。按 7 条实现即可。

---

## 8. 四条主链路时序

### 8.1 作业书 → 任务卡 → 核对清单（命令行主链路，D-10 的门 ②）

```
CLI: 指定作业书文件
  └─▶ M1 抽取（§6.3）→ 评分点 ──▶ data/rubric.json
  │                 └─ 作业元信息 ──▶ data/assignment.json（含 deadline）
  └─▶ M3 拆解 + 自检循环（§6.2）→ 任务卡 ──▶ data/cards.json
  └─▶ M7 简版 → 命令行输出「评分点核对清单」+ 覆盖率
```

### 8.2 志愿分配

```
群: 「方向」→ M2 出候选 ──▶ 群内投票（回复数字，窗口 10 分钟）→ 过半 / 超时 / 组长拍板 即落定
群: M3 拆完 → 私聊广播「你想做哪一块」
私聊: 组员回复志愿 ──▶ data/preferences.json
截止: M4 按 submitted_at 先到先得分配
      ├─ 命中第一志愿 → source = volunteer_1
      ├─ 落选 → 第二志愿 → source = volunteer_2
      └─ 未回复 → 兜底 → source = auto（保证每人 ≥1 个任务）
      └─ 组长指定 / 改派 → source = leader（P1）
      └─▶ data/assignments.json → M7 完整版（分配总表 + 甘特图）发群
私聊: 「完成 T3」→ 写 data/assignments.json 的 completed_at（D-31）
```

### 8.3 催办（M6）

```
schedule 每天固定时间扫描 data/assignments.json
  └─ 取 data/assignment.json 的作业级 deadline 作唯一截止线（D-23）
  └─ 找出 completed_at IS NULL 且临期的任务
  └─ 两档提醒：临期 48 小时 / 24 小时（待定义-35 已关闭）
  └─▶ 交回 gateway → 群内 @负责人
  └─ 记录送达（T11 要求"有送达记录"；保留截图进演示材料）
```

- **此链路已无规格空洞**：原待定义-34（截止时间 / 完成指令）与待定义-35（阈值）均已关闭。
- 完成入口：组员私聊「`完成 T3`」→ gateway 写 `completed_at`（D-31）。**MVP 不做提交物上传。**
- **谁来叫醒 M6**：B1 单进程 ⇒ 定时器**必须在本进程内**（asyncio 定时任务或后台线程，二选一，不新增进程、不引入外部调度器）。选哪种属于实现细节，编码 M6 时定，不影响本架构。
- **成员标识从哪来**：已关闭（D-34）——靠 §8.4 的「登记」表单从 @ 结构里拿 `open_id`。本节的 @负责人、§8.2 的私聊广播都从 `data/members.json` 取人。

### 8.4 花名册登记（M0，「登记」指令 —— D-34）

```
群里: 组长发「登记」
  └─▶ gateway 回一张空白表单（模板）
组长: 照模板填 + @人 发回 ──▶ 机器人从 @ 结构里拿 open_id（不需要群成员名单权限）
  └─▶ gateway 回显规范化后的名单（谁是谁、共几人）
组长: 回「同意」 ──▶ 写 data/members.json
      └─ 回别的 / 5 分钟没回 → 作废，不动原名单
      └─ 重发「登记」→ 整体覆盖旧名单
```

- 硬规则：`组长` 有且只有一个；`组员` 至少 2 人
- 落盘后，M2 的投票分母、M4 的广播对象、M5 的组长身份、M6 的 @对象、M7 的显示名全部有据可依

---

## 9. 排期与发布门（D-10 / D-11）

| 日期 | 里程碑 | 退出条件 |
|---|---|---|
| 9/11 五 今晚 | 定稿 + 认领分工 | 飞书团队注册、自建应用、LLM key 到位 |
| 9/12 六 – 9/13 日 | M0 网关骨架 + M1 原型 | 测试群收发跑通；3 份作业书评分点出来 |
| 9/14 一 | M3 迭代 + 人工基线 | few-shot 2 份入库；前 5 份基线标完 |
| 9/15 二 | **D5 检查点** | 三条门全过 |
| 9/16 三 | 指令层上线 | M4 + M5 + M2 可用 |
| 9/17 四 | 交付层 + 全量评测 | M7 完整版 + M6 @催办 + 15 份对比表 |
| 9/18 五 | 彩排 | 5 分钟演示跑 3 遍 + 材料定稿 |
| 9/19 六 | 提交 | 提前提交，不压哨 |

**D5 三条门**：① M0 测试群收发跑通；② 主链路命令行版跑通（M1+M3+M7简版）；③ 前 5 份覆盖率达标。**全过 → 开始 P1；任一不过 → 冻结全部 P1。**

---

## 10. 未定义项 —— 已全部清算（v1.2）

### 10.1 本文件原本新发现的两条 —— **均已关闭**

| 编号 | 原问题 | 结论 |
|---|---|---|
| 待定义-34 | 任务卡无截止时间、全局无"标记完成"指令 | 截止时间 = `data/assignment.json` 里的**作业级** `deadline`（`§6.0` 结构），任务卡不设 `due_date`（D-23）；"完成" = 私聊「`完成 T3`」→ 写 `completed_at`（D-22 / D-31） |
| 待定义-35 | 「两档临期提醒」的阈值 | **48 小时 / 24 小时**（D-25） |

### 10.2 继承 `requirements.md` §8 —— **全部已关闭**

`待定义-6b`（→ §5 M2 输入行、`requirements.md` §7.6）、`待定义-8`（→ §4 `proposals.json` 与真实身份留痕）、`待定义-9`（编号笔误 → D-27）、`待定义-11` / `待定义-12`（→ D-26 / D-27）、`待定义-25`（无人认领 → D-28）、`待定义-31 / 32 / 33`（→ §6.2 / §6.3）、`待定义-26`（→ 工程兜底，见 `requirements.md` §8.3）。

### 10.3 仍然开放的几条 —— 详见 `requirements.md` §8.1

| 编号 | 内容 | 挡不挡编码 |
|---|---|---|
| 待定义-36 | 组长查询匿名提交者的私聊**口令名** | 不挡：P1 功能，P0 链路不依赖 |
| 待定义-37 | T01 原文"指令列表（5 条）"与 §7 **七条**路由的口径 | 不挡：纯验收口径，按 7 条实现即可 |

> **编码前必读的结论**：**已无挡 P0 编码的开放项**，可以直接开工。剩下两条都不挡：36 是 P1 功能、37 是验收说法。
>
> 另有一条**工程默认**，你不认可随时推翻：**D-36**（投票参与人数门槛 —— 防"第 1 票就宣布过半"）。

> **待定义-38 已关闭**（D-33：先看状态、再看前缀；投票窗口见 §7）。未采用的备选（@机器人才算指令 / 卡片点选 / 搬去私聊）留档在 `requirements.md` §8.1 末尾 —— 其中"@机器人"是误判严重时的启用预案。

---

## 11. 风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| ~~飞书平台能力是否真支持长连接 / 私聊 / @ / 卡片~~ | ~~地基假设，不成立则整个交互层要改~~ | **已实测通过（2026-09-11）**：飞书侧草稿机器人可运行。本条从"未验证假设"降级为**常规运维项** |
| ~~飞书权限 / 用户标识来源未纳入设计~~ | ~~拿不到名单与 `open_id` ⇒ 私聊广播、@催办都做不了~~ | **已关闭**（D-34）：靠群内「登记」从 @ 结构里拿 id，**不需要"读群成员名单"权限**。**剩下的真风险**：下载群里的作业书文件仍需文件下载权限，且可能需管理员审批 —— 今晚建应用时顺手申请 |
| **群里闲聊的纯数字在投票窗口内被计成票** | 投票结果失真，T05 现场演示翻车 | 已按 D-33 兜底（窗口 10 分钟、一人一票、后投覆盖）；**备案 = 改成"@机器人才算指令"**，实测误判严重即启用 |
| LLM 输出非法 JSON | 解析失败、链路断 | `json_object` 模式 + schema 校验 + 重试 2 次 + 降级不猜 |
| LLM 编造评分点 | 覆盖率虚高、M8 复算对不上 | `quote` 必须原文可核对；M8 独立复算（B8） |
| 作业书评分表在表格里被抽扁 | M1 抽丢评分点 | `find_tables()` + 权重加总软校验（§6.3） |
| 飞书长连接断线 | 群消息收不到 | SDK 自动重连 + 启动打印连接状态；彩排前必检 |
| 演示录像返工 | 9/18 彩排是最晚发现问题的机会 | 9/18 跑满 3 遍再定稿 |
| M4/M5/M6 无人认领 | 9/16–9/17 直接塌 | **已关闭**（D-28）：三者归**机器人 owner**。今晚只需确认谁当机器人 owner |

---

## 12. 上手清单 —— 从零到能跑

> 给新对话 / 新同事。读完这一节就知道要装什么、要申请什么、代码从哪儿开始写。

### 12.1 需要准备的东西

| 项 | 说明 |
|---|---|
| Python | 3.10+ |
| **飞书** | 企业自建应用 + 机器人能力 + WebSocket 长连接（**已实测通过**）。**实测可用且已开通的权限组合（2026-09-12）**：`im:message:readonly`（读单聊 + 群组消息）、`im:message.group_msg.include_bot:read`（含机器人消息的群消息）、`im:message.p2p_msg:readonly`（私聊）、`im:message:send_as_bot`（发消息）、`im:message:update`、`im:resource`（下载文件资源）。另开了 `im:message:send_multi_depts` / `im:message:send_multi_users`，本项目未用到（催办与私聊都按单个 `open_id` 发）。**权限集已冻结、不再裁剪**（D-44，2026-09-12）：后台实测开通的就是这 9 条（`im:resource` 在后台占两行：应用身份 + 用户身份），裁剪需重走发布审批并重测，收益不划算。**坑**：`im:message.group_msg.include_bot:read` 会让**机器人自己的消息也回来** ⇒ 路由必须先滤 `sender_type == "app"`。诊断：`python -m tools.probe_feishu` |
| **LLM** | DeepSeek 或 GLM 的 API key（只服务三处：M1 / M2 / M3） |
| 依赖库 | `lark-oapi`（飞书 SDK）、`PyMuPDF>=1.23.0`（PDF + `find_tables()`）、`python-docx`、`matplotlib`（甘特图）、一个 LLM SDK |

### 12.2 环境变量（**建议命名**，实现时可改）

```
FEISHU_APP_ID / FEISHU_APP_SECRET    飞书自建应用
LLM_API_KEY / LLM_BASE_URL / LLM_MODEL
DATA_DIR=data                        落盘根目录（默认仓库根 data/）
```

### 12.3 两个入口怎么跑

```bash
# ① 飞书入口：长连接，不占端口、不需要公网
python -m src.gateway.app

# ② 命令行主链路（D5 门 ② 就是验它）
python -m src.main --file 作业书.pdf
#   → 评分点 + 任务卡 + 「评分点核对清单」+ 覆盖率

# ③ M8 评测（独立于运行时，不 import src/）
python -m eval.run --baseline data/baseline/
```

### 12.4 建议的开发顺序（与 §9 排期一致）

1. **`data/` 落盘层** —— JSON 读写。它是唯一有副作用的地方（§3），先做好，后面都好测
2. **M0 网关** —— 长连接 + 7 条指令路由（先剥 @段、再看状态、最后认前缀）+「登记」表单（§7 / §8.4）
3. **M1 解析** —— 文件 → 纯文本（§6.3）→ 作业元信息 + 评分点
4. **M3 拆解 + `check()` 自检循环**（§6.2）—— 灵魂，也是 D5 门 ③ 的主角
5. **M7 简版核对清单** —— 到这一步，D5 三条门都能验了
6. 之后依次：M2 / M4 / M5 / M6 / M7 完整版 / M8 全量

> M0 与 M3 是**并行**的两条线（前者是地基、后者是关键路径），互不阻塞。

### 12.5 常见坑

- **甘特图中文**：matplotlib 默认字体不含中文，演示会显示成方框 —— 提前配好中文字体。
- **权重加总**：作业书评分表常被抽丢行，记得开软校验（§6.3）。
- **不要**给 M4–M7 加 LLM（B2）；**不要**引入数据库、HTTP 服务或第二个进程（B1 / B4 / §1）；**不要**让 LLM 判定自己的产出（B8）；**不要**写飞书文档、申请 `docx` 权限（B7）；**不要**在 `data/` 之外落盘（D-30）。

### 12.6 一张图记住数据流

```
作业书 → 作业元信息 + 评分点 → （群投票定方向）→ 任务卡 → 私聊志愿
      → 分配 → 催办 → 执行报告（分配总表 + 甘特图 + 核对清单）
                              ↓
                        M8 离线复算，不信运行时自报的数
```
