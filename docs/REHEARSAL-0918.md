# 9/18 彩排清单（只做不依赖 U2 / U4 的部分）

> **依据**：`docs/ARCHITECTURE-UPGRADE.md` §4.6（矩阵未覆盖的三格）、§9.1（18 条失败路径话术）、§9.3 / §10.3（未验证项）。
> **为什么只做这一部分**：这三格必须赶在 **U2 动 `data-upgrade\` 之前**验完 —— 一旦工作空间隔离落地，环境要重建，现在这份"单工作空间"的日志就再也复现不出来。
> **本批不碰**：不迁移、不清空、不改名、不动 `data-upgrade\workspaces\`（那是 U2 的动作，走 `docs\OPERATIONS-U2.md`）。
> **谁做**：组长（真人发消息）+ 1 名记录人（看终端日志）。**记录人必须盯着网关窗口的 stdout**。

> **时效注（v1.30，2026-09-17）**：U2（**④-a** 两段式工作空间）已于 `21fd34a` 落地，并做过一轮真机验收（`docs\evidence\2026-09-17-u2-acceptance.md`）⇒ 上面那条「必须赶在 U2 动 `data-upgrade\` 之前验完」**只对预 U2 的那批成立**（穿透批 run1–run5 已跑掉）。本清单 9/18 的定位改成两条：① 补 **U2 之后才成立**的遗留（§1 的**格 D / 格 E**）；② 正式彩排（§2 的 10 条 + T01–T13 回归执行记录表）。**`data-upgrade\workspaces\` 现在是运行时的真数据域，别再当「待重建环境」处理。**

---

## 0. 前置（5 分钟）

1. **起升级版**（照抄这段；参数别自己发明）——两个窗口：一个跑网关，一个记日志。

```powershell
cd D:\AI创新创业大赛

# ① 清场确认：上一轮进程 / 锁文件（pid 还在 ⇒ 先把那个窗口关掉）
Get-Process python -ErrorAction SilentlyContinue | Select-Object Id, StartTime
Get-Content -LiteralPath .\data-upgrade\app.lock -ErrorAction SilentlyContinue

# ② 日志放仓库外：仓库里的运行日志可能带凭据（.gitignore 明令不入库），且仓库外不会被系统清理
$logDir = 'D:\rehearsal-logs'
New-Item -ItemType Directory -Force -Path $logDir | Out-Null
$stamp = Get-Date -Format 'yyyyMMdd-HHmmss'
$log   = Join-Path $logDir "u1_0918-$stamp.log"   # 每次一个新文件 —— 绝不覆盖上一轮

# ③ 中文不乱码 + 日志实时刷盘（网关侧自己不设这两个变量）
$env:PYTHONUTF8 = '1'; $env:PYTHONUNBUFFERED = '1'

# ④ 先冒烟 20 秒：只看「文件下载权限是通的」那一行
.\run-upgrade.ps1 -Probe -Seconds 20

# ⑤ 正式跑：10 分钟自动退出，边跑边留档（中途要停就 Ctrl+C）
.\run-upgrade.ps1 -Seconds 600 2>&1 | Tee-Object -FilePath $log
Write-Host "日志 = $log"
```

- `-Seconds` 到点**自动退出**（省得跑完忘了关窗口 ⇒ 后面 U2 动盘时被进程锁挡住）。
- `-Echo` 只在 `-Probe` 下有效 —— 网关侧不认 `--echo`（不带 `-Probe` 传它必 `exit 2`）。
- **硬规则：原始日志不许覆盖。** 文件名**必须带时间戳**（上面 `$stamp` 那段），或退一步用 `-Append` + 先写一行分隔标记；**永远不要**让第二次跑把第一次的日志截掉 —— 今天已经吃过一次（重启把日志截成 **1038 B**，那一轮的静默证据就没了）。重跑 = 新文件，旧文件留着。
- 关窗口前先确认 `$log` 有内容；收工要按下面第 4 条贴进证据文件。
2. 用**独立测试群**（「机器人007」的那个群），私聊用你自己的账号。
3. **判"静默"的唯一方法**：日志里**只有 `recv`、没有 `-> … ok` 那一行**。别靠"群里没看到"判断。
4. 记录人把网关窗口的输出**整段留着**（一会儿要贴进证据文件）。
5. 下面每一格都记下**时间戳**（HH:MM:SS），写结论时要对齐日志。

**收工后**：日志贴进新证据文件 `docs\evidence\2026-09-18-rehearsal.md`，形态照 `docs\evidence\2026-09-16-u1-smoke-real-device.md`
（**剔除带 `access_key` / `ticket` 的连接行**，其余逐字原文贴回）。

---

## 1. 五格（§4.6 未覆盖的 3 格 + U2 之后才成立的 2 项）

### 格 A：图片消息（群静默 / 私聊拒收）

| 步 | 在哪 | 发什么 | 期望 | 判据（代码口径） |
|---|---|---|---|---|
| A1 | 群 | 一张图（**不 @**） | **零响应** | `route()` 的 `message_type == "image"` 分支：群里直接 `Outcome()` |
| A2 | 私聊 | 同一张图 | 一句短拒收（`IMAGE_REJECTED`） | 私聊保留回执 |
| A3 | 私聊 | 先投一份 PDF，再发一张图，然后发 `作业书` | **仍然解析那份 PDF** | 图片**不入缓存**（D-45 ①：一张图不能把刚发来的 PDF 挤掉） |

- **翻车长什么样**：群里回了 `IMAGE_REJECTED` ⇒ U1 的群里静默没生效（要回 §12.3 第 13 条）。
- **翻车长什么样**：A3 回"没文件" ⇒ 图片把 `pending_file` 挤掉了（D-45 ① 被破坏）。

### 格 B：投票窗 × 群内裸数字（免 @ 白名单）

| 步 | 在哪 | 发什么 | 期望 |
|---|---|---|---|
| B0 | 群 | `@机器人007 作业书`（先有评分点）；没有花名册就先 `@机器人007 登记` | 出评分点 / 出花名册 |
| B1 | 群 | **花名册成员**发 `@机器人007 方向` | 候选 + 开窗回话（日志有 `-> … ok`） |
| B2 · 成员裸数字（**第二步发**） | 群 | **花名册成员**直接发 `2`（**不 @**） | **计票 + 有回执** |
| B3 · 非成员裸数字（**第一步发**） | 群 | **非花名册成员**发 `2`（**不 @**） | **静默** |
| B4 | 群 | 投票收口后，再发一次 `2`（**不 @**） | **静默** |

- **B2 的判据（代码口径）**：`may_speak()` 的免 @ 白名单 = `vote.exempt()`：**投票窗内 + 开窗那个群 + 花名册成员 + 纯数字** ⇒ 放行。
- **B4 的判据**：T05 —— 窗口一关（`awaiting` / 窗口块被清）白名单立刻失效 ⇒ **投完立即恢复门禁**。
- **⚠️ B1 的发信人必须是花名册成员**（U1 收尾的「开窗查名册」一落地，非成员发 `方向` 会被拒）—— run5 那次就是**非成员**（`ou_a7986…`）开的窗，加了判据这条就回绝了。
- **执行顺序（PM 2026-09-16 核代码后补）**：**先 B3（非成员）再 B2（成员）** —— 非成员那条**先**发、成员那条**后**发，两条连着、中间不插别的动作。反过来（成员先发）时，非成员那条静默会被「窗已经收口 / 状态已经变了」解释掉 ⇒ **是假阳性**，证明不了白名单真的校验了花名册。
- **⚠️ 门槛（D-36；代码 = `src/gateway/vote.py` 的 `_threshold()`）**：门槛 = `ceil(花名册人数 / 2)`，且落定还要「某方向**已投票者**过半」⇒ **演示第 6 步要出「方向已定」，必须安排 ≥2 个花名册成员投票**（当前花名册 3 人 ⇒ 门槛 2）。**只投 1 票永远不会自动收口**，只能等 10 分钟超时（`VOTE_TTL`）或组长 `封盘`。
- **⚠️ 分母是实时花名册**：门槛按**当下**的 `members.json` 现算 ⇒ **演示期间花名册必须冻结** —— 中途有人 `@机器人007 登记` 进来，门槛就从 2 变 3（3 人 ⇒ 2、4 人 ⇒ 2、5 人 ⇒ 3），本来够的票数就不够了。
- **翻车长什么样**：B2 静默 ⇒ 白名单没生效 —— **演示第 6 步（组员回数字投票）会当场死**，这是本格最值钱的结论。
- **翻车长什么样**：B3 计票 ⇒ 白名单没校验花名册；**B4 还计票 ⇒ 窗口没收口 / 门禁没恢复**。
- **B4 为什么这轮必须跑（2026-09-17 补）**：U2 真机验收 §5 只跑了**正面**（正常开窗 ⇒ 群内裸数字计票），**T05（窗一关、再发裸数字 ⇒ 应静默）没测** —— 这是那轮的遗留（证据 `docs\evidence\2026-09-17-u2-acceptance.md` §7.1）。

### 格 C：投票中 `@机器人007 作业书`（窗口不吃指令）

| 步 | 在哪 | 发什么 | 期望 |
|---|---|---|---|
| C1 | 群（**投票窗开着时**） | `@机器人007 作业书`（此时没有有效 `pending_file`） | **不被投票窗吃掉** ⇒ 回"没文件"那类提示（`FILE_MISSING` 的群版：把作业书发进这个群，再 @我一次） |
| C2 | 私聊 → 群（**投票窗开着时**） | 私聊投 PDF；再回群里 `@机器人007 作业书` | 群内**照旧**回"没文件"（跨会话的文件必须被拒 —— 这正是 §3.3 第 3 步的守卫） |
| C3 | 群（**投票窗开着时**） | 先群里投 PDF，再 `@机器人007 作业书` | **真的开始解析**（哪怕投票窗开着） |

- **判据（代码口径）**：`vote.accept()` 不命中的一律 `return _by_prefix(...)` ——「窗口开着时『拆解』『作业书』必须照常干活」（必修 1：不许有吃指令的死锁窗口）。
- **翻车长什么样**：C1 / C3 被静默、或回了一句跟投票有关的话 ⇒ 窗口吃指令（**必须在 U4 之前修掉**，否则 U4 再往状态机上叠一层会放大）。

### 格 D：按人归属的私聊指令（U2 ④-c 的覆盖面）

**为什么有这一格**：U2 真机验收 §2 只覆盖了 **2 条**私聊归属指令（`你想做哪一块`、`作业书`），其余没逐条跑（证据 `docs\evidence\2026-09-17-u2-acceptance.md` §7.2）。判据本身**已在 `21fd34a` 落地**（`app.py` 的 `handle()`：选不到数据域即回 `NEED_GROUP`）⇒ 这里补的是**覆盖面**，不是补代码。

| 步 | 在哪 | 发什么 | 期望 |
|---|---|---|---|
| D1 | 私聊（**未绑定者**，= `docs\ACCEPTANCE-U2.md` §0 的丙） | `报告` | `NEED_GROUP` + **不落盘、不起 pipeline** |
| D2 | 私聊（同一个人） | `完成 T3` | 同上 |
| D3 | 私聊（同一个人） | `我想提议：<一句话>` | 同上（**不许**再被转发进群 —— 那正是 U2 要修的旧行为） |
| D4 | 私聊（**已绑定**的组长） | `报告` | 拿得到（`报告` 另有它的门：**只认组长、只认群里**） |

- **判据（代码口径）**：判据是**按人**的（这个 open_id 在绑定表里查不到），不是「全局 `group_chat_id` 空」。**别拿「群里没人说过话」当对照** —— 只要群里有人发过话，旧口径就会把这个值写上。
- **顺序纪律（承接 `docs\ACCEPTANCE-U2.md` §2）**：**未绑定者先发、成员后发**，中间不插别的动作；反过来先发那条的静默会被「当时还没有可对照的活跃状态」解释掉 ⇒ **假阳性**。
- **逐条记原话**：`报告` 有自己的门（只认组长 / 只认群里），**实际回哪句以真机为准**，别照抄期望列 —— 与期望不符就把原话记下来（那是待收的口径）。
- **翻车长什么样**：D1–D3 里有任何一条**不**回 `NEED_GROUP` ⇒ 判据没覆盖到那条指令（回到 §7.4 的「覆盖**所有**依赖归属的私聊指令」）；D3 被转发进群 ⇒ U2 的按人判据没生效。

### 格 E：T01–T13 回归执行记录表（`§12.5` 的那一项）

**为什么设这一格**：U2 改了**数据地基**（§12.2 的口径）⇒ T01–T13 要**整体复跑**，且 §12.5 明写要交一张「回归执行记录表」（含 **6 处口径改写的理由**：T01 / T05 / T11 + 群内图片静默 / 私聊不反问 / 改派确认形态）。**没有这张表，§12.5 那一项就一直空着。**

| 用例 | 是否受影响 | 怎么跑 / 判据（照 §12.2） | 结果 |
|---|---|---|---|
| T01–T13 | 见 §12.2 逐条 | 见 §12.2 逐条 | **待填** |

- **每条要写全**：结果 **✅ / ❌** + **时间戳** + **关键日志行**（或落盘文件）—— 光写「通过」不算。
- **6 处口径改写的理由逐条写**（T01 / T05 / T11 + 群内图片静默 / 私聊不反问 / 改派确认形态）—— 这是 §12.5 点名的内容。
- **跑法**：T01–T13 里依赖任务的（T06 / T07 / T08 / T09 / T13）在 U4 落地后跑；**U4 未落地前先跑不依赖它的那几条**，剩下的留槽位。
- **回填落点**：结论回 `docs\ARCHITECTURE-UPGRADE.md` §12.5，并把这张表存档到 `docs\evidence\2026-09-18-rehearsal.md`。

---

## 2. §9.1 的 18 条话术样例（逐条触发）

记录要点：**原话 + 机器人回的原话**，逐条并存（这就是 §9.3 要的"18 条话术样例"）。

| # | 怎么触发 | 期望（升级后） | 本批 |
|---|---|---|---|
| 1 | 群里只发 `@机器人007`（不带文本） | 回**群内可用清单** + 一句"直接说要做什么就行" | ✅ |
| 2 | 群里 `@机器人007 拆解一下`（不认识的动作词 / 近似词） | **三级**：近似能对上 → 先确认"你是想做 X 吗"（**不执行**）；对不上 → 清单 + 最接近候选；真没有 → 直说做不到 | ✅ |
| 3 | 群 / 私聊发图片 | 群静默 / 私聊拒收（= 格 A） | ✅ |
| 4 | 群里 `@机器人007 作业书`（没投文件） | 回"没文件" + "把作业书发进这个群，再 @我一次" | ✅ |
| 5 | 投一个**坏文件**（后缀对、内容是乱码 / 空 PDF）再 `@机器人007 作业书` | 自然语言说解析失败 + 重试入口；**不甩异常类名** | ✅ |
| 6 | 投一份**没有评分点**的作业书 | **不再是失败**（走工作量链路 U3） | ⛔ **待落定**（U3 未落地） |
| 7 | 私聊 `作业书`（机器人还没认下任何群） | "我还没认下群…"（`NEED_GROUP`） | ⛔ **待落定**（U2 后） |
| 8 | 开志愿窗 → 谁也不填 | 兜底分配；**仍有剩余**才公示未分配清单 | ✅（要完整跑一轮 M4，建议放正式彩排，别占用本批时间） |
| 9 | 关掉 / 掐断 LLM（或断网）后 `@机器人007 作业书` | 分链路一句话 + "重试入口" | ✅ |
| 10 | 报告渲染失败（构造：把 `report.json` 写坏） | `REPORT_FAILED` 的人话版（去掉公文腔） | ✅ |
| 11 | 图片发送失败 | "文字版先到，稍后补图"（已是人话） | ✅ |
| 12 | 机器人掉线 | 无提示（**不在本轮范围**，记一笔即可） | ✅ |
| 13 | `@机器人007 改派 T9 @某人`（卡号不存在） | "这个编号我没找到" + 列出当前卡号；**不落盘** | ⛔ **待落定**（U4） |
| 14 | `@机器人007 改派 T3 @群里外人` | 非成员那句人话 + 指路"先 `登记`"（**不静默**） | ⛔ **待落定**（U4） |
| 15 | 私聊 `我不做了 T3`（T3 不是你的） | "T3 现在不在你名下"，**幂等** | ⛔ **待落定**（U4） |
| 16 | 两人同时私聊 `我想接 T3` | 后到者回"T3 刚被 <人名> 接走了" + 剩余可认领卡 | ⛔ **待落定**（U4） |
| 17 | `@机器人007 改派 T3 @自己`（无变化改派） | "T3 现在就在你名下，没改"；**不落盘、不公示、不静默** | ⛔ **待落定**（U4） |
| 18 | 群里 `@机器人007 我们要做的方向是：`（**空正文**） | 回提示（照 `PROPOSAL_EMPTY`） | ✅ |

**本批实做 = 1 / 2 / 3 / 4 / 5 / 9 / 10 / 11 / 12 / 18**（共 10 条）；**8 放正式彩排**；**6 / 7 / 13–17 留槽位**。

---

## 3. 待落定槽位（等 U2 / U4 落地后再补，别现在硬做）

| 槽位 | 依赖 | 落地后补什么 |
|---|---|---|
| 工作空间隔离 | **U2（已落地 `21fd34a`）** | **已跑**：多群 / 私聊归属的真机路径见 `docs\evidence\2026-09-17-u2-acceptance.md`（§1–§3）；本清单只补**按人覆盖面**（格 D） |
| 无评分点链路 | **U3** | §9.1 #6 的话术（"不再是失败"那句人话） |
| 任务变更（改派 / 退出 / 认领） | **U4** | §9.1 #13–#17 五条失败话术 + 演示第 13 步 |
| 话术盲评 | 无（本批就能做） | 抽 **10 条真实输出**，交 **3 名非项目成员**盲评"像人还是像机器"（§11.4 交付物②） |

---

## 4. 收工要交的东西

- [ ] 原样日志（剔除凭据行）→ `docs\evidence\2026-09-18-rehearsal.md`
- [ ] **五格结论**：A / B / C / D / E 每格 **✅ 或 ❌ + 时间戳 + 关键日志行**
- [ ] 格 D：**3 条按人指令**逐条记原话（未绑定 ⇒ `NEED_GROUP`；`报告` 另有两道门，实际回哪句以真机为准）
- [ ] 格 E：**T01–T13 回归执行记录表**（逐条结果 + 时间戳 + 关键日志行 + 6 处口径改写的理由）→ 回填材料 §12.5
- [ ] §9.1 的 10 条话术样例：**逐条 before / after 原话**
- [ ] 任何 ❌ **带回材料**（§4.6 / §9.1 / §12.3），别只在群里口头说一句
- [ ] 结论回填：§4.6 那三个 `- [ ]` 未覆盖格、§9.3 的两条未验证项
