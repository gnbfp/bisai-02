# 证据：架构材料 v1.5 逐条复核（审核员 · 第 3 轮）

> **时间**：2026-09-16（Asia/Shanghai）
> **复核对象**：`docs/ARCHITECTURE-UPGRADE.md` v1.5（标题 v1.5 / §13 "截至 2026-09-16 晨"）、`requirements-upgrade.md` v1.2、`docs/evidence/2026-09-15-*.md` 两份
> **工作区基线**：`bisai/main = 1e48358`；复核时 `git status` = `M docs/ARCHITECTURE-UPGRADE.md`、`M requirements-upgrade.md`、`?? docs/evidence/2026-09-15-workspace-migration-rehearsal.md`
> **方法**：不采信材料结论；全部回到代码 / 盘面 / 可复跑命令自己算一遍（命令见 §6）
> **判定权**：本文件只给"逐条判定 + 缺口"。**"通过"≠ 需求侧验收通过**，只表示"该条要求的证据已到位"。

## 1. 我亲手复核到的硬证据（可复跑）

| 项 | 我的实测输出 | 对应材料位置 |
|---|---|---|
| `replies.py` 常量数 | 顶层赋值 **62**（含 `__all__` 自身）/ 大写常量 **61** / `__all__` 条目 **61** / 悬空 0 / 未进 `__all__` 0 / **204 行** | §11.1 / §11.4 / §11.5 |
| `data\` 哈希 | 10 个已存在文件的 SHA256 前 16 位与证据文件 §1 表**逐行一致**（如 `rubric.json = 75742D5BB2AB7DAB`） | 证据文件 §1 |
| 覆盖率基线 | `rubric` 5 条（`normal` 4 / `ambiguous` 1）、`cards` 5、`members` 3、`preferences` 1、`assignments` 0；`coverage_loop()` = **4/4**，`missing=()` | §6.3③ / §3.4 |
| 单测基线 | `python -m pytest -q` = **381 passed** | （`AGENTS.md` 记 368，已过时） |
| `_save_assignments()` | `src/gateway/app.py:300-315` 确为**整份覆盖**、仅 `assignee` 未变时合并 `completed_at` | §8.4 的隐患判断成立 |
| `to_inbound()` | `src/gateway/events.py:156-163` 只取 `key/open_id/name`；全仓无 `mentioned_type`/`bot_mentioned`（`tools/probe_feishu.py:166` 已探到） | §4.5 缺口成立 |
| 锁端口 | `src/gateway/app.py:63` `INSTANCE_PORT = int(os.environ.get("GATEWAY_LOCK_PORT", 47653))`；`run-upgrade.ps1` 注入 `DATA_DIR=data-upgrade` + `GATEWAY_LOCK_PORT=47654` | §2.1 |
| 隔离卫生 | `.gitignore` 含 `data-upgrade/`、`.env`、`.env.*`；`git status` 无 `.env.upgrade` | §2.2 |

## 2. 开工前批（按 §15 已追认口径）

| 条目 | 判定 |
|---|---|
| #1 隔离四件套（只算已实测三样） | **通过** |
| #2 数据层：结构 / 迁移 / 回退 | **通过**（附 3 条开工后补） |
| #3 触发层：① 矩阵 + ③ 白名单定义 | **通过**；② ≥6 条穿透实测未验证（拆批建议见下） |
| #12 排期与冻结点 | **不通过** |

### #1 通过（凭据 / 数据 / 测试群）
- 凭据：独立应用 `cli_aa23…`、`tenant_access_token` 取到；`.env.upgrade` 5 个键（`FEISHU_APP_ID/SECRET`、`LLM_BASE_URL/MODEL/API_KEY`，无 `GATEWAY_LOCK_PORT`——由 `run-upgrade.ps1` 注入）。
- 数据：`data-upgrade\` 与 MVP `data\` 物理分离；`.gitignore` 已挡。
- 测试群：探针群 6/6 事件全 `sender_type=user`、无 app 回流 ⇒ MVP 不在该群。**逻辑我认可**：`im:message.group_msg.include_bot:read` 会把机器人自己的消息一起送回来，MVP 若在群里必然刷回话。
- 真机双进程并发已按 PM 追认移入开工后批 ⇒ 不挡 9/16 上午。
- 未过项：日志分文件（§2.3 仍在未验证清单，属"进程"条第 4 项，建议与 #1 剩余格一起归开工后）。

### #2 通过 + 3 条开工后补
- 结构：两段式（`index.json` + `workspaces\<chat_id>\`）；`app.lock` 落 `Path(data_dir)/app.lock`（`src/gateway/app.py:87`）与 §3.1 一致。
- 迁移 / 回退：证据文件 §2–§4 真跑过；迁移前后覆盖率逐项相等我**独立复算一致**（4/4）。
- 13 个候选名 / 10 个已存在已对齐（`assignments.json` / `proposals.json` / `reminders.json` 迁移时不存在，按"存在即复制"）。
- **补丁 1**：`tools\` 里**没有迁移工具**（只有 `probe_feishu.py`）⇒ §3.3 的工具幂等与回退守卫**未实现也未验证**，演练是手工做的。建议入库 `tools/migrate_workspace.py`（含 `Resolve-Path` 守卫）并落 `MANIFEST.<ts>.txt`（pre/post 哈希 + 命令），否则"前后完全一致 = True"不可复现。
- **补丁 2**：§3.3 第 3 步"读 `state.group_chat_id` 判定归属"在多群残留上会**静默串数据**：现网 `data\state.json` 里 `pending_file.chat_id = oc_6feb8f64…` 而 `group_chat_id = oc_33225a17…`，同一份数据里已有两个群，演练素材本身就带这个形态。建议加守卫：**发现 >1 个不同 chat_id 即报告 / 中止**，或明确"只迁群内作用域段 + 显式丢弃跨群残留并列清单"。
- **补丁 3**：`data-upgrade\_rehearsal\workspaces\` 空目录仍在（回退删了副本与 `index.json`，"两者不存在"严格成立）；§4 引用的 `Resolve-Path` 守卫没有落盘命令 ⇒ 与补丁 1 合并解决。

### #3 通过（限 ① ③）+ 拆批建议
- ① 矩阵：5 状态 × 9 列（`docs/ARCHITECTURE-UPGRADE.md:178-184`）**无空格**。逐格比对代码：计票限开窗群 + 花名册（`vote.py:169` / `:195-205`）、冻结后数字静默（`vote.py:179-185`）、`封盘` 限组长（`preference.py:176` + `_seal`）、志愿窗口群内裸数字不算志愿（`preference.py:184-185`）⇒ 那一格已从"待实测确认"改为**代码判定 + 门禁静默**，正确。
- ③ 白名单定义：词表 / TTL / 谁关 / 超时齐全（`vote.py:48` 10 分钟、`preference.py:46` 5 小时、`register._expired` 有 TTL；§4.4）。
- ② ≥6 条穿透实测：**未验证**（`:245` 自己写"开工后才有代码可测"）。
- **拆批建议**：§15（`:650`）把 #3 整体放"开工前必须通过"，与 `:245` 冲突。按 #1 的同款做法拆成"① 矩阵 + ③ 定义（开工前）"与"② 实测（开工后）"，否则 9/16 上午 #3 永远判不过。

### #12 不通过（唯一原因）
- `docs/ARCHITECTURE-UPGRADE.md:599-604` 只有：9/16 上午定稿、复审、9/16 下午 U1+U5、9/17 U2+U3、9/18 彩排、9/19 提交；**U4（P1 最小集）与 U6（P0 尾部）没有时间槽**，而 §10 演示第 7 / 13 步（`:486` / `:492`）与 §5.1 的 10–12 条都依赖它们。
- `requirements-upgrade.md` §5 的 P1 行（`:146`）与 P0 尾部（U6）在 §12.4 无对应行。
- 按 `:600` 你自己的规则（不通过 ⇒ 先做不依赖被卡节的部分）⇒ **U1+U5 可开工**；U4/U6 待补两行后解冻。
- 复审窗口（2 小时）与"只交 diff"两条我认可，已计入判定口径。

## 3. 开工后批（逐条）

| 条目 | 判定 | 依据 / 缺口 |
|---|---|---|
| #4 路由表 8→12 | 设计通过 · 实测未验证 | 12 行表齐（`:253-266`）；"互不为前缀"我验了 `我们要做的方向是：X`.startswith("方向")=False（`:270`）、全半角冒号 `router.py:41`；12 条各一条实测记录未验证 |
| #5 双链路零污染 | ①③④ 通过 · ② 未验证 | ① `coverage.py:86-90` 分母 = `normal` ✓；③ 基线 4/4 我独立复算一致，改造前后两组数字未验证；④ PM 已裁 + `:307-310` 判定式写死 ⇒ **口径唯一**（v1.5 已消灭"两把尺子"） |
| #6 私聊归属 | 按新判据通过 · 实测未验证 | PM 已正式改写判据（`requirements-upgrade.md:204`：不反问 + 回话点明来源）；§7.4 有双群实测槽（`:385`） |
| #7 变更机制 | **部分通过** | 台账字段 / 回流池 / `allocate()` 语义裁决 ✓（隐患为真，见 §1）；缺**跨文件原子性**（`changes.json` 与 `assignments.json` 两次独立 `mutate`）与**认领先到先得**判定 |
| #8 失败路径 | **不通过** | 表 12 行 + 两条纪律 ✓；`:451` 第 1 条**现状列写错**（真实是 `router.py:102-104` 静默），且**缺 U4/U6 分支**（`改派 T99`、@ 非花名册、改派给自己、认领已被认领、`我不做了` 名下无卡） |
| #9 演示保底 | **不通过** | 保底表 + "彩排留盘"硬规矩 ✓；`:508` 压缩方案不闭合：上表累计 260s（4:20，我复算一致），5–7 合并 + 11 预置 ≈ 省 25s ⇒ 235s > 210s |
| #10 话术层 | 通过（除计数） | 四层规则 / 两个例外 / 三交付物 ✓；`replies.py` 是 **61** 条不是 63（3 处待改） |
| #11 口径一致性 | **不通过** | §12.1 与 D-69～D-76 逐条对得上 ✓；但真源"双向确认"3 处残留（含 §6 内 L1 与 L14 打架）；`私聊 4 vs 7` 两个数并存 |

## 4. v1.6 待修清单（按优先级）

**P0（会误导复审 / 与已裁口径打架）**

1. `docs/ARCHITECTURE-UPGRADE.md:512` / `:537` / `:543`：`63 条` → **61 条**（证据见 §1）。
2. `requirements-upgrade.md:157`（**L1** "涉及方双向确认"）与同节 `:141` **L14** 直接矛盾 → L1 改为 A 方案口径或标注"由 L14 修订"。
3. `requirements-upgrade.md:146`（P1 表）、`:205`（§8 第 7 条）：同样残留"双向确认" → 改 A 方案口径。
4. `docs/ARCHITECTURE-UPGRADE.md:451`：第 1 条**现状列**改成"纯 @ / 空文本 → 静默（`router.py:102-104`）"，并把"从静默改为回清单"登记为行为变化。
5. `docs/ARCHITECTURE-UPGRADE.md:247` 与 `:183` 打架（前者要实测、后者已定"无需实测"）→ 删 `:247` 该条。
6. `docs/ARCHITECTURE-UPGRADE.md:286`："私聊**专属** 4" 与 L14 / §11.3 / §12.3#15 的"私聊 7"并存 → 统一写 `群 8 / 私聊 7 / 路由 12`，"专属 4"只作括注。

**P1（审核 7 / 8 / 9 / 12 的判据缺口）**

7. §9.1 补 U4/U6 失败分支 ≥5 条（含话术）。
8. §8 补：台账与卡的**原子性边界**（同一把锁内"先判后写"，或单文件承载）、**认领竞态**（仅 `assignee==""` 可写）。
9. §12.4 补 U4/U6 时间槽（建议：9/17 上午 U4 最小集 = 3 条指令 + 台账；U6 随 U1 一起落），并在 §13 同步。
10. §10.3 补**闭合的压缩表**（给出目标 210s 的分步时长，而不是"可合并"）。
11. U2 两条需求无落点：`requirements-upgrade.md:57` 防呆三动作（清空 / 重置 / 改名，二次确认 + 记录执行人）、`:55` "可读名出现在报告抬头" → 要么设计落点，要么登记为 P2 并请 PM 认。

**P2（可复核性与整洁）**

12. `docs/ARCHITECTURE-UPGRADE.md:664` 的"62 个处置条目 / 64 处文本命中"**不可复核** → 落 `docs/evidence/2026-09-16-v1.5-修订清单.md`（A1–A48 / R1–R13 / R8b 全列），或删掉该半句。
13. `:664` 拆成 ①–⑦ 分条 bullet（600+ 字单行段落无法逐条对位）。
14. `:595` 与 `requirements-upgrade.md:103` 的"**误判**"措辞 → 改"材料缺落点（v1.5 已改为引用）"；真源里不留对审核结论的定性。
15. `requirements-upgrade.md` 存回 LF（`.gitattributes` = `* text=auto eol=lf`，现为 CRLF，git 每次 warning；不污染 diff，已核 `--numstat`）。
16. `docs/ARCHITECTURE-UPGRADE.md:439` §8.5 "二次确认的形态（**若选 B 方案**）"已成死引用（B 已裁否）→ 标留档或删。
17. §13 第 12 行（`:629`）"v1.4 增补 16 条" → v1.5。

## 5. 未验证总表（开工后随实现补）

- §2.3：两真实进程同时在线 ≥4 条、日志分文件
- §3.5：≥2 工作空间并发写、迁移中断、磁盘清理
- §4.6：≥6 条穿透实测、`bot_mentioned` 识别率
- §5.4：12 条指令实测、`我们要做的方向是：X` 回执样例、归并阈值
- §6.3：② 无评分点分子/分母、③ 改造前后两组数字
- §7.4：双群用户私聊实测、绑定并发
- §8.6：三类变更流转记录、改派后重跑结算回归
- §9.3：12 条话术样例、近似匹配阈值
- §10.3：彩排记录、零响应录屏
- §11.5：61 条对照表、10 条盲评
- §12.5：回归执行记录表；口径改写理由现为 **6 处**，而 `requirements-upgrade.md` §3 仍写"3 处"，需同步

## 6. 复核命令附录（可复跑）

```powershell
# 1) replies.py 常量数（62 顶层赋值 / 61 常量）
python -c "import ast,re;s=open(r'src/gateway/replies.py',encoding='utf-8').read();t=ast.parse(s);a=[n.targets[0].id for n in t.body if isinstance(n,ast.Assign) and isinstance(n.targets[0],ast.Name)];print(len(a),len([x for x in a if re.fullmatch('[A-Z][A-Z0-9_]*',x)]))"
# 2) 覆盖率基线（4/4，missing=()）
python -c "import sys;sys.path.insert(0,r'D:\AI创新创业大赛');from src.storage import JsonStore;from src.intelligence.coverage import coverage_loop;s=JsonStore(r'D:\AI创新创业大赛\data');r=s.load_rubric();c=coverage_loop(s.load_cards(),r);print(len(c.covered),'/',len(c.eligible),c.missing)"
# 3) 单测
python -m pytest -q
# 4) 盘面哈希（对照证据文件 §1）
Get-FileHash -Algorithm SHA256 data\*.json,data\*.png,data\*.md | ForEach-Object { "$($_.Path.Split('\')[-1]) $($_.Hash.Substring(0,16))" }
# 5) 变更面
git diff --numstat -- docs requirements-upgrade.md
```

> **结论（审核员）**：开工前批 **#1 通过 / #2 通过 / #3 通过（限 ① ③）/ #12 不通过**；按 `:600` 的规则 U1+U5 可开工，U4/U6 待 §12.4 补行后解冻。开工后批 #8 / #9 / #11 判不通过，缺口见 §4。**我不替需求侧判"通过"**；本文件即"修订后只交 diff"复审的**基线记录**（绑定 v1.5 内容）。