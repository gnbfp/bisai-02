# 证据：架构材料 v1.8 **全量重判** + v1.9 增量（审核员 · 第 5 轮）

> **重判基线**：提交 **`3efe36f`**（`docs/ARCHITECTURE-UPGRADE.md` v1.8 = 751 行；`requirements-upgrade.md` v1.5）。重判时工作区干净。
> **判据**：`requirements-upgrade.md` §8 的 12 条 + 材料 §0 + §15 的两批证据口径（PM 2026-09-15 追认）。
> **方法**：不采信材料结论。代码位置逐个回盘、数字自己数、能跑的跑（`pytest` / 只读探针）；跑不出来的显式标"未验证"。
> **判定权边界**：本文件只给"该条要求的证据是否到位"。**"通过"≠ 需求侧验收通过**；我不替 PM 判"开工"。
> **稳定性**：`git diff --numstat 1e48358 3efe36f -- src tests` = 空 ⇒ §1–§6 的所有代码位置可按现盘复核。

## 1. §0 完整性检查（按实质核；勾选动作归需求侧）
| # | §0 要求 | 我的核查 |
|---|---|---|
| 1 | 每节都填了证据（实测 / 数字 / 代码位置 / 输出样例） | 成立：§2.2 实测表、§3.4 演练表、§4.3 矩阵、§4.4 词表、§5.1 路由表、§6.3 证据槽、§12.1 对照表 |
| 2 | 只有设计意图的已显式标"未验证" | 成立：§2.3 / §3.5 / §4.6 / §5.4 / §6.3②③ / §7.4 / §8.6 / §9.3 / §10.3 / §11.5 / §12.5 |
| 3 | 与第 1 层无冲突 | T01 / T05 / T11 的改写已在 REQ §3 表内逐条 + §12.2 登记；D-30 与 `data-upgrade\` 的张力已在 §2.4 + §12.3 第 1 条登记 |
| — | §0 自身三个复选框 | **仍是空的**（归 PM）—— 我只核实质，不代打勾 |

## 2. 开工前批（§15 口径：本次必须判）

### #1 双机器人隔离四件套 → **仍通过**（三样实测；第 4 格归开工后）
- 凭据：独立应用 `cli_aa23…` + token 7130s；事件订阅 6/6；文件下载 3/3（129583 字节）；数据分根（`.env.upgrade` 不进 `git status`）。
- 进程：同端口 `exit=3`、换端口可并存（`GATEWAY_LOCK_PORT` 47653 / 47654）。
- 独立测试群是**可判据**而不是声明：探针期 6 条事件全 `sender_type=user`、无 app 回流 —— 而 `im:message.group_msg.include_bot:read` 本会把机器人自己的消息一起送回来。
- 未验证（照 §15 归开工后）：两真实进程同时在线 ≥4 条、日志分文件。

### #2 数据层（落盘结构 / 迁移 / 回退）→ **仍通过**
- 演练证据在 `docs/evidence/2026-09-15-workspace-migration-rehearsal.md`；覆盖率迁移前后逐项相等（4/4、`missing=()`）；回退干净；MVP `data\` 哈希前后一致；"0→0 不算证据"已标注。
- §3.6 四条事实我照盘复现（`state.json` 并存 `oc_33225a17…` / `oc_6feb8f64…`，`.bak` 是第三个 `oc_c02a3ec4…`）；它引的 5 处代码（`app.py:279`、`router.py:49` / `345-358` / `391` / `396-404`）逐处对得上。
- §3.3 第 3 步"不一致就不迁 `pending_file`"是**收窄**，不动已有证据。

### #3 触发层（限 ① ③；② 归开工后）→ **仍通过**
- ① 矩阵 5 态 × 8 列无空格；含"无 @ 文件消息""图片 / 其他资源""@ 文件消息（不可复现 · D-42）"三列。
- ③ 白名单给了词表 + 生效条件 + 代码位置，并有"窗口关闭与超时"三行（投票 10 分钟 / 志愿 5 小时 / 登记 TTL）。
- ② ≥6 条穿透实测要代码，照 §15 归开工后。

### #12 排期与冻结点 → 在 `3efe36f` 判"需条件"；**在 `28f898d` 已关（见 §7.4）**
- 已好：REQ §5 时间盒已同步"9/17 = U2 + U3 + **U4 最小集**"；冻结点一致；P2 砍掉开关在（交互卡片 → 改名 → 拆卡/并卡 → 多份作业 → 匿名查询）。
- 当时未过：§12.4 有"**9/17 上午（U2 之前）补丁批**"这一档（迁移工具 + 多会话守卫 + 手册），REQ §5 时间盒里没有对应项，而 §12.4 自己的标题写着"（与 `requirements-upgrade.md` §5 一致）"。
- 当时给的条件（一行文字，二选一）：① §5 时间盒补这一档；② §12.4 该行改写为"属 U2 交付内容、随 U2 一并验收"。
- 附建议（非条件）：现有兜底只覆盖 U4，补丁批没有降级序。

## 3. 开工后批（8 条）
| # | 条目 | 本次判定 | 依据 / 缺口 |
|---|---|---|---|
| 4 | 路由表 8 → 12 登记 + 优先级 | **通过（登记）** | §5.1 表 12 行逐行数过；新前缀不与既有互撞（`我们要做的方向是：` 不以 `方向` 开头）；群 7 / 私聊 5 派生值我按表逐行派过 |
| 5 | 双链路覆盖率零污染 | ①④ 通过 / ②③ 未验证 | ① `coverage.py:86-90`（分母 `status=="normal"`）+ `app.py:437-442` 出口（0 分母会打成 `覆盖率 0/0`）；④ PM 已裁；②③ 要有代码 |
| 6 | 私聊归属 | **未验证** | §7.2 规则 + 代价写清；缺一条"双群用户私聊"实测 |
| 7 | 变更机制 | 部分可判 / 流转未验证 | 台账字段表 + 写序 + 认领竞态（`mutate_many` 只保证单文件原子，`storage.py:125` 我读过）；`_save_assignments()` 确为唯一无读-改-写保护处（`app.py:300-315`） |
| 8 | 失败路径 | 设计可判 / 话术样例未验证 | §9.1 18 行、编号连续；§9.2 两条纪律 |
| 9 | 演示可回归 | 设计可判 / 彩排未验证 | 13 步 + 3 条链路保底；算式 260 − 60 = 200s ≤ 210s 我复算过 |
| 10 | 话术层 | 设计可判 / 对照表与盲评未验证 | 四层 + 机器可判据；`replies.py` 61 条 / 204 行（v1.8 时点）我数过 |
| 11 | 口径一致性 | **通过** | §12.1 八行对 D-69～D-76（`requirements.md` §9 的 507–514 行我核过）；四行指针模板一致；映射 D-45→L12 / D-70→L11 / D-72→L14 / D-76→L14 与 REQ §6 L11–L14 对得上；"口径改写 6 处"六项逐项对得上 |

## 4. 本轮发现（重判时点 = `3efe36f`；"v1.9 现状"列是我读 `28f898d` 之后补的）
| 级别 | 问题（重判时） | v1.9 现状 |
|---|---|---|
| **P1** | **版本归属错标**：正文把 v1.7 的改动写成 v1.6（§3.3 第 3 步"（v1.6，依据 §3.6 诊断）"、§13 第 3 行"v1.6 补 §3.6…"、§13 两条补丁行"（v1.6 登记）"）。`git show a1dec84:…` 为证：v1.6 版**无** `### 3.6`、**无**"多会话残留"、**无**"补丁批"，也没有 `migrate_workspace` / `OPERATIONS-U2` / `maintenance.log` 这三个词 | **已订正（v1.9 变更记录第 ⑧ 条）→ v1.7**；现盘 `rg` 那四处已无命中 |
| P2 | §12.4 复审行的版本清单过期（"v1.3 / v1.4 / v1.5 三次"） | **已订正**（现为"v1.3 / v1.4 / v1.5 / v1.6 / v1.7 / v1.8 六次（+ 本轮 v1.9）"） |
| P2 | 两处代码位置指到**类定义行**而非校验处：§6.2 / §6.4 的 `models.py:143`、§8.3 的 `models.py:198` | **仍在**（430 行附近那三处没动）。真正抛错在 `models.py:161-167`（`if not self.rubric_refs: raise SchemaError`）。建议写方法名 + 方法内行号 |
| — | 我自己在第 4 轮写的"`src/gateway/app.py:430` = `_save_assignments`"确实错了 | 430 是 `_run_decompose`；正确是 `:300-315`（docstring 写着"整份分配结果一次性覆盖"）。这条更正成立 |

## 5. 未验证总表（开工后随实现补）
真机双进程并发 / 日志分文件 / 迁移中断现场 / 并发写 `index.json` / 磁盘与清理策略 / 矩阵 ② ≥6 条穿透 / 路由 12 条实测（U4 后）/ 覆盖率 ②③ / 双群用户私聊 / 三条变更流转记录 / 崩溃重放 / 认领竞态 / 18 条话术样例 / 对照表 + 盲评 / 彩排计时 / 回归执行记录表。

## 6. 复核命令附录
```powershell
# 基线稳定性（应输出空）
git -C "D:\AI创新创业大赛" diff --numstat 1e48358 3efe36f -- src tests
# 单测
cd "D:\AI创新创业大赛"; python -m pytest -q
# 版本归属取证：v1.6 版里没有 §3.6
git -C "D:\AI创新创业大赛" show a1dec84:docs/ARCHITECTURE-UPGRADE.md | Select-String "### 3.6"
```

---

## 7. 对 v1.9 的增量（审核员再核 · 2026-09-16 下午）

> **本节基线 = 已落库的 v1.9**：`bed2574`（U1 + U5 + U6 代码）+ `28f898d`（材料 v1.9 + 真源 v1.6）。读的时候工作区干净 ⇒ **工作区 = 提交内容**，本节所有数字都锚在提交上、可复跑。
> **本节只判三样**（外加一条我已关的条件）。`src/gateway/replies.py` 工作区 MD5 = `52D58B7516AFA0907D4FF2E90E96F6A7`。

### 7.1 §11 计数口径（70 / 72）→ **已关：数字与名词都对了**
- 我数的（HEAD 现盘）：`__all__` **72** 项、模块级常量 **70** 个、非常量导出 **2** 个（`Command` / `command_list`）、**0** 个常量漏出 `__all__`、文件 **276** 行。
- 重判时材料写的是"`__all__` 里 **72 条常量**"（把**导出数**说成了**常量数**）；**v1.9 已改为"`__all__` 里 **72 项**（**文案常量 70** + `Command` / `command_list`）"** —— 与我实测逐项一致。⇒ 这条**关掉**。

### 7.2 §4.5「登记状态机前置」→ **行为订正成立；但"用单测钉住"这半句仍不成立**（P1，未改）
- **订正本身对**，我用只读探针在真代码上验过：同一条表单（`组长：@_user_1` / `组员：@_user_2`，`bot_mentioned=False`）—— `may_speak()` 判 **False**（即门禁会拦），但 `route()` 仍把它交给登记状态机并**完成了登记**（回执"花名册已保存…共 2 人"）；`bot_mentioned=True` 对照组结果相同。
- 代码顺序（`src/gateway/router.py`）：`sender_type=app` → 资源分支 → 剥 @段 → **`awaiting=="register"`**（`register.classify()`）→ **`may_speak()` 门禁** → 投票 / 志愿 → 前缀 → 兜底。与 §4.5 写的"实际顺序"逐段一致。
- **后半句仍不成立**：钉住这个顺序，需要"**没 @ 机器人 + `awaiting=="register"`**"的用例。我查遍 `tests/`：`bot_mentioned=False` 只出现在 `tests/test_gateway_router.py` 的 `:407` / `:539` / `:540` / `:712`（`_nobody()` 助手）/ `:727`，**全是门禁自身的用例**；register 窗口的用例（`:335` / `:384` / `:390` / `:429` / `:437` / `:476` / `:483` / `:491` / `:496` / `:508` / `:514`）都走 `_inbound()` 默认值，而默认值是 `data.setdefault("bot_mentioned", data["chat_type"] != "p2p")` ⇒ 群聊默认 **True**。**把门禁挪回状态机之前，这些用例一个都不会红。**
- 建议补一条用例（约 5 行）：`_nobody("登记\n组长：@_user_1\n组员：@_user_2", mentions=…)` + `awaiting=="register"`，断言 `stage` 推进到 `confirm` —— 它的作用就是把 §4.5 那半句话变成真的。
- 另一处（P2，未改）：§4.5 的**编号清单第 5→6 步仍写着旧的错顺序**（门禁紧接"剥 @段"），订正只在下文散文里。本材料已经因为"照字面实现"翻过三次车（门禁位置 / 私聊豁免 / 矩阵格），建议直接把清单改成 `剥 @段 → 登记状态机 → @ 门禁 → 投票 / 志愿状态机 → 前缀 → 兜底`，或把旧清单标成"v1.3 原文（已被下文订正取代）"。

### 7.3 证据档位能否升为「单测已过、真机未测」
- **前提数字我先自己复跑**（HEAD 现盘）：`python -m pytest -q` = **417 passed**（我跑了两遍：13.10s / 14.43s）。净增 **36** 条（`def test_` 逐文件 diff，`3efe36f` → HEAD）：`tests/test_gateway_router.py` **+13**、`tests/test_vote.py` **+12**、`tests/test_gateway_events.py` **+4**、新增 `tests/test_replies.py` **+7**。⇒ **"417 / 381 / +36" 成立**。
- **但材料的分类对不上总额**：§13 与变更记录写"门禁 11 / U6 12 / @ 识别 4 / 话术与清单 7"= **34**，比实测的 36 少 2（router 文件净增是 **13**，其中 4 条是资源分支而非门禁穿透）。建议直接写**分文件的四个实测数**（router / vote / events / replies），别用自制分类 —— "报数只认脚本输出"这条规矩同样适用于单测数。

| # | 能否升级 | 理由 |
|---|---|---|
| 4 路由表 | **可升（部分）** | 已落地 9 条（第 1–9；10–12 属 U4）；`test_replies.py::test_every_listed_prefix_is_actually_routed` 是防漂移守卫（表里每条前缀必须真被 router 接住，掉兜底就红）；派生值"群 7 / 私聊 5"被 `test_command_list_is_derived_from_the_scope_table` 钉住。**但"12 条实测"仍归 9/18** |
| 6 私聊归属 | **不可升** | U2 未实现：`rg "user_last_group\|index\.json\|workspaces" src` = **0 命中**。现有 `NEED_GROUP` 用例（`test_gateway_app.py:671` / `test_preference.py:385`）钉的是**现状"未入群"分支**，不是单值映射 |
| 8 失败路径 | **可升（限已实现子集）** | §9.1 第 1–12 + 第 18 条（U6）涉及的路径有单测：空文本静默、@ 后清单、群内无 @ 静默、私聊不套门禁、登记后无意义 → 清单、兜底只在被 @ 时回。**第 13 / 15 / 16 条属 U4、未实现 ⇒ 那三行仍"未验证"** |
| 9 演示保底 | **不可升** | 保底靠"预置产物 + 彩排"，没有单测可钉；§10.3 三项（彩排记录 / "什么都没发生"要录屏 / 秒表计时）本质是 9/18 现场项 |
| 11 口径一致性 | **可升（条数派生部分）** | `tests/test_replies.py` 三条钉住派生：清单由 `COMMANDS` 按作用域数出、两份清单不同（群有「方向」、私聊有「我想提议：」）、条数不写死；台账指针是文档级（已过）。**"口径改写 6 处"仍要执行记录表（9/18）** |

### 7.4 #12 的条件 → **已关（我关掉它）**
- v1.9 **两条路都落了**：① `requirements-upgrade.md` §5 时间盒加了"**9/17 上午（U2 之前）= 补丁批**（迁移工具 + 手册 + 多会话守卫，随 U2 一并验收）"；② `docs/ARCHITECTURE-UPGRADE.md` §12.4 该行补了"**属 U2 交付内容、随 U2 一并验收 —— 审核 #12 唯一未过的条件**"。
- 冻结点（9/17 收工）、P2 砍掉开关、U4 兜底三项均未受影响。⇒ **#12 判"通过"**；开工前批四条（#1 / #2 / #3 的 ①③ / #12）在 v1.9 上**全部通过**。
- 一条非条件的提醒（不影响判定）：补丁批仍没有降级序，若 9/17 上午挤掉，"只保换人"的兜底覆盖不到它 —— 建议顺手写一句"补丁批挤掉时砍什么"。

### 7.5 顺手记一笔（正面）
- 状态行不再写死版本号，改成"**版本号不写在这里，一律以文末变更记录最后一条为准**" —— 这是对"标题 / 状态行版本号漂"这个老病的**结构性**修法，比每次手工同步更可靠。我认这个改法。

## 8. 对 v1.13 / v1.14 的增量（审核员三核 · 2026-09-16 傍晚）

> **基线锚定**：本节判的是 **`6cc851b`**（材料 v1.13 + U2 离线手册 + `tools\count_replies.py`）。判定做完之后又落了 **三笔小改** —— `249e066`（v1.14）/ `92103a9`（冒烟证据 §1 补日志）/ `f390706`（订正 v1.12 ⑨ 的过期说法）⇒ **本节结论以这三笔为限**：三笔没碰的部分我没重判；唯一跨到 `92103a9` 上补验的是「§4.6 引用悬空」（见 §8.4，已关）。
> 工作区干净（`git status --short` 为空）⇒ 本节数字都锚在提交上、可复跑。远端基线 = `f390706`（我自验）。
> 本节判四样：**§11 计数 / §4.5 登记前置 / §4.6 档位 / #4·#6·#8·#9·#11 档位**。

### 8.1 §11 计数 → **通过（复跑逐字一致）**
- 我自己跑 `python tools\count_replies.py`，与材料 §11.1 的输出快照**逐字符一致**：

  ```
  all   = 78   (= len(__all__))
  text  = 73   (字符串字面量 69 + 非字面量模板 4 ['COMMANDS', 'COMMAND_LIST_DM', 'COMMAND_LIST_TEXT', 'REGISTER_FORM_BAD'])
  sym   = 5    ['Command', 'command_list', 'file_missing', 'needs_rubric', 'parse_failed']
  lines = 326
  ```

- 独立再算一遍（不靠脚本）：`__all__` **78** / UPPER 常量 **73** / 非常量导出 **5**（1 class + 4 function）/ **326** 行 / CR=0 / 无 BOM / 16219 bytes / md5 `F687BAD15A5CA3FEA15074ABAD0F18BB`；`78 == 73 + 5` 成立，常量漏出 `__all__` = **0**；`len(COMMANDS)` = **9**（材料「路由不是作用域」那句注记对）。
- 材料 §11.1 已改成「**复跑命令 + 最近一次输出**」，写死的 72 / 70 / 276 只作「漂过一轮」的历史出现；脚本 `tools\count_replies.py` 已在库（纯 AST、不进运行时）。⇒ **关掉**。

### 8.2 §4.5「登记状态机前置」→ **行为与钉子都成立；但清单里 5 处「现状行号」全错**
- **§7.2 记的 P2（编号清单仍是旧顺序）已改对**：`:249-256` 现在是 `app → file → image → 其他资源 → 剥 @段 → **登记状态机** → **@ 门禁** → 投票/志愿 → 前缀 → 兜底`；`:262` 补了私聊豁免（`chat_type == "p2p"` 跳过第 7 步），`:258` 附订正说明。
- **P1（「用单测钉住」那半句）已关**：`tests/test_gateway_router.py::test_the_register_form_is_taken_before_the_mention_gate` 已随 `af68f6f` 入库。
- **变异验证我自己复现**（不采信自报）：把 `may_speak()` 挪到 register 分支之前 ⇒ 该用例 **1 failed**（`TypeError: 'NoneType' object is not subscriptable`），未变异 **1 passed**；两次都确认导入的是**各自副本**的 `router.py`（先删 `__pycache__`，排除「读的是别处旧字节码」这种假象）。⇒ 这条用例确实钉得住顺序，不是摆设。
- **新发现（P2，未改）**：清单里「（现状行号）」**5 处与代码不符** —— `router.py:89-90`（app）实际 **`:131-132`**；`:92-93`（file）实际 **`:138-139`**；`:97-98`（image）实际 **`:142-145`**；`:99-100`（其他资源）实际 **`:146-147`**；`:268` 的 `_by_prefix()` 兜底 `:247` 实际 **`:322-323`**。偏移 42–76 行（U1 落地后 `router.py` 变长）。建议照「变更记录」的规矩办：**只写锚文本，行号以 diff 为准**。

### 8.3 §4.6 档位 → **通过（口径诚实）**
- `:273` 判据①真机已验，且**自己写明「那次两条判据同时配着、日志分不出哪条命中」**；`:274` 判据②**只有单测、真机造不出**（平台正常都带 `mentioned_type`），明确「保留为兜底，**不计入真机覆盖**」；`:272` 三格未覆盖列名。⇒ 标 `[x]` / `[ ]` 与证据相符，没有拿设计意图冒充证据。
- 一处口径小差（不影响判定）：`:271` 写「**7 条**跑通」，而证据文件 §2 是 **6 行**（①–⑥）—— 7 是把 ⑥ 拆成「私聊回执 + 私聊执行」两格。不是错，但两个数对不上容易被复审挑；建议在 `:271` 注明「= 证据 §2 的 6 行，⑥ 计两格」。

### 8.4 「§4.6 引用悬空」→ **已关（`92103a9` 修，我在它之上复验）**
- `6cc851b` 上我报的：材料 `:271` / `:273` 写「原样日志见 `docs/evidence/2026-09-16-u1-smoke-real-device.md`」，而该文件 §1 是 **`{log}` 占位符** ⇒ 引用悬空。
- `92103a9` 后我自己复验：
  - `rg "\{log\}"` = **0 命中**；§1 现在贴的是 `%TEMP%\u1_smoke.log` 的**第 9–23 行逐字原文（15 行）**（自述该文件 3332 bytes / 24 行、sha256 前 16 = `7EB8633B39C147DB`）。内容是**真日志**：真实 `om_…` message_id / `oc_…` chat_id / 时间戳，且能看到改后的「把作业书发进群，再 **@我** 说「作业书」」。
  - 凭据面：`rg "cli_[A-Za-z0-9]+|app_secret|Bearer|tenant_access_token|FEISHU_APP_SECRET"` = **0 命中**；文件头自述剔除了「启动横幅 / 两条 Python warning / 带 `access_key` / `ticket` 的 `[Lark] connected` 行 / 退出行」。**自述我当口供、不当证据**，但上面的独立 grep 通过 ⇒ **可接受**。
  - 文件属性：6756 bytes / 69 LF / CR=0 / 无 BOM。
  ⇒ **关掉。**

### 8.5 档位四问（能不能升为「单测已过、真机未测」）
| # | 能否升级 | 理由（我自己核的） |
|---|---|---|
| 4 路由表 | **可升（部分）** | 12 条已登记（材料 §5.1 + `requirements-upgrade.md` §8 第 4 条：终值 12 / 当前派生 9）；防漂移守卫 `tests/test_replies.py::test_every_listed_prefix_is_actually_routed` 在。缺「逐条实测记录」（§12.5，9/18） |
| 6 私聊归属 | **不可升** | `rg "user_last_group\|workspaces\|index\.json" src` = **0 命中**（只有 `tools/migrate_workspace.py`）⇒ U2 未落地，§7.4 三条全 `[ ]`。附注：其中「未入群私聊 → `NEED_GROUP`」这一格其实已有单测（`tests/test_gateway_app.py:671` / `tests/test_preference.py:385`），**该子项可先升一档** |
| 8 失败路径 | **可升（部分）** | §9.1 的**机械判据**已落单测（`tests/test_replies.py`）；18 条逐条样例 + 3 人盲评仍归 9/18（§9.3） |
| 9 演示保底 | **不可升** | 无单测可依（彩排 / 录屏 / 秒表）。但 §10.3 的**算式我复核过**：30+10+5+5+5+5 = 60，260 − 60 = **200s ≤ 210s** ✅；且 `:561` 自己写明「待验证的是实际计时，不是这个算式」 |
| 11 口径一致性 | **可升（部分）** | 6 处清单在 §12.5 列全（T01 / T05 / T11 + 群内图片静默 + 私聊不反问 + 改派确认形态）；`requirements.md` §9（`:428` 起）**4 条 D- 指针在位**（`:482` D-45→L12 / `:508` D-70→L11 / `:510` D-72→L14 / `:514` D-76→L14）；计数口径有脚本兜。缺「回归执行记录表」（9/18） |

- 旁证（我自己跑的）：`python -m pytest -q` = **438 passed**（13.98s），与 §13 自报的 **438** 一致。

### 8.6 更正我自己的两条
- **`assignment.json` 不是笔误**（上一轮我提的疑问**撤回**）：`src/storage.py:52` = `ASSIGNMENT`（**作业元信息**）、`:56` = `ASSIGNMENTS`（**分配记录**），两文件并存。
- **「带 `http.proxy` 也验不通 `ls-remote`」是我说错了**：那是**沙箱内**的现象；出沙箱 + `-c http.proxy=http://127.0.0.1:7897` 即可（本轮远端 `6cc851b` → `f390706` 都由我自验）。以后远端我自己验，不靠转述。
