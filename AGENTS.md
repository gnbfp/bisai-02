# AGENTS.md —— 本仓库的 AI 工作约定

> **本文件是唯一入口。读完这一份就有全部上下文。**
> **不要为了"了解项目"去通读仓库里的其他文档** —— 那会浪费大量 token，而且 `docs/` 下大部分是 MVP 开发期的历史过程记录，不是升级所需的信息。
> 只有当任务**确实需要**某个细节时，才按 §8 的索引去翻对应文件。

---

## 0. 工作目录铁律（最重要的一条）

- **本项目的唯一工作目录 = `D:\AI创新创业大赛`。** 所有读、写、git、运行命令都在这里。
- **不要碰 C 盘。** 尤其 `C:\Users\617\Documents\ChatGPT\AI创新创业大赛pro` 是一个**空的、与项目无关**的 git 仓库，不要在那里工作，也不要往 C 盘任何位置新建或修改文件。
- 如果沙箱提示 `D:\AI创新创业大赛` 不可写 → **申请授权**，不要退回去改 C 盘。
- 沙箱以 `CodexSandboxOffline` 身份运行，git 会报 `dubious ownership`。用**内联参数**绕过，不要改全局 git 配置：
  `git -c safe.directory=D:/AI创新创业大赛 <子命令>`

---

## 1. 这个项目是什么（30 秒版）

- **一句话**：一个**飞书群机器人**，扮演小组里的"项目经理"，角色设定是 **"群里的第 N+1 个成员"**。
- **主链路**：小组在飞书群丢一份作业书 → 机器人抽出评分点 → 群里投票定方向 → 拆成任务卡 → 私聊收志愿并自动分配 → 执行期催办 → 输出执行报告。
- **人的动作只有两个**：私聊填志愿、群里投票。
- **不做什么**：不提供思路、不替人写作业、不做开放式闲聊。只做三件事——**主持流程、匿名代言、发产物**。
- **形态**：**1 个 Agent + M0–M8 九个模块，单进程 Python 服务**；数据落仓库根 `data/` 的 JSON 文件，**不用数据库**。
- **比赛**：AI 创新创业大赛。交付物 = **运行视频 + 附件**（附件 = 软件运行所需的一切）。**截止 9/19**。

---

## 2. 仓库与远程（别搞混）

| 远程 | 地址 | 是什么 |
|---|---|---|
| `origin` | `https://github.com/gnbfp/-.git` | **队友在测试的 MVP 版本**，也是**升级失败时的保底提交方案** |
| `bisai` | `https://github.com/gnbfp/bisai-02.git` | **升级版仓库**，升级代码推这里 |

- 升级代码一律 `git push bisai main`。**不要动 `origin`**（除非用户明确要求）。
- 两个远程的 `main` 当前都停在 `9938ab7`（= MVP 完成态），带 11 个标签：`v0.2-intelligence` → `v0.9.1-review-fixes`。
- 本机 git 凭据账号 = `sy616313`（Windows 凭据管理器），对 `bisai-02` **有写权限，已实测推送成功**。
- 本机 git 默认分支是 `master`，但远程默认分支是 `main` —— 推之前先 `git branch -M main`。

---

## 3. 模块地图

| 模块 | 职责 | 代码位置 | 有没有 LLM |
|---|---|---|---|
| M0 | 机器人网关：消息收发 + 指令路由 + 状态机 | `src/gateway/app.py`、`router.py`、`events.py` | 无 |
| M1 | 输入解析：作业书 → 评分点 | `src/intelligence/extract.py`、`parse.py` | **有** |
| M2 | 方向共识：候选方向 + 群内投票 | `src/intelligence/direction.py` + `src/gateway/vote.py` | **有**（只生成候选） |
| M3 | 任务拆解：评分点 → 任务卡 + 自检循环 | `src/intelligence/decompose.py` | **有** |
| M4 | 志愿分配 | `src/gateway/preference.py`、`allocation.py` | 无 |
| M5 | 匿名代言 | `src/gateway/replies.py` | 无 |
| M6 | 催办 @提醒 | `src/gateway/complete.py`、`reminder.py` | 无 |
| M7 | 执行报告（分配总表 + 核对清单 + 甘特图） | `src/gateway/app.py` `_run_report()`、`src/report/` | 无 |
| M8 | 离线评测集（不进运行时） | `eval/` | 无 |
| — | 数据层（配置 / 模型 / 落盘） | `src/config.py`、`models.py`、`storage.py` | — |

**顶层指令 = 8 条**：`作业书` / `方向` / `拆解` / `你想做哪一块` / `我想提议：…` / `完成 T3` / `登记` / `报告`。
`封盘` **不是**顶层指令，只在 `awaiting=preference` / `awaiting=vote` 两个状态机内被消费。

---

## 4. MVP 期的硬性约束（升级时逐条判断要不要保留）

1. **不是三 Agent**。不存在 Brain / Feishu / Data 的进程或目录划分。
2. **全项目只有三处 LLM 调用**（M1 / M2 / M3）。M4–M7 是纯规则与模板，**零智能**。
3. **不用数据库**，数据存 JSON 文件，落盘只有仓库根 `data/` 一处。
4. **不写飞书文档**，不需要 `docx` 权限。
5. **指令用前缀精确匹配**，不做自然语言理解。
6. **LLM 不判定自己的产出**。判定权在代码（M3 循环内）或独立脚本（M8）。
7. **拍板永远在人**。机器兜底到建议为止。

> 这 7 条是 MVP 定稿时的边界。**升级方案可以推翻它们**，但推翻必须在 `requirements.md` §9 决策台账里记一笔并说明理由。

---

## 5. 当前状态（2026-09-15）

- **M0–M7 全部落地**，M8 只有"扩量评测（15 份）"这一项 P1 没做。
- **381 个单测全绿**（`python -m pytest -q`）。
- **D5 发布门三条已全绿**：① 测试群收发跑通 ② 命令行主链路跑通 ③ 5 份真实作业书覆盖率 5/5。
- **真机已走通**：指令路由、登记两步确认、作业书主链路、M2 群投票、M4 志愿分配 + M5 匿名代言、M6 催办 @提醒、M7 执行报告。
- **飞书侧实测**：自建应用凭据有效、WebSocket 长连接连得上、6/6 事件全收到、文件可下载。
- **时间线**：9/15 D5 发布门 → 9/16 指令层 → 9/17 交付层 + 全量评测 → 9/18 彩排 → **9/19 提交**。
- **仍然开放**：`待定义-37`（T01 那句"5 条"怎么改述）；`待定义-20`、`待定义-24`（背景类，不挡开发）。

---

## 6. 新对话怎么开工（省 token 规矩）

1. **只读本文件**。不要一上来就 `ls -R`、不要通读 `docs/`、不要通读 `src/`。
2. 需要看代码时**按需精确读**（`rg` 定位 → 只读那一个文件/函数），不要整目录粘贴。
3. 需要某份历史文档时才按 §8 打开**那一份**。
4. **唯一例外（必读、且很便宜）**：`docs/requirements/README.md`（**仅 6KB ≈ 2k token**）是**比赛验收口径** —— 功能优先级表 + T01–T13 验收用例。**任何涉及功能取舍、排期、砍需求、改验收方式的讨论，开口前先读它一次。** 它是评分依据，不读就会做出跑偏的方案。
   - 只读这份 6KB **转录**即可，**不用读那两张 PNG 截图**（图片才贵）；只有怀疑转录与截图冲突时才打开图。
4. 接手升级任务时，**先问清升级目标**，再决定改哪些模块 —— 别默认要把 M0–M8 全部重读一遍。

---

## 7. Git 约定

- 单仓库（monorepo），不拆模块仓库。
- 分支命名：`feat/<模块>-<内容>`、`fix/<内容>`。
- 升级期的主线推到 `bisai`；`origin` 保持 MVP 原样。
- **推送必须走代理**：本机 GitHub 直连会被墙（`github.com` 解析到 `20.205.243.166` 连不上，但 `api.github.com` 通）。本机代理 = `http://127.0.0.1:7897`，**已写入本仓库的 .git/config**（http.proxy）。若报 `Failed to connect to github.com port 443`，先确认代理软件在跑。
- 不配 CI（周期太短），但 **PR / 提交记录要留** —— 交付物含源码，提交历史是分工的证据。
- `.env`、`data/`、`eval/runs/`、`*.log` 已被 `.gitignore` 忽略，**永远不会入库**，别去改动这个设置。

---

## 8. 深度文档索引（**按需查阅，不要预读**）

| 文件 | 里面有什么 | 什么时候才需要 |
|---|---|---|
| `requirements.md` | MVP 需求唯一真源：硬性边界、数据模型、关键机制、未定义清单、决策台账（D-01～D-68） | 要查某个 MVP 决策的**理由**，或要改接口/数据模型时 |
| `docs/requirements/README.md` | **比赛验收口径**：功能优先级表 + T01–T13 验收用例（6KB 转录，成本极低） | **策划类任务开口前必读**；判断某个功能是不是比赛要求时。同目录两张 PNG 是原件，默认不用读 |
| `docs/ARCHITECTURE.md` | 架构设计 v1.2；**§12 = "从零到能跑"的上手清单**（飞书应用怎么建、开哪些权限、装什么依赖） | **要重新搭环境 / 重新配飞书应用时** |
| `docs/GLOSSARY.md` | 名词速查（「方向」「模块」「花名册」「D5 门」……） | 看到看不懂的专有名词时 |
| `eval/report.md` | 5 份真实作业书的覆盖率/均衡度复算结果 | 要引用评测数字时 |
| `tools/probe_feishu.py` | 飞书诊断探针（验凭据 / 长连接 / 文件投递），**不属于运行时，别被 `src/` import** | 飞书连不上、要排查权限时 |

---

## 9. 常用命令

```powershell
Set-Location D:\AI创新创业大赛

# 跑测试
python -m pytest -q

# 命令行主链路（不需要飞书凭据）
python -m src.main --file 作业书.pdf

# 跑飞书网关（长连接，不占端口、不需要公网）
python -m src.gateway.app

# 推送升级版
git -c safe.directory=D:/AI创新创业大赛 push bisai main
# 万一代理配置丢失，内联补上：
git -c safe.directory=D:/AI创新创业大赛 -c http.proxy=http://127.0.0.1:7897 push bisai main
```