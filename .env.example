# 复制成 .env 后填写。.env 已被 .gitignore 忽略，永远不要提交。
# 注意：必须是 NAME=VALUE 形式；没有变量名的裸值会被 src/config.py 直接拒绝。

# —— 飞书自建应用（docs/ARCHITECTURE.md §12.1）——
FEISHU_APP_ID=
FEISHU_APP_SECRET=
# 机器人自己的 open_id（兜底判据）：平台没给 mentioned_type=bot 时，认 @ 到的 open_id。
# 从群里 @机器人 的那条事件里抄，或用 tools/probe_feishu.py 探。
FEISHU_BOT_OPEN_ID=

# —— DeepSeek（OpenAI 兼容接口）——
# LLM_BASE_URL 请从控制台/文档确认完整地址后再填
LLM_BASE_URL=
LLM_API_KEY=
LLM_MODEL=deepseek-flash

# —— 落盘目录（唯一落盘位置，D-30；默认仓库根 data/）——
DATA_DIR=data
