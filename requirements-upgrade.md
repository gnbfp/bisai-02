# 升级需求 · v1.4（升级期唯一真源）

> **生成**：2026-09-15 · **修订**：2026-09-16（v1.4，U2 防呆收窄经 PM 认 + 三条件落点；v1.3 修 L1/§8#7 双向确认残留 + 补两处落点 + D- 台账落点改行尾指针）· **基线**：`bisai/main`（`origin` = MVP 保底提交，全程不动）
> **依据**：第 1 层 `docs/requirements/README.md`（功能优先级表 + T01–T13）＋ `requirements.md`（MVP 真源与 D- 台账）＋ 用户 2026-09-15 对话拍板
> **地位**：第 1 层（截图）仍高于一切（D-01 不变）；本文件是**升级期的主要依据**，与第 1 层冲突时以第 1 层为准。
> **范围**：本轮升级**只做** U1–U6 与契约 P1 的必要收口；未列入的（卡片按钮、云文档、多份作业并存等）默认不做。
> **建议**：把本文件登记进 `AGENTS.md` §8 索引（由前置准备负责人决定）。

---

## 0. 一句话

把机器人从"群里一个发指令的工具"改成"派驻到这个群的一名成员"：只在被 @ 时说话、每个群有自己的身份与数据、没有评分点的作业书也能拆、任务可变更、方向允许人拍板、所有输出说人话。

---

## 1. 目标与非目标

**目标（都可验收）**
1. 新群第一次接触不用读说明书：入群一句话说清能力，之后 @ 就能用。
2. 多群并行互不污染；一个群只有一份作业、一套数据。
3. 没有评分点的作业书走通全链路，不失败、不硬凑。
4. 分配可以改：换人 / 退出回流 / 加入补位，改完有据可查。
5. 方向既能投票收敛（T05），也能人工拍板（U6）。
6. 所有输出是自然语言句子，不是字段拼接。

**非目标（本轮明确不做）**
- 开放式闲聊、替人写作业、知识问答、点评作业质量。
- 写飞书文档 / 云文档 / 多维表格（`docx` 权限仍不申请，A2 不变）。
- 换数据库（继续 JSON 落盘）。
- 一个群并存多份作业（用户 2026-09-15 拍板：一个群一份）。
- 投票交互卡片、拆卡 / 并卡、匿名提交者查询（契约 P1，排 P2）。

---

## 2. 需求清单

### U1 群聊必须 @ 才响应（含闭嘴纪律）
- **用户故事**：作为群成员，我不想机器人对每句话都插嘴；我 @ 它的时候它再回应。
- **行为口径**
  - 群聊触发 = **必须 @ 机器人**；@ 后的文本按**功能词精确匹配**（沿用 MVP 路由表，仍不做自然语言理解）。
  - 匹配上 → 执行；口语化/近似能对上 → **先确认**"你是想做 X 吗"，不直接执行；完全对不上 → 回"我能做这几件事" + 一个最接近的候选；真没有 → 直说做不到。
  - 只 @ 不带文本 → 回能力清单（**群内可用的 8 条**；条数由前缀表按作用域派生，不写死，见 §6 L14）。
  - 私聊**不套** @ 规则（一对一没有噪音问题），沿用前缀。
  - **闭嘴纪律**：没有被 @ 就不说话。白名单播报 4 类：入群欢迎（仅一次）、投票发起、临期 @提醒（两档）、报告发布。
  - **状态机免 @ 窗口**：投票进行中允许免 @ 的纯数字回复与 `封盘`；志愿窗口内允许免 @ 的 `封盘`；窗口一结束立即恢复门禁。
  - 文件类指令：@ 机器人 + `作业书` → 取本群"最近一条还没解析的文件"；取不到 → 回"先把作业书发进群，再 @我一次"。
- **契约影响**：T01 需口径改写；T05 / T11 需例外（见 §3）。
- **推翻项**：群聊触发方式（记 D-69）。

### U2 一个群一份身份、一份数据
- **用户故事**：我加入机器人以后，只有这个群能用它；多个群同时工作，数据不互相污染。
- **行为口径**
  - 一个群 = 一个工作空间；群的飞书唯一标识就是它的"身份证"。
  - 落盘：**每群一个独立数据目录**（`data-upgrade\workspaces/<群标识>/` —— 按 D-75 与 MVP `data\` 物理分离；`data/` 全程只读、不改不删），MVP 的扁平 `data/*.json` 迁入对应工作空间；工作空间有**可读名**（如"计科2201-A组"），出现在报告抬头（落点：复用 `source_title` 渲染位，见 `docs/ARCHITECTURE-UPGRADE.md` §3.2）。
  - **私聊归属**：私聊必须绑定工作空间，规则是"绑定该用户最近一次群内互动的工作空间"；**歧义不反问**（PM 2026-09-15 裁，订正 D-70 字面）：L4「一个群一份作业」⇒ 单值映射天然无歧义，取"最近一次群内互动"即可；回话首行点明任务卡来自哪份作业书，用户自己就能看出归属错了。该用户还没在任何群里用过 → 回"先把我拉进你们的群"。
  - 防呆：清空 / 重置 / 改名三类动作要**二次确认**并记录执行人（**PM 2026-09-16 认**：本轮不做指令化。三条件 —— ① 落点 = 离线操作手册 `docs\OPERATIONS-U2.md`，**随 U2 一起交付**；② 执行人记录落点 = `data-upgrade\maintenance.log`（追加写，动手前先写）；③ 排期与分工见 `docs/ARCHITECTURE-UPGRADE.md` §12.4 / §13 与 §3.2 / §12.3 第 17 条）。
  - 机器人被移出群：不再响应，数据默认保留（清空走人工），口径见 §7。
- **契约影响**：契约无对应项（纯新增），但这是**改数据地基** → T01–T13 **整体回归复跑**。
- **推翻项**：D-57「单群假设」（记 D-70）。

### U3 没有评分点的作业书也能用
- **用户故事**：作业书没有评分点时，我也希望能拆出任务；组里拿不定主意时，可以拿机器人估的工作量当参考。
- **行为口径**（两条平行链路，互不污染）
  - **有评分点** → 现有链路不变，报告出**覆盖率**；T03 / T12 的口径**一字不改**。
  - **无评分点** → 工作量链路：M1 识别"无可引用评分点"时不报错、不拒收；拆分按工作量 / 难度出任务卡，每张卡带工作量估算与溯源字段（原文段落引用 + 估算依据）；报告出**工作量分布**，**不出覆盖率**。
  - 估算一律呈现为"参考值、可调整"，不得写成结论（例：`约 3 小时（可以改）`），不得出现"这是最优分配"这类断言。
  - **不得把"无评分点"当成"要求模糊"**：只有逐条内容模糊才走 T04 的"需确认"路径，否则整份作业书都会变成待确认项，等于没拆。
  - 估算值可被人工覆盖（与 U4 同一套变更台账）。
- **契约影响**：新增验收 = 无评分点作业书走通全链路、任务卡 ≥3 张且各有溯源字段（对齐 T02 的字段要求）。

### U4 任务可以改
- **用户故事**：分配不合理、有人中途退出或加入时，任务要能改。
- **变更类型**：P0 = 换人（改派）、退出 → 任务回流、加入 → 补位；P1 = 拆卡、并卡、工作量重估。
- **机制**
  - 一律**留痕**：谁、什么时候、从什么改成什么、为什么。
  - 换人与退出的确认形态（PM 2026-09-15 裁 **A 方案**，订正 D-72 字面）：**退出 / 接手 = 本人发起即确认**；**改派 = 组长直改即生效 + 被指派人可用 `我不做了 T3` 退回池子**。不做事前确认窗口 —— 少一个 `awaiting` 就少一处"窗口吃掉指令"的死锁面。
  - 变更触发一次群内公示（属白名单播报）。
  - 权限沿用既有 `roster.leader`（组长），**不新造角色**：组长拍板改派与"待确认项"，组员可以提变更请求。
  - 回流任务进"待认领池"，按既有兜底分配规则补位（T08 的兜底不砍）；**兜底之后仍有剩余** → 群内公示未分配清单，等人工拍板。
- **契约影响**：把表 1 的 P1「组长改派」提前交付（记 D-72）。

### U5 说人话
- **标准**：**完整句子、口语词序、不用字段分隔符与标签、不用敬语套话、不卖萌**。允许"机械"= 短、直、不修辞。
- **口径示例（合格 / 不合格）**
  - 机器语言（不合格）：`已登记｜任务:T3｜负责人:张三｜状态:完成`
  - 公文腔（不合格）：`系统已受理您的登记申请，任务 T3 的状态现已更新为已完成。`
  - 合格：`收到，T3 算张三做完了。`
  - 越界（不合格）：`太棒啦！张三你也太厉害了吧～`
- **红线**：人名、任务号、时间、数字必须**原样**输出，不得被润色成同义表达（"周五 18:00" 不能变成"这周末之前"）。
- **覆盖面**：回执、催办、报告导语、报错、兜底全部统一口径。群消息默认 ≤4 行，长内容进报告（与 U1 的闭嘴纪律同源）。
- **验收（主观口径）**：抽 10 条真实输出，由 3 名非项目成员盲评"像人还是像机器"，多数判"像人"为通过。

### U6 方向允许人拍板
- **用户故事**：机器人给的候选不够用时，我要能自己定方向。
- **输入**：`@机器人 我们要做的方向是：<任意文本>`（前缀 + 自由文本，与既有 `我想提议：…` 同一种形态）。
- **行为**：视为**直接落定**，不进投票；回执写清"方向定了：X"；写进方向台账（谁定的、什么时候）。
- **与候选的关系**：人工方向与既有 2–3 个候选之一高度重合 → 回"这跟候选 B 差不多，我就按 B 记了"，避免同义方向并存导致后面拆解分裂；不重合则作为新方向落定，候选保留为历史。
- **与匿名提议严格区分**：`我想提议：…` 是匿名（不泄露姓名，T09）；人工拍板方向是**署名**并落台账。两条路不能混，否则匿名机制会被绕过。
- **已落定后再改**：走 U4 的变更机制（组长确认 + 台账 + 群内公示）；**是否重拆已出的任务卡由人决定，不自动重拆**（理由同 D-68：重拆会让 M8 基线与门③复算作废）。
- **权限**：任何群成员都能落定；**覆盖已经定过的方向**要组长确认。
- **契约影响**：T05 不受影响（候选 + 数字投票仍要跑通）；新增验收 = 拍板口令回执 + 台账有记录；路由表 **8 → 12 条**（本条 + U4 的 3 条变更指令；PM 2026-09-15 裁 A 方案；记 D-74）。
- **注（2026-09-15 · PM 核）**：上面【权限】与【已落定后再改】两条**就是硬口径**（覆盖已定方向需组长确认、已出任务卡不自动重拆），`docs/ARCHITECTURE-UPGRADE.md` §5.3 引用它们；是 `docs/ARCHITECTURE-UPGRADE.md` 缺落点（v1.5 起改为引用）。

---

## 3. 契约对齐：T01–T13 逐条处理

| 契约项 | 原口径 | 升级后处理 | 理由 |
|---|---|---|---|
| T01 | 拉入测试群，发送任意消息 → 回指令列表（5 条） | 入群时**主动**发一条欢迎语（含 5 条高频指令 + "有事 @我"）；@ 后给**群内可用的 8 条**（条数由前缀表按作用域派生） | U1 要求"没被 @ 不说话"，与字面冲突，必须改写口径 |
| T05 | 「方向」→ 2–3 候选 + 回复数字投票，过半落定 | 不变；**投票进行中开免 @ 白名单**（纯数字 / `封盘`），结束即恢复门禁 | 一刀切 @ 会让投票变成全场刷屏 |
| T09 | 私聊提议 → 群内匿名发布，不泄露姓名 | 不变；**私聊不套 @ 规则** | 私聊一对一，没有噪音问题 |
| T11 | 临期未完成 → 群内 @负责人提醒（两档）＋送达记录 | 不变；催办属白名单播报，**不受闭嘴纪律约束** | 闭嘴纪律只约束"不接无关消息" |
| T02 / T03 / T12 | 任务卡带评分点溯源字段；覆盖率可复算 | 口径**一字不改**；无评分点走并行链路、不出覆盖率 | U3 不得污染硬指标 |
| T13 | 非作业书文本 → 拒拆或要求确认 | 不变；与"指令匹配失败"是**两套**兜底 | U1 兜底管指令识别，T13 兜底管内容合法性 |
| 待定义-37 | T01 那句"5 条"怎么改述 | **关闭**：入群欢迎语报 5 条高频，@ 后报**群内可用的 8 条**（派生、不写死） | 一处改写同时满足 U1 与 T01 |
| 表 1 P1 | 组长查询匿名提交者 / 改派 | 改派提前交付（U4）；匿名提交者查询仍不做 | 与 U4 同一套变更机制 |

**回归要求**：U2 动了数据地基，T01–T13 必须**整体复跑**，不是只跑受影响的几条；口径改写 6 处要在执行记录表里注明理由（T01 / T05 / T11 + 群内图片静默 / 私聊不反问 / 改派确认形态）（验收是人工清单 + 执行记录表，D3 / D-26）。

---

## 4. 双机器人隔离（升级版 vs MVP）

**目的**：队友继续在 MVP 机器人上调试（`origin`），升级版另开一个飞书机器人，互不污染。

**隔离四件套（缺一就算没隔离）**
1. **凭据**：升级版用新 app id / secret + 新 LLM 密钥，放独立环境文件；不复用 MVP 凭据。
2. **数据**：升级版数据根与 MVP 的 `data/` 物理分离（配合 U2 的按群目录）。
3. **进程**：两个长连接进程各自独立，互不订阅对方的群事件；日志分文件。
4. **测试群**：升级版用**独立测试群**，不与 MVP 调试同群（MVP 仍无 @ 门槛，同群会互相抢答）。

**凭据纪律**：密钥一律不入库、不贴进对话；`.env` 已被 gitignore（§7 不变）。审核架构时我只需要知道"凭据怎么放"，不需要看到密钥本身。

**验收**：给升级版机器人发消息，MVP 机器人零响应；反之亦然。两个进程的数据目录互不可见。

---

## 5. 优先级与时间盒

| 优先级 | 内容 |
|---|---|
| P0 | U1 + U5（都属"机器人的说话方式"，一起改最省事）；U2（数据地基）；U3（无评分点链路） |
| P0 尾部 | U6（量级很小：一条新指令 + 台账） |
| P1 | U4 最小集（换人 / 退出回流 / 加入补位 + 变更台账 + 确认形态 —— 见 L14，**不是**事前双向确认窗口） |
| P2 / 可砍 | 投票卡片、云文档、拆卡并卡、多份作业并存、匿名提交者查询 |

**时间盒**：9/16 上午 架构材料定稿送审 → **复审窗口 = 定稿后 2 小时内（判通过才开工；不通过则先做不依赖被卡节的部分）** → 9/16 U1 + U5 → 9/17 U2 + U3 → 9/17 收工**冻结** → 9/18 彩排 + T01–T13 全量回归 → 9/19 提交。U4 若挤不进去，9/18 只保"换人"一种变更类型。

---

## 6. 本轮锁定决策

| 编号 | 决策 |
|---|---|
| L1 | 变更权限 = 基于既有 `roster.leader` 的组长（**不新造角色**）；确认形态见 **L14**（退出 / 接手 = 本人发起即确认；改派 = 组长直改 + 被指派人可退回，不做**事前**确认窗口） |
| L2 | 工作量估算口径 = 参考值、可调整，不当结论播报 |
| L3 | 群隔离形态 = 每群独立数据目录，可整体备份 / 清空 / 演示重置；工作空间名可读 |
| L4 | 一个群 = 一份作业，不做多份并存（工作空间保持单层） |
| L5 | 触发口径 = 群聊必须 @；私聊沿用前缀 |
| L6 | 兜底分配保留（T08 不砍）；只有"兜底后仍有剩余"才公示等人工拍板 |
| L7 | 主动播报白名单 = 入群欢迎（一次）/ 投票发起 / 临期提醒两档 / 报告发布 |
| L8 | 话术 = 自然语言成句 + 事实字段锁定 |
| L9 | 方向 = 候选投票（T05）与人工拍板（U6）双通道 |
| L10 | 双机器人隔离四件套（凭据 / 数据 / 进程 / 测试群） |
| L11 | 私聊归属 = 单值映射 `index.user_last_group`（最近一次群内互动），**歧义不反问**；订正 D-70 的"反问"口径（行尾指针，PM 2026-09-15 裁） |
| L12 | 触发层边界 = 门禁**只拦群聊文本**；群内 `file` 静默缓存、群内 `image` 静默（订正 D-45 的群聊口径；行尾指针）；私聊沿用现状（PM 2026-09-15 裁） |
| L13 | 双链路总开关 = **是否存在 ≥1 条 `status="normal"` 的评分点**（含"列表非空但全是 `ambiguous`"）；只走一条链路，否决混算（PM 2026-09-15 裁） |
| L14 | 变更机制 = A 方案（组长直改 + 本人可退回，不做事前确认窗口）；**指令清单条数由前缀表按作用域派生**（当前群 8 / 私聊 7 / 路由 12）（PM 2026-09-15 裁） |

> **L11–L14 的来源**：PM 2026-09-15 对 6 处台账冲突的裁决（另两条不改字面：U6 两条硬口径**本来就在 U6**）。D-45 / D-70 / D-72 / D-76 **不回改台账原句、不续 D-77+**：由需求侧在 `requirements.md` §9 对应行**行尾追加统一指针**，模板 = `升级期订正：见 requirements-upgrade.md §6 L##（PM 2026-09-15 裁）`（映射：D-45→L12 / D-70→L11 / D-72→L14 / D-76→L14）。**本文件与架构材料里写的"订正 D-XX 口径"，一律指该行尾指针，不是回改原句。**

---

## 7. 开放项（不挡开发，各带建议默认值）

| 项 | 建议默认值 |
|---|---|
| 机器人被移出群 / 群解散后的数据处理 | 数据保留不自动删，清空需人工指令；汇报里不提 |
| 工作空间命名规则 | 入群时按"群名"自动取可读名；成员可改名，改名需二次确认 |
| U4 变更的"为什么" | 选填；不填仍记录操作人与时间 |
| 无评分点样本是否进 M8 评测集 | 进；与契约 P1「M6 全量评测 15 份」合并做 |
| 投票卡片是否并入本次 | 不并入，保持 P2 |

---

## 8. 架构材料审核清单（架构前按这 12 条过）

> **总规则：只认证据。** 每条结论都要落到可复核的证据（实测记录 / 数字 / 代码位置 / 输出样例）；只写设计意图的，一律判"**未验证**"，不算通过 —— 与契约的验收方式（人工清单 + 执行记录表，D3 / D-26）保持一致。

1. 双机器人隔离四件套是否齐（凭据 / 数据 / 进程 / 测试群）。
2. 数据层：工作空间落盘结构、MVP 旧数据怎么迁移、迁移失败怎么回退。
3. **触发层边界穿透（本轮最易翻车之一）**，要三样证据：
   - ① **状态 × 消息矩阵**：状态（空闲 / 投票中 / 志愿窗口中）× 消息形态（无 @ 闲聊、无 @ 纯数字、无 @ `封盘`、@ 非功能词、@ 功能词），逐格写明响应 / 静默 / 先确认。
   - ② **穿透用例实测记录（≥6 条）**：投票中发纯数字要计票；投票中无 @ 发 `作业书` 必须静默（不能因为"在投票态"就把门禁整体放开）；`封盘` 之后免 @ 白名单词必须静默（窗口关闭的边界）；投票中 @ 机器人发 `作业书` 要明确是拦截还是放行；机器人自己的白名单播报不得触发自我响应（回声 / 自激）；私聊免 @ 要响应、群聊免 @ 要静默（两套规则不串）。
   - ③ **白名单的机器可读定义**：允许免 @ 的词与允许的回复形态（纯数字范围、`封盘`）；窗口由谁关、超时怎么办（无人在投票时不能永久停在投票态）。
4. 路由表 8 → **12** 条（新增方向拍板 + 3 条变更指令）是否登记，前缀匹配的优先级 / 状态优先是否仍成立。
5. **双链路覆盖率分母零污染（本轮最易翻车之一）**，要四份证据：
   - ① **取数来源**：两条链路的覆盖率分母是否同一个（有评分点路径的 `status="normal"` 计数），给出代码位置。
   - ② **零污染实测**：无评分点作业书跑完后分子 / 分母各是多少（要能区分"不计算"与"算出来是 0"），且报告模板里那个位置不得被"工作量分布"顶替。
   - ③ **回归数字**：同一份作业书 + 同一份人工基线，改造前后 T12 的覆盖率必须一致，给出两次数字与复算输出。
   - ④ **混合作业书的裁决**：部分要求有评分占比、部分模糊或无评分时走哪条链路、分母怎么算，必须明确（不明确即判未通过）。**PM 2026-09-15 已裁：总开关 = 是否存在 ≥1 条 `status="normal"` 的评分点**（"列表非空但全是 `ambiguous`"一并走无评分点链路），只走一条链路、否决混算。
6. **私聊归属的歧义判定（第 3 条的姊妹项，属静默串数据）**：同一个人在多个群里时，私聊指令落到哪个工作空间；要给出判定规则 + 歧义时的兜底话术（**PM 2026-09-15 裁为"不反问"**：单值映射 + 回话点明作业书来源，供用户自查归属）+ 一条"双群用户私聊"的实测记录。
7. 变更机制：台账字段、确认形态的流转（L14）、回流池与兜底分配的关系；**跨文件原子性与认领竞态**（`assignments.json` 与 `changes.json` 的写序、两人抢同一张卡）。
8. 失败路径：只 @ 无文本、文件找不到、私聊归属不明、无人志愿、LLM 超时或报错 —— 降级话术是否都是人话。
9. 演示可回退：每条演示链路是否都有"不需 LLM"的保底路径。
10. 话术层：事实字段是否模板化锁定，长消息会不会刷屏。
11. 口径改写 **6 处** + D- 台账新增条目是否与 `requirements.md` §9 一致（核对对象已入库：D-69～D-76；PM 裁决另见本文件 §6 L11–L14）。
12. 排期与冻结点是否与 §5 一致；P2 是否有明确的"砍掉"开关。

---

> **变更记录**：
> - 2026-09-15 首版：U1–U6 六条需求 + 契约对齐 + 双机器人隔离 + 审核清单 11 条。
> - 2026-09-16 **v1.4（U2 防呆收窄经 PM 认）**：§2 U2 的"防呆"行补三条件（手册 `docs\OPERATIONS-U2.md` 随 U2 交付 / 执行人记录落点 `data-upgrade\maintenance.log` / 排期与分工写进架构材料 §12.4 与 §13）；口径细节见 `docs/ARCHITECTURE-UPGRADE.md` §3.2 / §3.6 / §12.3 第 17 条。
> - 2026-09-16 **v1.3（14 条清单回写）**：订正 **L1**（与 L14 打架的"双向确认"）、P1 行（`:146`）、§8 第 7 条（`:205`）共 3 处；U6 注里的"误判"改述；§3 与 §8 第 11 条的"口径改写 3 处"改 **6 处**；U2 补两处落点（报告抬头可读名 / 防呆三动作走离线手册）；文件行尾按 `.gitattributes`（`eol=lf`）存回 **LF**；PM 2026-09-15 另裁：**D-45 / D-70 / D-72 / D-76 不回改台账原句**，落点 = 在 `requirements.md` §9 各行行尾追加统一指针（本版已落）。
> - 2026-09-16 **v1.2（PM 裁决回写；裁决 09-15 夜，回写 09-16 晨）**：新增 §6 **L11–L14**（私聊不反问 / 门禁只拦群聊文本 + 群内图片静默 / 双链路总开关 / A 方案 + 清单条数派生）；订正 U1 条数、U2 落盘路径与私聊归属、U4 双向确认形态、U6 契约影响（8→12 条）并加引用注；§8 第 4/5/6 条改为已裁口径；§5 加复审窗口。
> - 2026-09-15 v1.1：审核清单扩到 **12 条** —— 第 3 条与第 5 条由"设计意图"改为**证据要求**，新增第 6 条「私聊归属的歧义判定」，并加"只认证据"总规则。**注意**：`AGENTS.md` §6 第 0 条与 §8 索引行写的是"11 条"，需同步改成"12 条"。
